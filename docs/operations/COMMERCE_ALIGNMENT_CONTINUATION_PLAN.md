# Commerce Alignment Continuation

This is the saved boundary record for completing [COMMERCE_ALIGNMENT_E2E_PLAN.md](../product/COMMERCE_ALIGNMENT_E2E_PLAN.md). [AGENTS.md](../../AGENTS.md) owns execution; [the active checkpoint](checkpoints/COMMERCE_ALIGNMENT_EXECUTION.md) is the only mutable task ledger and next-action record. Use its stable CA IDs and exact phase titles. Search [prior evidence](checkpoints/COMMERCE_ALIGNMENT_EXECUTION_HISTORY_20260909.md) only for a specific missing claim; historical pauses and next actions do not apply.

Preserve these owner/session boundaries:

- Leave `.codex/config.toml` and `docs/product/SIMPLIFICATION_DISCUSSION.md` untouched and outside commits, including the current local deletion of config. The discussion records agreed product decisions; the active Product guide maps them without inferring missing rules or treating proposals as implementation authorization.
- Preserve unrelated local deletions and all unfinished commerce files. Reconcile Git and actual code before editing. Never reset or stage the whole tree.
- `docs/product/IMPLEMENTATION_STATUS.md` has pre-existing encoding damage. Preserve its bytes; append bytes only if necessary. It is historical evidence, not the current task ledger.
- Only `apps/core/.wrangler/e2e-commerce-alignment-20260907` is identified as disposable. All other local/shared/retained/remote data remains protected unless its ownership and authorization are established. Changing an applied migration does not upgrade an existing database.
- Do not overlap Core suites with managed browser stacks. Keep source fixed during browser acceptance and record process/session state. Verify stale process claims on resume.
- No subagents. No inferred deployment, live payment/refund/booking, outbound-message or remote-reset authorization. Provider/account activation and operational values remain external factual inputs.

Keep all criteria in sections A-I, the audit-defect list, phase table and final journeys assigned to the checkpoint ledger. A split adds child task IDs without dropping the parent coverage. Distinguish implemented, locally verified, actually provider-accepted and externally blocked work. Count remaining work explicitly as major phases, slices or acceptance obligations; test totals are not completion percentages.

The owner authorized GD-1 guidance consolidation on 2026-09-09, followed by automatic continuation of the original implementation. All nine subsequent product discussion choices are approved as recorded in the active checkpoint; the protected discussion remains unchanged. The owner resumed from the checkpoint after the model handoff. GD-1 was verified, committed and pushed as `83854bd`; the [guidance audit](GUIDANCE_REBUILD_AUDIT.md) records the preserved requirements and approved changes. Continue the saved implementation using the active checkpoint. Guidance work does not accept CA-4.2 or erase CA-0–2/CA-5–7 obligations.
