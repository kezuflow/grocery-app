# Schema remediation

Status: completed, 2026-09-07 13:28 UTC. Changes remain in the working tree.

Task: owner requested correction of all findings in the schema audit before continuing the commerce phases. Scope is integrity, relationships, query efficiency/pagination, single pool ownership, financial identity/status, and delivery attempt persistence. Future warehouse, receiving allocation and media feature delivery remains in the owning commerce phases.

Workspace: main, observed HEAD f5383a9. Pre-existing harness/documentation and staff-invitation changes are retained; migration 0068 belongs to that work. Initial tracked diff and status saved outside the repository with the audit artifacts. No database reset or deployment is authorized by this work.

Completed: migration 0069 hardens application tables with STRICT types/safe integer bounds, non-null primary keys, missing operational FKs, null-safe exact-demand checks, valid pickup phones, useful query indexes, unique provider identity, refund projection removal and sole Product-to-pool ownership. Delivery storage supports multiple immutable external/manual attempts with one active/uncertain attempt and structured manual assignment evidence. External booking identities/replay, latest-attempt reads and superseded-event rejection are updated. Order history is bounded end-to-end with filtered keyset pages and loading/error/retry UI. Inventory's guarded UPSERT now uses a valid insertion candidate so stock removals reach the guarded conflict update. Fixtures and canonical documentation are aligned. Catalog regeneration exposed and corrected pre-existing 0025 manifest-output drift; its generator remains scoped to the historical 0025 schema.

Remaining product work: manual delivery commands/screens, warehouse transfers, cycle goods and media publication remain owning commerce phases; schema support is not feature acceptance. No schema-audit remediation item remains open in this scope.

Validation: `pnpm check` passed, including 846 Core tests / 157 files, 358 Web tests / 88 files, shared/contract tests, 25 harness tests, conventions, migrations, lint/types and both builds. Additional final regressions passed: 11 delivery tests after bounded attempt-ID refinement; four Web route/page tests; and an actual Worker/D1 populated-upgrade test proving full schema rollback on invalid stock, preservation of every application/auth row and row ID, foreign keys, and per-table integrity checks. The browser order-history test passed on desktop/mobile with controlled RPC DTOs. `pnpm catalog:check`, `pnpm --filter @freshmarkets/web check:vinext`, and final format/naming/architecture/lint/types/diff checks pass. Logs are retained with the original external schema-audit artifacts.

Resolved verification issues: a default unbounded full run crashed workerd without an assertion diagnostic; bounded worker concurrency is now configured. RPC boundary assertions moved out of application tests into the entrypoint tests. D1 disallows `integrity_check`; whole-database `quick_check` hit SQLite's allocation limit, so the Worker upgrade test checks every application/auth table individually, while the Node verifier retains full `integrity_check`. No assertion or business guard was suppressed. Build output retains existing Wrangler environment-selection and Web chunk-size notices; no production/provider acceptance is inferred.

Next: resume the authorized commerce implementation phase from its current status. Existing local/shared databases remain retained/unclassified; no migrations/reset/deployment applied to them. Invalid retained records must fail upgrade, never be silently deleted/coerced. Temporary migration copies require storage headroom and retained deployment validation on a backup. The temporary browser development server has been stopped. Unrelated harness and staff-invitation work is preserved.

## Owner follow-up — policy placement, 2026-09-07

Removed the proposed Scheduled-only manual INSERT trigger, manual-only status subset and completion-status handover requirement from the 0069 generator and regenerated migration. Retained required manual assignment identity, consistent recorded timestamps, immutable history, one active/uncertain attempt and pending-cancellation exclusion. No existing database was migrated/reset. An environment that has applied a different 0069 needs a tested forward upgrade; editing the file does not update it.

Core still owns the agreed Scheduled-only manual policy. Its commands/UI remain pending. The saved commerce plan now assigns eligibility, state transitions, packed handover, completion evidence, direct-RPC rejection, replay and race tests to Phase 6, and records other policy reviews in their owning phases. AGENTS, coding standards, data/state/contracts and design decisions describe the same enforcement boundary. This follow-up does not claim all older schema policies have been removed or that manual delivery is implemented.

Follow-up validation: `pnpm migration:check` passed fresh application and retained-baseline scenarios including populated 0068 preservation/rollback. Four focused Worker/D1 files passed all 19 tests (schema integrity, populated schema upgrade, provider requests and Admin provider operations). The schema tests distinguish direct storage flexibility from business permission and retain rejection checks for malformed assignment identity, inconsistent timestamps and duplicate attempts. Naming, lint, Core typecheck, formatting, architecture/readiness checks and all 25 harness tests passed. A test-fixture type error was corrected before the final typecheck. The earlier aggregate/build/browser evidence above predates this follow-up; those checks were not rerun for this bounded schema-policy change.

## Additional policy placement completed — 2026-09-07

Removed component-count uniqueness from checkout_promotion_claim and order_promotion_application, including the null-amendment expression index. Replaced it with promotion-benefit identity plus unique redemption application. Removed the three promotion usage-count triggers, current/historical global mode/cadence coupling CHECKs and the obsolete service_fee_configuration.active_for_new_commerce flag. Historical migrations retain their original boundary semantics; final 0069 is regenerated from its source. Retained field comparisons exclude the removed constant-zero flag while preserving the historical financial/configuration data.

Core still enforces current stacking at Quote claims and paid commitment, with transaction-time claim-set validation and per-claim redemption idempotency. Global/customer usage checks already existed in the commitment transaction; grant checks now cover system as well as targeted grants. The retained trial writer receives an atomic grant guard before trigger removal. Current configuration reads reject unsupported stored cadence. New quotes still carry zero Service Fee and never use the retired fee table as an active pricing source. No new cadence, stacking, fee, membership or manual-delivery feature is enabled.

The saved plan records each change, remaining phase acceptance and the broader Core-policy review list; canonical domain/data/contracts, coding guidance, implementation router and migration documentation agree. No existing database was migrated/reset and no deployment, commit or push was performed. Previously applied variants of 0069 require a separately tested forward upgrade; do not rerun this edited migration over them.

Validation: final `pnpm check` passed: 857 Core tests across 158 files, 360 Web tests across 89 files, 68 contract tests, shared package tests, 25 harness tests, fresh/populated migration verification, lint/types/convention/security checks and both builds. Focused tests prove competing global/customer/targeted/system-grant commitments have one winner, replay safely and leave no partial losing Order/redemption; Core rejects a stored extra stack and unsupported cadence; storage accepts distinct benefits and rejects duplicate identities. Worker/D1 upgrade/rollback tests pass. Initial focused failures came from invalid duplicate-active-cart fixtures and leaked test grant/promotion configuration; fixtures were corrected without removing policy/race assertions. Full log: `C:/Users/reggi/.codex/visualizations/2026/09/07/01a07ac6-9304-74e0-9c5e-5b7ed403f049/schema-policy-check.log`. Existing Wrangler environment-selection and Web chunk-size notices remain. No browser/provider acceptance was rerun or inferred from these checks.
