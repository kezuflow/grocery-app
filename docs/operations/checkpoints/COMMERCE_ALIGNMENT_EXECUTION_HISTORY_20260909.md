# Commerce alignment execution

## Active execution - 2026-09-08 20:00 UTC

Main is e50ef9a (customer-aware preview/selection), following f6f8c88 canonical delivery benefits. Current verified slice adds getAdminPromotionAudience/setAdminPromotionAudience RPCs and same-origin audience GET/PATCH, a draft Save audience editor for all five approved condition types, named Customer/segment choices, conjunctive matching, current reference/IAM/state checks, complete replacement/audit/frozen receipt and original replay after activation. Strict shared rule parsing rejects malformed/retired conditions at new checkout; activation requires explicit replacement of unsupported retained conditions. No new migration. Segment assignments remain Customer-owned and are not edited by these RPCs.

Executed: 99 Core tests across seven audience/preview/command/Instant Quote/paid commitment files passed, then all 13 audience tests passed after adding customer-label privacy and local-scope denial. New reachable Admin create -> audience save -> activate -> Instant Quote verifies the applied claim and definition version. Web 400/100 and contracts 68/19 passed; types/lint/format/naming/architecture/readiness, both builds and both Wrangler binding --check commands passed. Nine browser journeys passed with zero retries, including three-condition audience save after a lost response, minimum-based rejection, eligible customer preview and grant replay on mobile. Screenshot inspected. Logs promotion-audience-* under the existing visualization directory. No running stack/suite remains; save/push then continue.

Next: R2 campaign image upload/preview/replace/remove and anonymous publication, with independent media version so image edits never rewrite financial benefit authority. One existing PRODUCT_MEDIA bucket, promotions namespace, internal durable intent/unknown-outcome observation/bounded cleanup, no operator recovery workspace. Inspect existing product-media command/recovery/publication and cleanup patterns; do not duplicate legacy compatibility behavior needlessly. Then warehouse transfers, exact Scheduled procurement/receiving/allocation, preparation delivery/manual fallback and remaining onboarding/membership removal/full acceptance. No whole phase or external provider acceptance. Only apps/core/.wrangler/e2e-commerce-alignment-20260907 is disposable. Preserve unrelated .codex/config.toml AND newly appeared untracked docs/product/SIMPLIFICATION_DISCUSSION.md outside commits. Append status bytes only. No subagents, routine approvals, retained reset or deployment.

## Prior customer-preview slice - 2026-09-08 19:44 UTC

Main is f6f8c88 (canonical delivery benefits). Current verified slice: named Customer search/selection for promotion preview and grants, customer-aware preview through the existing checkout evaluator, explicit amount-only estimate semantics, and transactional grant validation against active Customer plus matching active commerce principal. Three preview regressions and the separate disabled-principal grant race reproduced before repair. Full customer-preview check requires Global customers.read as well as promotions.read; no new claims/redemptions are written. No schema or new RPC method; preview adds optional customerId and returns eligibilityChecked.

Executed: 66 focused Core tests (six files, including Instant quote and grant races), 400 Web tests/100 files, 68 contracts/19, types/lint/format/naming/architecture/readiness and both builds pass. Nine real Admin promotion browser journeys pass zero retries, including mobile Customer profile -> named selection -> eligible preview -> lost-response grant/replay. Screenshot inspected. Test-iteration failures were an extra DTO field in an old exact assertion, a malformed fixture permission SQL replacement, and missing mandatory grant_id in seeded retained usage; corrected without weakening business assertions. No tests or stack remain running. Logs/screenshots promotion-preview-* under the existing visualization directory. Save/push then continue.

Next: closed audience-rule authoring and R2 campaign media/publication, then warehouse transfers, Scheduled exact-demand procurement/receiving/allocation, delivery/manual fallback and remaining onboarding/membership removal/full acceptance. Full aggregate last passed for the f6f8c88 slice, not rerun for this bounded slice. No whole phase/provider acceptance or retained database operation. Only apps/core/.wrangler/e2e-commerce-alignment-20260907 is disposable. Preserve .codex/config.toml, append docs/product/IMPLEMENTATION_STATUS.md, never overlap Core suites with managed browser stacks or edit source during browser runs. Owner authorizes continuation without routine approvals/subagents.

## Prior delivery-benefit slice - 2026-09-08 19:31 UTC

Main was 762a18e after promotion transaction, variant and membership-eligibility repairs. Current verified slice adds canonical delivery waiver/percentage/fixed authoring, caps, total/customer redemption limits and automatic selection; draft saves and all RPC boundaries share bounded schemas. Checkout and amount preview share integer discount calculation; unsupported retained benefit types/parameters fail closed. Web exposes all benefits and active configuration. New 0085 forward migration preserves the complete retained six-table promotion graph/rowids/paid snapshots and translates only legacy definition types with a version increment. Generator uses an in-memory 0084 baseline. No retained database was reset or upgraded.

Validation: pnpm check passed (1433 Core tests/187 files, 398 Web/99, 68 contracts/19, shared/harness, migration checks, lint/types and both builds). Final retained-parameter guard followed by 55 focused Core tests passed. All 8 promotion browser tests passed with zero retries; after visual review added active caps/limits and reran all 3 delivery author/preview journeys successfully, zero retries. Mobile screenshot inspected. Workspace types, catalog generation and vinext compatibility passed; final format/naming/diff checks follow this record. Logs promotion-benefits-*.log and screenshots under C:/Users/reggi/.codex/visualizations/2026/09/08/01a0822b-3c98-76a1-8c9e-a2d5e223a182. Browser stack and Core suite finished. Save/push verified slice then continue.

Next: named customer selection for grants, customer-aware preview through the Quote policy, closed audience authoring, R2 campaign media/publication. Then central transfers, Scheduled procurement/receiving/allocation, delivery/manual fallback, remaining onboarding/membership routes/jobs/notifications and full acceptance. No whole phase accepted. Only apps/core/.wrangler/e2e-commerce-alignment-20260907 is disposable; never overlap its browser stack with Core suites or change source during browser runs. Preserve unrelated .codex/config.toml; append only docs/product/IMPLEMENTATION_STATUS.md. No real provider/account operations or deployment performed. Owner authorizes continued dependency-ordered implementation without routine approvals or subagents.

## Prior membership-eligibility slice - 2026-09-08 19:04 UTC

Main is d14de2c after the verified promotion transaction/draft UI slice; prior commits b8e32b2 and f7f17dc remain pushed. The owner still authorizes the full commerce plan, no routine approvals or subagents. Preserve .codex/config.toml. Append status only to docs/product/IMPLEMENTATION_STATUS.md.

The current bounded slice removes subscription entitlement from new-commerce promotion evaluation. MEMBER and NON_MEMBER are absent from its closed domain types/parser; retained unsupported rules fail closed to an empty eligible-customer set. History is not changed. Two genuine failing Worker regressions were reproduced, then fixed. Normal evaluator fixtures no longer create subscriptions; dedicated retained-history cases still do. Added a reachable Instant quote with no subscription: both retired codes are INELIGIBLE and create no promotion claims.

Executed: 52 tests across promotion domain/evaluator, Instant quote and actual payment-reaction integration files pass; Core types/lint pass. No schema, RPC signature, Web source or provider changes in this bounded slice. Existing browser evidence is from d14de2c and is not new browser acceptance for this rule. Logs promotion-membership-regression.log and promotion-payg-worker.log under C:/Users/reggi/.codex/visualizations/2026/09/08/01a0822b-3c98-76a1-8c9e-a2d5e223a182. No stack/Core test remains running. Save/push this slice then continue.

Remaining dependency order: complete canonical delivery benefits and promotion targets/limits/preview, R2 campaign images/publication, central transfers, exact Scheduled purchasing/receiving/allocation, operational delivery/manual fallback, remaining onboarding and membership routes/jobs/notifications, full customer/operator acceptance. Current stored DELIVERY_FEE_DISCOUNT means percentage; canonical types require DELIVERY_PERCENT_DISCOUNT and DELIVERY_FIXED_DISCOUNT in addition to waiver. Schema currently ends at 0084. Retained/shared databases are unclassified and must be preserved; only apps/core/.wrangler/e2e-commerce-alignment-20260907 may be rebuilt. New schema work needs clean creation and a tested retained baseline upgrade. Do not overlap Core suites with managed browser stacks or change source during browser tests. Full aggregate last passed at b8e32b2; no full phase is accepted. No real provider/account operations or deployment performed.


## Prior verified promotion transaction slice - 2026-09-08 19:00 UTC

Main contains b8e32b2 transaction repairs, f7f17dc variant editor, and 17744d8 status-path correction. The owner still authorizes every remaining commerce workflow in dependency order, without routine approval or subagents. Preserve unrelated .codex/config.toml outside commits. Product status is docs/product/IMPLEMENTATION_STATUS.md; append bytes only.

The current verified promotion slice repairs create/edit/status/grant complete transactions and original results. Twelve real effect/replay regressions reproduced (one suppression scenario already passed); the first exploratory run also exposed a test total-count assumption, corrected to command-specific identities before the recorded regression run. Repaired commands validate unknown inputs with bounded schemas, recheck Global current IAM and mutable state in the transaction, require write/audit/receipt effects, replay before later lifecycle rejection, and recover lost responses from frozen JSON receipts. No new migration/RPC signature or business benefit vocabulary in this slice. Web uses bounded adapters and preserved complete requests. Added the missing draft campaign edit form; date fields are explicitly UTC.

Executed: 25 Core Worker/D1 tests across promotion effects/admin suites, including authority revocation, activation/edit race, deactivation/grant race, concurrent identical create with lost batch responses. All 398 Web tests (99 files), 2 shared validation tests, workspace typechecks, lint, formatting/naming, architecture/readiness and Core dry-run build pass. Managed Web build/clean disposable D1 and 5 promotion browser tests pass with zero retries, including lost-response create/edit/activate and denied access. Screenshot inspected. Evidence logs: C:/Users/reggi/.codex/visualizations/2026/09/08/01a0822b-3c98-76a1-8c9e-a2d5e223a182/promotion-*. Full aggregate last passed at b8e32b2; no whole phase accepted.

Next after saving/pushing this verified slice: complete canonical promotion benefits (delivery waiver/percentage/fixed), controlled targets/limits and preview using the Quote policy, remove active membership eligibility, and campaign media/publication. Current evaluator still uses legacy DELIVERY_FEE_DISCOUNT and membership entitlement; Admin supports merchandise only; raw customer-ID grant control and incomplete preview remain. Review migrations 0029/0047/0069 and retained baseline policy before any schema work. Then continue transfers, exact Scheduled allocation/receiving and delivery/manual fallback, remaining onboarding and full acceptance. Only apps/core/.wrangler/e2e-commerce-alignment-20260907 is disposable; no retained reset/provider operation was performed. No browser/Core process from these runs remains active. Keep source fixed during browser tests and never overlap Core suites with their stack.

## Prior verified variant slice - 2026-09-08 18:45 UTC

The owner still authorizes every remaining commerce workflow in dependency order, without routine approval or subagents. Transaction repairs are committed and pushed as b8e32b2. The Global variant editor is committed and pushed as f7f17dc; preserve the unrelated .codex/config.toml change outside commits.

Variant editing now exposes display name, optional label, display order, active/inactive status and Piece shipping estimates through the existing guarded Core PATCH command. Consumption stays fixed. Local views show catalog status and retain separate local selling controls. One automatic response-loss retry and subsequent Save preserve the complete original body/key; unconfirmed fields remain frozen. There is no extra recovery panel.

Executed evidence: 22 catalog Worker/D1 tests, 2 editor Web tests, Web typecheck, lint and naming pass. The new Worker case verifies label omission versus explicit null, original receipt replay and stale/different-payload rejection (initial expected generic error codes corrected to the specific existing IDEMPOTENCY_CONFLICT/STALE_VERSION results). Desktop/mobile browser journeys both pass with zero retries: real product/variant create, variant edit, price and local activation, with lost-response replay for each write. Managed Web build and disposable clean D1 setup pass; browser setup took about two minutes. Mobile result inspected. Logs and screenshots are under C:/Users/reggi/.codex/visualizations/2026/09/08/01a0822b-3c98-76a1-8c9e-a2d5e223a182/variant-editor-*. Full aggregate passed at b8e32b2; not rerun for this bounded UI slice. No schema/RPC signature changes or real provider operations.

Next: promotion commands need atomic complete effects and original-result replay before extending authoring/media. Inspection found split update/audit writes, post-commit effect checks, lifecycle validation before replay, and missing transaction-time IAM rechecks. These are observations awaiting regression evidence. Promotion delivery benefits/campaign media and later transfer/Scheduled/delivery workflows remain incomplete. Keep browser stacks and Core suites separate. Only apps/core/.wrangler/e2e-commerce-alignment-20260907 is disposable. Preserve retained state. Continue promotion repair from the committed variant slice.

## Prior verified transaction slice - 2026-09-08 18:32 UTC

Owner request remains implementation of every remaining agreed commerce workflow in dependency order, without routine approval or subagents. Main was 4b611c5 at resume; the verified cancellation/catalog slice was committed and pushed as b8e32b2. Preserve the pre-existing .codex/config.toml change separately. Earlier handoff is retained below as context, not current failure status.

Completed in this slice:
- Scheduled cutoff cancellation survives real START_PICKING acceptance before/at/after cutoff; Instant late-state and delivered/terminal rejection remain. Paid acceptance and its required audit guard are verified. PayMongo refund reason uses `others`, supported by both official guide/reference; request-body test asserts it. Resource reference also allows the older reason, so this is documented compatibility alignment, not proven provider rejection. Canonical stale preparation/trial/media-cleanup wording and PayMongo runbook corrected.
- Unit/SKU/update/local activation/Global price repair uses the existing catalog transaction helper, bounded shared input, mutable IAM/unit/location/currency rechecks, every required write/audit/receipt effect, frozen original results and exact target fields. Concurrent prices allocate distinct SKU-wide versions in the transaction. Eleven original failures reproduced; expanded 21 effect/race/replay tests pass. Fixtures use actual canonical Gram/Piece pools.
- Web adapters validate bounded shared schemas; variant quantities no longer round; gram variant requests omit nonapplicable shipping estimates. Price choices show names, Save price defaults to now, scheduling/history are secondary. Product image CRUD remains ordinary; no operator cleanup/recovery workspace restored.

Executed verification:
- `pnpm check` passed: Core 1405 tests/185 files (356.45s), Web 396/98, contracts 68/19, shared/harness, clean/populated migrations, conventions, lint/types and both builds. `commerce-check.log`.
- `pnpm catalog:check`, Web `check:vinext`, final format/naming/diff checks pass. New browser locator corrections are test-only and postdate the aggregate.
- Managed browser first run: 7 passed, 2 new variant tests failed solely because the final locator expected raw AVAILABLE instead of displayed Selling. Corrected locator rerun passed both 1440/390 journeys with zero retries. `commerce-browser.log`, `catalog-variants-browser.log`.
- Passed browser coverage: real Web/Core/D1 variant create, price and local activation after lost responses with identical key/body; product upload/local R2/cart/anonymous rendering/removal; staff refund submission/recheck with the test provider and lost-response recovery. Price presentation and customer cancellation messaging use controlled DTOs and are not complete payment journeys. Variant desktop/mobile screenshots inspected and copied outside test-results.

All logs and saved screenshots: C:/Users/reggi/.codex/visualizations/2026/09/08/01a0822b-3c98-76a1-8c9e-a2d5e223a182.

No schema change, retained DB reset/migration, deployment or actual provider action. Only apps/core/.wrangler/e2e-commerce-alignment-20260907 was rebuilt by managed tests. Browser/Core processes finished. No whole commerce phase or actual PayMongo/Lalamove/OAuth/email acceptance claimed. Account/retention/accounting facts remain external acceptance inputs.

Next: commit/push this verified slice to main, then continue the active plan. Remaining catalog work includes normal variant editing, routine form simplification, promotion authoring/benefits and promotion images/publication. Warehouse transfers, complete Scheduled purchase/receiving/allocation/surplus, preparation-stage delivery/manual fallback, membership removal and full customer/operator/provider acceptance remain. Existing setup/media/financial work must not be restarted from old reports.

## Previous handoff — 2026-09-09, owner moving to a new Astra High chat

Implementation is authorized across all remaining phases of `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, in dependency order. The owner paused this execution only to obtain a prompt for the new chat. Do not treat the earlier workflow-review pause below as current authority. No whole commerce phase is accepted.

Saved/pushed main: `4b611c5 fix(catalog): remove image recovery operator workflow`, after Product `587ca02`, Category `e51a2bd`, media `dc74065`. The working tree has unfinished changes; inspect and preserve them. No new commit or push was made during this resumed policy/acceptance slice.

### Current owner decisions

- Ordinary product images are CRUD: upload/preview/replace/remove, primary image, order and alt text. The operator recovery panel and storage/cleanup administration were removed in `4b611c5`; preserve that removal. Internal duplicate/publication/cleanup safeguards remain invisible.
- Reviewed simplifications are authorized: normal Create/Save actions, named choices and sensible defaults for ordinary catalog/category/price/location/hour/cycle setup. Avoid technical versions/IDs, dedicated saved-command recovery workspaces or mandatory reasons merely for routine saves. Keep atomicity, authorization and stable internal request identity. Price scheduling/history and formal privacy workflows are secondary; unfinished irreversible anonymization waits on real policy. Do not replace every working module or restart completed setup.
- Provider-confirmed successful payment precedes staff acceptance. Instant paid Orders are customer-cancelable while awaiting location acceptance; acceptance/start picking is the existing `COMMITTED -> FULFILLMENT_PENDING` boundary. Scheduled customer cancellation is allowed strictly before the snapshotted cutoff, regardless of earlier preparation; equality/after closes that window. Refund original payment plus every committed paid addition. FreshMarkets-caused full-refund obligations remain.
- Eligible customer cancellation automatically initiates PayMongo refunds, without staff approval. Show processing until provider confirms success; do not promise immediate bank credit.
- **Latest correction: staff-reviewed post-delivery refunds are issued from FreshMarkets' own admin dashboard through the PayMongo API.** The owner first requested PayMongo-dashboard execution, then explicitly changed to FreshMarkets-dashboard execution. Retain and finish the existing `requestAdminRefund -> requestStaffRefund -> PayMongo requestRefund` path; do not replace it with a separate dashboard-import workflow. Staff chooses the approved full/partial amount and reason, confirms, and Core enforces Global `refunds.manage`, current financial preconditions and immutable evidence. Keep detailed financial diagnostics, provider confirmation and reconciliation.
- Order/payment/delivery/cancellation/refund transaction updates always send; promotions alone are optional. Email transport/retry internals remain internal; staff may see meaningful delivery failures and appropriate resend. Keep useful operational metrics and customer courier charge versus actual cost. Profit/contribution waits on reliable costs and an approved definition.

### Uncommitted implementation and actual checks

Canonical AGENTS/domain/state/data/API/product scope/plan documents were revised for these agreements; descriptive status below is historical. Review the current diff for consistency, especially other untouched cancellation/preparation wording. No schema change was made.

New uncommitted cancellation/fulfillment work:
- `orders/domain/cancellation-policy.ts` now applies Scheduled cutoff independently of earlier operational progress, while Instant still requires `COMMITTED` and delivered/terminal customer cancellation remains rejected.
- `operations/application/advance-fulfillment.ts` now rechecks successful matching Payment evidence inside the acceptance transaction (canonical Payment when present, successful attempt for retained pre-canonical Orders), with a required audit-effect guard. No new Order state was added.
- Admin fulfillment labels now include Accept order & start picking; customer cancellation acceptance says refund processing.
- Added policy before/at/after cutoff cases, D1 cutoff with earlier preparation, current-payment rejection, and a paid Instant acceptance-versus-cancellation race through real commitment/commands.

**Executed focused Worker run: 84 passed, 1 failed in 4 files (85 total).** Command: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/orders/domain/cancellation-policy.test.ts src/orders/application/cancel-order.integration.test.ts src/orders/application/instant-commitment.integration.test.ts src/operations/application/fulfillment-command-recovery.integration.test.ts`. Log: `C:/Users/reggi/.codex/visualizations/2026/09/07/01a07c32-22c4-78a3-b481-1267eb6aca38/cancellation-acceptance-worker.log`. The existing `rejects cancellation from the terminal or late lifecycle state OUT_FOR_DELIVERY` case expects rejection for a Scheduled fixture whose cutoff is still in the future; the revised policy permits it. Reconcile the test and reachable operational timing against the approved cutoff rule, preserving Instant/post-delivery/terminal rejection rather than blindly deleting coverage. The run has finished; no Core test or browser stack remains running. No post-edit type/lint/Web/browser/build or full-phase gate has run for this slice.

Previously preserved unfinished Unit/SKU repair remains:
- `admin/application/catalog-command-recovery.ts`: optional capability/location authority extension, currently no new callers.
- `packages/validation/src/admin-products.ts`: Unit/SKU/price/availability body schemas, not yet wired.
- untracked `admin/application/catalog-effects.integration.test.ts`: eleven genuine failing regressions for required effects/audit/receipt, replay and mutable IAM. Prior log `catalog-effects-regression.log` records 11 failures; commands in `catalog-commands.ts` are still unrepaired. Do not discard these or mistake schemas for completed implementation.

The current PayMongo adapter and staff screen already submit refunds via `/v1/refunds`. Official documentation confirms API full/partial refunds: https://docs.paymongo.com/docs/payment-acceptance-refunds . Verify exact provider reason values and method/account restrictions while completing adapter acceptance: the current adapter uses `requested_by_customer`, while the current guide lists `duplicate`, `fraudulent`, `others`; this was observed, not yet resolved or tested against the real provider. An attempted further documentation/read call was interrupted before a result. No live refund, payment, email, booking, deployment or provider acceptance was performed.

### Remaining authorized work and environment

Complete canonical reconciliation and current correctness checks, then customer/staff/location/address/serviceability/pickup/cycle setup acceptance; catalog/categories/variants/promotion media/Global prices/local activation; warehouse dispatch/transit/accepted receipts/discrepancies; exact Scheduled purchase/receiving/allocation/packing/inspected surplus; preparation-stage Lalamove booking/normalized states/reconciliation/Scheduled-only emergency manual fallback; membership removal, complete customer/operator journeys, notifications/support/refunds and final acceptance. Inspect current code/tests before revisiting historical defects. Many setup, media and financial repairs are already committed; warehouse transfers and promotion media are absent, and downstream workflows remain incomplete.

Preserve existing databases and all local changes. Only identified disposable `apps/core/.wrangler/e2e-commerce-alignment-20260907` may be rebuilt for managed browser acceptance. Do not reset `.wrangler/state`, `.wrangler/e2e-state` or retained/shared/remote data. No retained database was migrated/reset. Do not overlap Core suites with managed browser stacks; keep source fixed during browser runs. Run focused checks during iteration and full gates at actual phase completion. Commit directly to main and push origin main after coherent verified slices. No subagents unless the owner explicitly authorizes delegation. `IMPLEMENTATION_STATUS.md` has pre-existing invalid UTF-8 elsewhere; append bytes only rather than rewriting the file.

## Historical checkpoint snapshot — superseded by the active handoff above

## Current recovery state — 2026-09-09

Current saved main is `587ca02` (Product commands), following Category `e51a2bd` and media `dc74065`. Owner has paused broad implementation to review the 12 proposed business workflows. Only removal of the overcomplicated image-recovery feature is currently proceeding. Transaction updates always send; promotions alone are optional.

Uncommitted owner-directed simplification: removed the product image-recovery panel, its Web route, Core operator query/commands, shared service/DTOs/validators and tests solely for those removed actions. Kept ordinary image upload/update/remove/publication and internal duplicate protection/background cleanup with their failure tests. Upload now has normal progress/errors and a single Upload image button; one automatic transport retry preserves the same request. No schema or retained-data change. Focused Worker 35/2, Web 396/98, contracts 68/19, types/lint/architecture/naming pass. First browser attempt exited during managed-stack startup before tests. Diagnostic rerun passed both 1440/390 desktop/mobile upload, lost-response automatic retry, storefront/cart display and removal journeys with zero retries (image-simplification-browser-debug.log); mobile screenshot inspected. Web managed build and Core dry-run build pass. No provider or complete-commerce-phase acceptance.

Paused catalog transaction slice is preserved separately: catalog-command-recovery.ts authority extension, admin-products.ts Unit/SKU body schemas and untracked catalog-effects.integration.test.ts. The 11 new regressions reproduce remaining Unit/SKU/activation/price defects (catalog-effects-regression.log), not completed repairs. Do not stage these with image simplification or resume broader changes until the owner finishes workflow review.

Audit trace: 48 commits since `619df3c` preserve schema/policy baseline `6daaf3d`, repair paid commitment/refund/provider recovery, add Global exact-location price authority, and implement guarded identity/customer/location/service-area/hours/readiness/cycle setup. `SCHEMA_REMEDIATION.md` and `STAFF_INVITATION_HANDOFF.md` describe historical bounded work; neither accepts the commerce plan. Current status entries distinguish aggregate, Worker, browser and provider evidence. No whole phase is accepted solely from these records.

- Phase 0: canonical target and profile/closure/capability/retained-upgrade decisions exist. Irreversible anonymization and official accounting remain dependent on factual owner policy.
- Phase 1: committed repairs have Worker/D1 and selected browser recovery evidence. Full two-mode customer/payment/provider acceptance remains open.
- Phase 2: setup through named Admin controls and Core commands exists, with connected site/address/scoped-staff evidence. Actual OAuth/email account acceptance remains separate. Latest saved aggregate passed Core 1341/181, Web 389, contracts 68, harness/migrations/types/lint and both builds (`readiness-check.log`).
- Phase 3: product-media publication/recovery/cart and category repairs are committed. Product create/edit/status repair is in verification. Promotion media is absent. SKU, unit and local activation commands still need complete transaction-time authority/precondition/effect/replay repairs. Global pricing is implemented but the entire acceptance matrix is unfinished.
- Phases 4–5: warehouse transfer commands are absent from the service manifest. Procurement/receiving repairs exist, but full allocation/shortage/packing/surplus journeys are unfinished.
- Phases 6–7: preparation/delivery alignment and final journeys are unfinished. Membership contracts and renewal/cancellation jobs remain registered. No real provider acceptance, deployment or release acceptance is established.

Committed media slice: forward migration 0084 retains associations and adds durable upload identity/digest and bounded cleanup. Core guards media publication/removal and operator observation/discard/cleanup retry; Web freezes uploads/recovery intents. Same-origin versioned anonymous media rechecks publication, catalog/cart use canonical R2 images, and bounded multipart input rejects invalid media. No promotion-image implementation yet.

Evidence correction: the prior checkpoint was stale. Initial multipart browser failures came from Playwright `route.fetch()` forwarding empty file bytes; constructor narrowing was not the root cause. Native browser fetch followed by simulated lost response passed two desktop/mobile journeys (`media-browser-native-upload.log`). Later expanded price/local activation/cart/cleanup journeys failed on an ambiguous region locator after reaching cleanup (`media-cart-browser.log`); they were not accepted. Exact locator correction passed both desktop/mobile cases with zero retries (`media-audit-browser.log`); mobile cart and cleanup screenshots were inspected. The cart still advertises the retired trial, confirming membership removal remains unfinished.

Current focused evidence: media/catalog/cart Worker run 37 tests/3 files (`media-cart-worker.log`); affected media/operator/cleanup run 58/4 (`media-affected-worker.log`); Web 394/97 (`media-cart-web-tests.log`). Earlier suppressed-effect run passed 13/13 after correcting a shared-fixture fallback assertion. Fresh/populated migration checks pass. Current aggregate `pnpm check` passed Core 1358/182 (342.55 seconds), Web 396/98, contracts 68/19, shared/harness/migrations/types/lint and both builds (`media-audit-check.log`). Vinext and catalog generation checks pass. Two recovery-panel regressions reproduced failed-loading/stale-read defects and pass after correction; 17 media Worker tests pass including minimal historical audit receipt recovery (`media-audit-final-worker.log`).

Category repair committed at e51a2bd: new category-commands.ts replaces older writes through the same Core methods. Six genuine Worker regressions reproduced missing required effects, authority revocation, lost-response recovery and mutable replay; an initial invalid-code/revocation fixture was corrected before recording those failures. Current 29-test category/catalog run passes (category-worker-current.log), Web 398/99 passes, types/lint/architecture/readiness/naming and Core dry-run build pass. Both desktop/mobile real managed category create/edit/status journeys pass with zero retries (category-browser.log), preserving exact bodies/keys after each lost response; mobile screenshot inspected. Category parent choices are paginated and read failures/stale responses are handled. No migration or retained-data mutation. The last aggregate predates this category repair; no whole phase accepted. Product verification is recorded below; next complete variant/unit/local-activation/price transaction and retry gaps together before promotion media. Keep current source fixed during managed browser runs. Do not overlap full Core suites with browser stacks.

Product create/edit/status repair verified for this slice: shared catalog command persistence now guards current Global IAM, claims, every required effect and frozen receipts. Core validates active canonical base units/categories at creation, replaces details atomically and guards lifecycle/version; Web uses bounded shared schemas, validated reads, paginated category choices and full frozen recovery through scope changes. Product initial regressions reproduced seven failures before implementation (product-regression.log). Current Product 12/12 tests pass (product-worker-final.log); the affected Category/catalog/media run passed 57 tests and failed one new test solely on the wrong audit column, corrected in the Product rerun. Web 398/99, types/lint/conventions/architecture/readiness/format and Core dry-run build pass. Four desktop/mobile Category and Product create/edit/status browser journeys pass with zero retries (product-category-browser.log); same URL/body/key is asserted after each lost response, including an edit-scope change. Mobile Product screenshot inspected. Managed setup built Web and reset only the identified disposable state. No schema/RPC signature change, no retained DB mutation, no provider acceptance. Last aggregate still predates Category/Product repair. No whole phase accepted.
Only identified disposable `apps/core/.wrangler/e2e-commerce-alignment-20260907` may be rebuilt. No retained database was migrated/reset and no live provider operation occurred. External provider/account, operational setup, accounting and retention inputs remain final acceptance items. Historical chronology follows and must not be read as the current next action.

## Earlier execution evidence

Address-map slice in progress after pushed `b5c1a6a`: Locations now includes Mapbox search/pin/device controls and submits text/coordinate provenance. Shared Geography finalization is reused by Customer addresses and location commands. Core permanently finalizes temporary text at the chosen pin, preserves saved evidence, re-finalizes moved provider-derived addresses, and still guards current IAM/version after lookup. Twelve location Worker tests, 23 existing customer-address Worker tests, three Web tests, workspace types/lint pass. A real local managed browser rerun is in progress at `location-map-browser.log`; this does not establish actual geocoder account acceptance. No new schema migration or retained-environment mutation. Serviceability authoring/readiness/cycles and later phases remain open.

Location milestone verification: actual managed-stack Playwright `location-setup.spec.ts` passed both 1440px/390px flows through Web/Core/D1 creation, activation, reload persistence and deactivation. Screenshot inspected. `pnpm check` passed 874 Core tests, 364 Web tests, 68 contract tests, shared/harness checks, migrations and both builds (`location-check.log` in the task artifact directory). `check:vinext` and `catalog:check` pass. Final review added unstarted-quote invalidation, protection against moving an origin during started payment/delivery work, and inactive-site pickup-profile reads; focused rechecks are in progress. These additions do not constitute Phase 2 acceptance. Further address-map finalization, serviceability and cycle/window setup are still required.

In-progress location slice after pushed `dd4860c`: migration 0071, Global location list/create/update/activate/deactivate commands, current IAM/market/version guards, audit/replay, shared DTO validation and `/admin/locations` Web workspace are implemented but not accepted. Purpose distinguishes inventory-only warehouse from customer fulfillment in assignment, quote/revalidation and readiness paths. Existing operational access remains available after deactivation. Eight new Worker tests and two Web tests pass; migration verifiers and workspace types pass. Initial fixture login, retained capability vocabulary and SQLite row-prototype test issues were corrected. Browser test is running against newly created disposable `apps/core/.wrangler/e2e-commerce-alignment-20260907`, explicitly absent before this run; existing state/e2e-state were not reset. Log: `C:/Users/reggi/.codex/visualizations/2026/09/07/01a07c32-22c4-78a3-b481-1267eb6aca38/location-browser.log`. Address map/provider finalization, serviceability authoring/readiness details and cycle setup remain unfinished. No provider acceptance.

Resumed 2026-09-07 after `dc18720`, clean working tree. Owner reaffirmed continuing all remaining phases and deferring the reminder about factual policy/external acceptance items until the final handoff. No external blocker is established.

Current verified slice: selling configuration now batches audit with configuration, quote invalidation and idempotency success. Core rechecks current staff/scope/capability, reopening readiness and mode-switch unresolved work at the write boundary. Reasons participate in replay identity. Focused `global-commerce-configuration.integration.test.ts` and `admin-operations.integration.test.ts` passed 21 Worker tests, including injected scope/role/staff revocation, late batch failure and readiness change, plus reachable RPC replay and changed-reason rejection. Core types, lint, architecture and readiness checks pass. No new browser/provider acceptance or retained database migration. All phase acceptance remains open; continue location setup next.

Status: in progress, 2026-09-07. Active plan: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, all remaining phases authorized in dependency order.

Workspace: main at observed baseline `619df3c`, following `6daaf3d`. Initial working tree was clean. Existing local/shared databases remain preserved; no migration/reset/deployment performed.

Established: canonical documents already incorporate the agreed target, with explicit historical sections. The Phase 0 design record defines profile fields, closure intake, capabilities and retained-baseline upgrades. Irreversible anonymization and official accounting require factual owner policy; independent implementation can continue. Schema and staff checkpoints are historical evidence, not phase acceptance.

Current slice: Phase 1 Scheduled pay-as-you-go correctness. Removed membership checks from quote creation, pre-payment revalidation, compatibility eligibility and paid commitment. Existing promotion limits, atomic commitment/holds, delivery recovery and staff onboarding are retained. The current Web checkout has no separate membership gate.

Validation: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/checkout/application/instant-quote.integration.test.ts src/orders/application/apply-checkout-payment-reaction.integration.test.ts` passed 30 tests. After strengthening payment reachability, the commitment file passed all 16 tests; it reaches real payment initiation/replay and test-provider reconciliation before commitment, with zero physical stock and no subscription. Core typecheck and repository lint passed. `pnpm --filter @freshmarkets/web exec vitest run test/app/checkout/checkout-client.test.tsx test/app/api/checkout/quote/route.test.ts test/app/api/checkout/payment/route.test.ts` passed 15 tests. New test fixture field-path mistakes were corrected without weakening assertions. Aggregate `pnpm check` is still running; the first attempt stopped on formatting, which was corrected.

Acceptance remains open for all phases. Subsequent work: complete Phase 1 evidence/recovery, setup/access, catalog/media/pricing, warehouse, Scheduled allocations, delivery/manual fallback and full retirement/journeys. No browser or provider acceptance has been executed in this task.

Aggregate result: `pnpm check` passed on this application slice: 857 Core tests, 360 Web tests, 68 contract tests, shared/harness suites, conventions, migrations, architecture/readiness, lint/types and both builds. Log: `C:/Users/reggi/.codex/visualizations/2026/09/07/01a07c32-22c4-78a3-b481-1267eb6aca38/commerce-check.log`. Existing Wrangler environment-selection and Web chunk-size notices remain. No browser/provider acceptance or retained-environment upgrade was run.

The pay-as-you-go slice is committed and pushed on main as `e95e0e5`.

Verified slice: Phase 2/3 price authority. Migration 0070 adds explicit prices.read/manage definitions without automatic grants. Core requires Global price authority, offers typed exact-location current/history reads, and guards current scope/capability plus active target in the complete price batch. Global Product detail includes the price editor/history; local Product detail retains selling controls without price inputs. Lost Web responses retain the exact payload and idempotency key. The 0069 verifier now tests hardening preservation before applying/validating additive migrations; existing permission/grant/price evidence is checked across 0070.

Focused pricing validation: 20 catalog Worker/D1 tests passed, including direct RPC scope/capability rejection, price replay/concurrency and injected authority/location changes before the write with no partial prices/audit/success results. Two new Web component tests and ten existing catalog-adapter tests passed. Workspace types, architecture/readiness, migrations and 25 harness tests passed. Initial race fixtures leaked location deactivation into later tests; teardown now restores only that test-owned change. UI test setup/type issues were corrected. Final aggregate, builds, browser verification and commit are pending.

Additional price validation: executed `global-pricing.spec.ts` against a temporary vinext development server with controlled RPC responses at 1440px and 390px; both pass. The test submits exact location/amount/version, refreshes history and rejects horizontal overflow. Screenshots were inspected and panel spacing corrected, then both browser cases reran successfully. This is browser interaction evidence, not a live Web/Core/D1 journey. The final focused 20-test Core run also proves another location's missing price remains null despite a price at Cebu. `check:vinext`, `catalog:check`, final formatting/lint/naming/diff checks pass. Aggregate price check remains in progress at `C:/Users/reggi/.codex/visualizations/2026/09/07/01a07c32-22c4-78a3-b481-1267eb6aca38/pricing-check.log`.

Final pricing aggregate: `pnpm check` passed with 861 Core tests, 362 Web tests, 68 contract tests, shared/harness suites, migrations, types/lint and both builds. After final source cleanup, 20 focused Core tests, 12 Web component/adapter tests and workspace types/lint passed. The final two browser cases also verify local selling controls remain visible while price editors are absent. Screenshots were inspected; vinext/catalog/format/naming/diff checks passed. The temporary port-3200 server was stopped. No provider sandbox operation or retained database migration/reset was performed.

Next: continue location/customer/staff/cycle setup before later operations. Location schema primitives and courier-profile commands exist, but Global site creation/serviceability and cycle/window authoring are not complete journeys. All remaining phases remain authorized and incomplete. No phase is accepted by this checkpoint. Full media, warehouse transfers, Scheduled purchase/allocation/surplus, delivery/manual fallback, membership retirement, support/refunds/notification and provider acceptance remain unfinished; external provider/account and irreversible accounting/privacy policy inputs do not replace the remaining independent implementation work.

Final location rechecks: 16 focused Worker tests (location commands and delivery-provider operations), workspace types/lint, Core dry-run build and 26 harness tests pass. Formatting/naming/diff checks pass. The managed browser stack stopped after both successful tests; its newly created E2E directory is disposable. Existing local/shared databases remain untouched. Continue with map/permanent address finalization and versioned serviceability authoring.


2026-09-08 continuation (uncommitted): location map search/pin/device confirmation now shares permanent address finalization with customer addresses; provider-derived temporary text is finalized at the actual confirmed pin and saved evidence remains protected. Service-area/zone publication, Global editor, typed read/preview commands, pagination and migration 0072 geography revision are implemented. Options/Instant/Scheduled quotes and payment revalidation now check actual polygons and current eligible locations. Quote creation and payment-intent persistence fence geography changes atomically. Public serviceability retains candidate-zone association and resolves overlapping eligible areas by closest location.

Executed evidence: 33 focused Core tests passed for location/serviceability/RPC/commitment paths; subsequent 32 quote/payment-reaction tests passed including geography changes during provider quotation/revalidation with zero quote/hold/payment effects. Five Web workspace tests passed, including exact unknown-response body/key replay. Initial real managed Web/Core/D1 service-area publication/preview passed at 1440 and 390 pixels; mobile screenshot review prompted form/header spacing cleanup. Pagination and final layout updates require final focused/browser recheck. Migration verification, workspace types/lint and architecture/readiness/naming pass. Full `pnpm check` is running in `service-area-check.log`. No phase accepted. Existing retained DBs untouched; managed tests use only `e2e-commerce-alignment-20260907`.

Final service-area validation: aggregate `pnpm check` passed (890 Core tests / 161 files; 367 Web tests / 92 files; 68 contract tests; shared/harness checks, migrations, types/lint and builds). Final routing-guard changes then passed 41 focused Core tests, including provider-time revision/expiry/readiness/cycle rejection, no partial effects and payment replay after pause/geography change; public overlap/zone tests passed in the 45-test focused run. Expanded race fixtures initially expired shared test-file links; cleanup now restores the captured rows and the complete focused runs pass. Five Web workspace tests passed, plus the final two service-area tests after inactive-assignment/pagination review. Final migration verification explicitly checks revision-1 initialization while preserving retained site/grant/price rows.

Executed final managed browser run: `location-setup.spec.ts service-area-setup.spec.ts` passed all four cases at 1440/390 pixels through real local Web/Core/D1. Mobile screenshot inspected after header/form spacing correction; no horizontal overflow. This is local application evidence, not live Mapbox/Lalamove/PayMongo acceptance. Final named-assignment handling keeps inactive/off-page sites visible for removal; its five-test Core command/read suite and final builds passed. No retained environment was migrated/reset. Next independent work: finish mutable IAM/audit/idempotency guards in reachable operational commands, then remaining setup/readiness/cycle and downstream warehouse/Scheduled/delivery workflows. No entire commerce phase is accepted.

Saved/pushed geography/address slice: main `5abe8e8`. Follow-on Phase 1 fulfillment boundary repair is implemented: both reachable RPC paths pass trusted actor identity; current capability and Global/market/location scope, idempotency claim, Order lock/stock consumption, Fulfillment transition, audit and frozen result commit together. Admin no longer appends audit or reloads a later mutable state after success. Retained order-ID results reconstruct the original action/version, and interrupted legacy pre-transaction claims can be safely claimed with the same aggregate guards. Current tests: 20 focused Core fulfillment/Instant tests (including eight new reachable recovery cases), earlier 19 Admin/recovery tests, four Web operation-adapter tests, workspace types/lint and Core dry-run build pass. No new schema/public DTO required. No browser/provider acceptance or whole commerce phase claimed. Next inspect procurement/receiving current authorization, audit and recovery paths.

Final fulfillment recheck: 27 Worker tests passed across recovery, Instant commitment/packing and delivery-provider operations after the final location guard. Public contracts remain unchanged; no additional browser/provider acceptance was performed for this Core-only repair.

2026-09-08 receiving continuation after pushed `7897bfe` (not yet committed/accepted): new receiving commands replace the obsolete physical-stock repository. Trusted actor/requested location, current procurement capability/scope, requirement/receipt versions, receipt event, cycle allocation, audit and frozen result share one guarded D1 batch. Matching unapplied legacy claims recover; historical successful keys lacking an original snapshot return an explicit already-applied conflict rather than fabricating replay. Migration 0073 preserves old physical balances/ledger/events, marks immutable legacy accepted quantities and creates no guessed cycle goods. The populated-upgrade verifier passes, including nonzero accepted evidence and unchanged retained stock.

Scheduled MARK_PACKED consumes cycle/location/pool allocation across the complete paid demand in the same Order/Fulfillment transaction. Missing allocation, cross-cycle context or a competing pack cannot partly consume goods. The receiving workbench now uses named receipts/current versions/Core actions, validates response bodies and freezes unknown requests for recovery. Historical quantities remain visible as requiring evidence review. Focused 47 Worker tests passed for receipt/recovery, packing, existing Instant commitment and provider projections; another 18 Admin/recovery tests and six Web receiving/adapter tests pass. Workspace types/lint and migration checks pass. Full Core suite and managed receiving browser tests are running; no final browser result or phase acceptance yet. Purchase confirmation, inspected surplus, historical allocation reconciliation and the rest of the authorized plan remain independent unfinished work. Existing retained/local application databases remain untouched.

Final receiving slice evidence: full Core suite passed 910 tests in 164 files. Final policy/replay checks then passed 34 focused Worker tests; the populated retained-upgrade and current-seed schema suites passed nine tests after freezing the 0068 fixture and marking current seed legacy receipts. Both managed Web/Core/D1 browser cases passed again at 1440/390 after mobile layout repair; the mobile screenshot was inspected. Six Web receiving/route tests, 68 contract tests, workspace types/lint, migration verification, conventions/architecture/readiness and Core dry-run build pass. Logs: receiving-core-tests.log, receiving-final-browser.log, receiving-core-build.log in the task artifact directory. Web production build ran as part of managed browser setup. No provider or retained-environment migration acceptance is implied. Continue procurement transaction/purchase reachability and remaining setup dependencies; all whole-phase acceptance stays open.

Procurement aggregation repair after pushed receiving slice `1156ae7`: removed pre-transaction run/idempotency writes and post-commit Admin audit/readback. Current capability/scope, cycle/destination market/cutoff, exact demand totals, run/requirement and empty unstarted receipt are guarded with the complete batch and original result. New keys cannot bypass expected versions/reasons on replay; retained incomplete claims recover while successful legacy keys lacking snapshots fail closed without duplicate purchase demand. No new schema/public contract. Nine new Worker recovery/boundary/race cases plus three existing Admin command cases passed; earlier 15 Admin integration cases passed. Workspace types, conventions/architecture/readiness and Core dry-run build pass. An unused obsolete audit helper was removed after lint identified it. Purchase approval/order/supplier commands and the named procurement UI remain unfinished; continue correctness guards on existing stock adjustment before later setup/operations acceptance.

Final procurement recheck: 13 focused Worker tests pass, including the legacy RPC retaining Scheduled operations after selling is paused and global mode switches to Instant. Final workspace types and lint pass. No provider/schema/browser acceptance was added for this Core-only repair.

Inventory correction after pushed procurement slice `1ca61e9`: replaced pre-transaction claims and mutable replay with current IAM/scope, claim/recovery, stock/hold/reservation guards, ledger, audit and frozen result in one D1 batch. Historical successful keys without original snapshots report already applied; matching unapplied claims recover. Inventory reads include catalog pools without balances at version zero and publish held/available stock. Web freezes unknown requests and presents mobile product/stock controls together. No schema change.

Validation: 21 final focused Worker tests passed for Admin initial stock, Instant holds/commitment and adjustment recovery; earlier 25 adjustment/Instant/recovery and 26 existing operational tests passed. Four Web recovery/adapter tests, 68 contract tests, workspace types/lint, format/naming/architecture/readiness and Core dry-run build passed. Final managed browser run passed both desktop/mobile cases, including a committed-but-lost response, exact retry, removal and ledger reload, after mobile layout correction. Catalog setup is an explicit test fixture, not catalog-authoring acceptance. Screenshots inspected; browser used only disposable e2e-commerce-alignment-20260907 state. Logs: inventory-final-browser.log and inventory-core-build.log. All whole commerce phases remain open.

Next Phase 1 finding: cancellation claims before validation, returns mutable replay, and has incomplete refund recovery. Refund projection also trusted supplied state and could not recover an unlinked refund; that bounded repair is in progress separately from inventory. Continue through cancellation acceptance and remaining setup dependencies.

After inventory commit `d99fb9e`, bounded refund projection repair: canonical Refund identity/amount/currency/status now controls member updates; member/cancellation/Order completion shares a batch with a final Order-effect guard. Unlinked refunds recover by the stable cancellation key; competing original/addition observations complete once. Late submission responses reconcile the saved Refund and cannot downgrade successful members or completed cancellation. Refund ingress marks an already-observed event applied only after projection; payment reconciliation attempts canonical projection before provider/unchanged-state early exits.

Validation: 45 focused Worker tests pass across cancellation, Refund commands and verified provider ingress. The added ingress test creates/payment-confirms through the payment commands, explicitly fixtures paid Order linkage, then uses real cancellation/refund commands and signed test-provider events. It proves repeated projection failure leaves the inbox RECEIVED with a bounded lease and later replay completes with one Refund. Other tests cover unlinked identity, unrelated Refund rejection, stale status, late-response downgrade, failed Order mutation rollback and competing members. Types/lint, format/naming/architecture/readiness and Core dry-run build pass; an initial fixture expected PROCESSING instead of the implemented leased RECEIVED inbox vocabulary and was corrected against claimInbox. No new schema/public DTO/browser/live-provider acceptance.

Remaining Phase 1: requestOrderCancellation still claims before validation, lacks complete current admission guards and original replay, and refund submissions need durable reachable recovery; Admin refund/cancellation boundaries also remain to inspect. Do not treat the projection slice as complete cancellation acceptance. Continue these repairs before setup and later authorized phases.

IN-PROGRESS after pushed projection commit `9b13183`: cancellation admission now has transaction-local idempotency claim/reclaim, trusted customer/staff guards, exact paid-set/refund exclusion and Scheduled cutoff guards, audit, frozen accepted snapshot, and legacy-success conflict handling. Customer/Admin adapters pass actual identity; Admin write result changed to AdminOrderCancellationResult (current detail is a separate query). No final acceptance/commit yet. New resume-cancellation-refunds module and registered orders.cancellation-refunds job bound submission retries (five, minute separation), recover persisted intent and avoid submitting when a Refund identity exists. requestRefund no longer reissues an existing REQUESTED identity. These changes need final failure/race/replay/Web checks and documentation/consumer review.

Current checks: 57 existing focused Worker tests passed before new guard/recovery cases. Expanded cancellation file had 27 passing / 1 fixture assertion failure: the registered job correctly also considered an earlier accepted test cancellation, so its aggregate affected count was two; target-member attempts/status is now asserted instead of assuming the entire fixture database contains one pending operation. Rerun required. Types/lint previously pass. Current Web cancellation confirmation still needs frozen request/validated response recovery; complete remaining admission/recovery tests, Admin replay DTO tests, browser flows and builds before commit. Existing paid-set helper still requires all existing refunds to go to review; full remaining-refund policy and unknown provider Refund lookup/reconciliation remain incomplete. No retained database or deployment touched.

Cancellation admission/recovery slice ready for final commit: current customer ownership/activity or Global staff capability, request claim/reclaim, Order version/state, exact original/addition payment set, refund exclusion, Scheduled cutoff, operational release, audit and original acceptance share a guarded batch. The accepted result stays REQUESTED even when immediate provider processing progresses; current progress is queried separately. AdminOrderCancellationResult replaces mutable AdminOrderDetail as the write result. Retained successful keys lacking snapshots report already applied; unapplied claims recover. No schema migration.

Orders owns bounded recovery selection/submission (ten due cancellations/run, five attempts/member, minute delay); the registered scheduler delegates through the real refund port. Existing REQUESTED Refund identities are not resubmitted; unresolved/exhausted work remains EXCEPTION. Both canonical and Admin refund budget writes exclude active cancellation members; Admin budget accounting now includes ESCALATED amounts. Generic Admin refund admission/execution is still unfinished and must be repaired next.

Validation: full Core suite passed 946 tests/166 files in 405.12 seconds. Later reservation/read-model refinements passed 70 focused tests; final cancellation test recheck is running after a legacy-recovery fixture was added. Twelve Web tests and 68 contract tests passed. Managed browser passed three cases: two real local Admin cancellation/lost-response/replay/reload journeys at 1440/390 and one customer rendering flow with controlled RPC responses. Paid Order linkage in Admin browser is explicitly fixture evidence; no provider confirmation inferred. First screenshot caught loading; stronger loaded-state assertions exposed mobile overflow, which was fixed and rerun successfully. Final mobile screenshot inspected. Source types/lint/format/conventions/architecture/readiness and Core dry-run build have passed, with a final recheck running. Logs: cancellation-core-tests.log, cancellation-mobile-browser.log, cancellation-final-core-build.log. Only the named disposable browser state was reset.

Next: inspect/rewrite requestAdminRefund and reconciliation resolution. Existing Admin refund code still inserts REQUESTED without provider execution, preclaims idempotency, lacks transaction-time current IAM and returns mutable replay. Unknown provider Refund lookup/reconciliation and cancellation of the remaining set after prior refunds remain acceptance gaps. Customer cancellation currently routes any preexisting Refund to review. No whole commerce phase is accepted. Continue remaining canonical/setup/cycle/catalog/media/warehouse/procurement/delivery/membership-removal/journey work in dependency order.

Final cancellation recheck: 33 cancellation Worker tests pass after legacy replay, exact cutoff boundaries and unpaid-customer no-effect coverage; 70 affected cancellation/Admin/refund/scheduling tests passed before the final legacy/unpaid additions. Final current financial reservation guards, immutable receipt and current IAM tests pass. Mobile browser passed all three cases after loaded-state/overflow correction; the visible Order screenshot is inspected. Final audit-presence guard and Admin audit key are being rechecked in the two affected suites before commit. Web/core workspace typechecks and lint pass. The schema remains unchanged. The full 946-test run predates the final bounded reservation/receipt refinements; those have focused follow-up evidence, not another full-suite claim.

Final pre-commit result: 51 cancellation/Admin Worker tests passed with the audit-presence guard; Core dry-run build passed. The verified cancellation slice is ready to save on main. Continue directly with Admin refund execution/reconciliation; no phase or release acceptance is implied.

IN PROGRESS after pushed cancellation commit `cd4afaf`: begun Admin refund repair. Extracted existing external submission/CAS/reconciliation into submitClaimedRefund in request-refund.ts, and shared authoritative refundBudgetStatement in the payment repository. Added Payments-owned request-staff-refund.ts with Global current IAM, expected payment version/provider evidence, active-cancellation exclusion, atomic claim/budget/audit/frozen acceptance and first-identity-only provider submission. Not yet connected to Admin/public contract/Web and not tested; do not claim implemented acceptance. Next wire required expectedVersion through AdminRefundRequest, refundRequestSchema/index (including provider registry), finance command, Web route/form and tests. Existing Admin fixture lacks provider attempt linkage/reference and must model a real captured payment seam. Existing generic Refund unknown-outcome lookup remains a separate pending capability.


2026-09-08 Admin refund continuation (uncommitted; browser recheck running): replaced the Admin REQUESTED-only insert with a Payments-owned command that atomically checks current Global refunds.manage, captured payment/version/provider evidence, cancellation exclusion, budget, audit and immutable receipt before first provider submission. Shared AdminRefundRequest now requires expectedVersion; AdminPaymentDetail supplies refundUnavailableReason. Web validates exact minor-unit input and acceptance responses, freezes unknown requests, and exposes explicit saved-request recovery. Payment reads include ESCALATED reservations and refund audit evidence. Only wrangler.e2e.jsonc selects the existing mock provider in ENVIRONMENT=test; normal/deployed environments remain unchanged.

Current evidence: 76 affected Worker tests pass (refund, Admin finance, cancellation); 68 contracts, nine Web tests, workspace types/lint/format/static checks and Core dry-run build pass. Harness 26 tests pass. Added PayMongo timeout/server/malformed-success handling after discovering the adapter falsely returned a rejection for unknown create outcomes; 33 adapter/refund tests pass. First browser run failed on a wrong test dialog label and cold shell loading; invalid-amount feedback was behind the dialog, now closes it for correction. A clean desktop/mobile browser rerun is in progress; no browser success yet.

Remaining immediately after this slice: provider Refund lookup/reconciliation (currently no lookup port), guarded reconciliation-case resolution, remaining-balance cancellation after prior refunds, then other Phase 1 and setup dependencies. PayMongo official lookup sources consulted: https://docs.paymongo.com/reference/retrieve-a-refund and https://docs.paymongo.com/reference/list-all-refunds; list is payment-scoped and cursor-paginated. Reference pages omit /v1 while the current guide uses /v1/refunds, so verify the published OpenAPI/account contract before assuming endpoint details. No sandbox acceptance or full commerce phase acceptance is claimed. No retained database migrated/reset.


Final Admin refund slice: 76 affected Worker tests, 33 final PayMongo/refund tests (including uncertain submission), 68 contract tests, types/lint/format/static checks, 26 harness tests and Core dry-run build pass. Final managed browser run passed both 1440/390 journeys in 3.4 minutes with stable source: exact decimal rejection, actual Core test-provider submission, committed-but-lost response, identical saved body/key replay, one PROCESSING Refund and audit after reload. Mobile screenshot inspected with no horizontal overflow. Earlier rerun stalled on reload while source was changing; final run waits for canonical progress before reload and passed without retry. Logs: refund-stable-browser.log, refund-core-build.log, refund-harness.log. Only disposable e2e-commerce-alignment-20260907 state reset. Live PayMongo acceptance and all whole commerce phases remain open.

Next confirmed Phase 1 defects: reconcilePayment does not validate lookup reference/amount/currency; duplicate successful Refund ingress skips payment-total projection recovery; Refund event transitions can overwrite terminal success and mapping does not constrain provider identity. Repair these owning projections/observations, then provider Refund lookup and guarded reconciliation resolution. Preserve the Admin frozen acceptance and original deterministic provider identity.


IN PROGRESS after pushed Admin refund slice 22cb679: payment observations now carry verified provider/reference/amount/currency into the guarded batch, including immutable subject/purpose, exact observed attempt, required reaction, continuation and settlement evidence. Same-state capture repairs a missing historical reaction without bumping Payment version. Mock lookup previously returned amount zero for every Payment; it now reports the amount/currency actually created in that test provider instance. Signed Refund observations resolve one provider-owned identity, reject ambiguous references, preserve SUCCEEDED against delayed/conflicting states and atomically project successful totals with settlement. Duplicate success repairs old incomplete totals before inbox application. No schema or public RPC change.

Current evidence: 75 affected refund/reconciliation/cancellation Worker tests passed, then final two-suite recheck passed 43 after ignored reaction and current-mapping guards. Types/lint/format/architecture/readiness and Core dry-run build pass. Full Core suite is running in payment-observation-core-tests.log; no final result yet. Canonical state rules updated; corrected residual active Membership wording in the Payment commitment boundary and Admin navigation contract. This documentation correction does not remove the still-present active Membership UI/jobs. Continue full-suite failures if any, final provider mapping/race evidence, then commit/push and proceed with unknown Refund lookup, guarded reconciliation resolution and remaining-balance cancellation. All whole commerce phases remain open.


Final payment observation repair: full Core suite passed 980 tests in 166 files (261.02 seconds). A final retained-reaction refinement then passed 62 focused Worker tests across payment creation, reconciliation, refunds and registered scheduling: same-intent/type historical reactions keep their original identity, and ambiguous duplicates fail closed. Earlier new coverage includes lookup amount/currency/reference mismatch; lost version/subject/provider mapping; ignored reaction/settlement/payment projection; delayed pending/conflicting failure; cross-provider and duplicate Refund references; and retained incomplete projection recovery. Types/lint/format/naming/architecture/readiness and final Core dry-run build pass. No schema/public RPC or new browser/provider acceptance; the preceding Admin refund browser acceptance remains separately recorded. Logs: payment-observation-core-tests.log and payment-observation-core-build.log. Current full-suite count predates only the final bounded retained-key refinement, covered by the 62-test recheck.

Next independent Phase 1 work remains unknown Refund provider lookup and guarded reconciliation-case resolution, then cancellation of remaining paid balances after prior Refunds. Provider lookup must not infer failure from absence or resubmit an unresolved identity blindly. ReconcilePayment currently opens repeated cases/throws for an unavailable registry/provider; inspect bounded recovery/error ownership while adding lookup. Preserve all current atomic acceptance, immutable receipts, historical effects, stock/cycle separation and the retained database policy.


IN PROGRESS after pushed observation commit ea5f87a: added optional typed read-only lookupRefund port, PayMongo known-reference retrieval and bounded payment-scoped list lookup using exact freshmarkets_refund_key metadata, plus test-provider observation storage/control. Adapter checks payment origin, stable key, refund status and exact positive integer amount; absence, ambiguity, mismatch, unavailable and truncated search remain UNRESOLVED, never authorize POST. Nineteen PayMongo adapter tests pass including pagination/bounds and no POST. Workspace types pass. These edits are uncommitted and not yet connected to Core recovery, scheduling or Admin UI.

Official sources: https://docs.paymongo.com/reference/retrieve-a-refund ; https://docs.paymongo.com/reference/list-all-refunds ; https://docs.paymongo.com/reference/refund-resource ; https://docs.paymongo.com/docs/payment-acceptance-refunds ; https://docs.paymongo.com/docs/developer-tools-go-live-checklist . List query names follow the reference (data.attributes.payment_id/after/limit). The implementation uses the existing /v1 API namespace corroborated by the current refund guide; the reference display omits /v1 for GET Refunds. Real account endpoint/query/pagination/capability acceptance remains an explicit external gate, not proved by fake-fetch fixtures. Direct documentation HTML fetch returned 403; the web tool supplied the official page content. No provider account or live API was called.

Next design: Payments-owned reconcile-refund command leases due Refunds using existing attempt_count/processing_started_at/next_retry_at fields, performs read-only lookup, validates returned payment/refund/key/amount/currency, and atomically binds an unknown reference with financial projection and immutable audit. Factor current guarded refund status statements for application transaction composition instead of adding an unguarded metadata update. Max five lookup attempts with backoff, limited batch, per-item unavailable handling and a linked open REFUND_UNRESOLVED case; absence never releases budget/resubmits. Preserve a due lease until Order cancellation projection is complete so crash after financial success can recover without another provider write. Add a thin registered scheduler and a Global guarded, versioned, idempotent Admin recheck action with original receipt and explicit Web recovery/current progress. The existing test provider is instance-scoped and not proof of persistent live-provider behavior. Continue remaining financial/Phase 1/setup work after this slice.


IN PROGRESS refund lookup/recheck slice after ea5f87a: read-only provider port/adapters now connect to Payments reconcile-refunds and registered every-minute payments.refund-reconciliation job. Five due rows/run, five-minute leases, five bounded lookup attempts, exact evidence validation, guarded reference binding/financial projection/audit, and separately bounded terminal projection recovery. No recovery POST. Current Global versioned recheck command, typed RPC/read progress and Web frozen-response recovery are wired. Existing schema fields suffice. Arbitrary adapter error messages were removed from uncertain-submission case evidence.

Current evidence: 112 affected Worker tests in eight files pass, including 13 new recovery/recheck cases, 19 adapter cases, existing Admin finance, financial reconciliation and scheduling. Unknown-response fixture first executes the test provider acceptance, then loses its response; registered job recovers the same identity from explicit provider evidence. Tests cover wrong key/payment/amount/currency, ignored required audit, concurrent leases, bounded absence/projection failures, current scope/version/lease rejection and original replay. Types/lint/format/static conventions pass; 68 contract and two existing Web parsing tests pass. Managed desktop/mobile browser run is in progress; no new browser/provider acceptance yet. Hold source stable until it finishes. No retained DB changes; only named disposable browser state is used.

Still pending in this slice: final browser result/visual inspection, direct boundary review and any regression, final builds, diff review and commit. Then repair existing reconciliation-case resolution and payment lookup error ownership/duplicate cases, remaining-balance cancellation and remaining Phase 1/setup/commerce dependencies. All whole phases remain open. PayMongo account endpoint/pagination/capability acceptance remains an external gate as recorded above.


Final refund lookup/recheck evidence: 112 affected Worker tests passed in eight files, 68 contract tests and two existing Web response tests passed; workspace types/lint/format/naming/architecture/readiness and Core dry-run build pass. Managed browser passed both desktop1440/mobile390 journeys in 2.0 minutes with no retry: real local Web/Core/D1 submission, lost response, identical replay, versioned recheck with another lost response/identical replay, current history and single audit on reload. Mobile screenshot inspected without overflow. Browser proves queued lookup acceptance, not a live or persistent mock provider lookup. Logs: refund-lookup-worker.log, refund-lookup-browser.log, refund-lookup-core-build.log. No schema or retained database change; no whole-phase or live-provider acceptance. Ready to commit, then continue unsafe reconciliation-case resolution and the remaining financial/commerce plan.


IN PROGRESS after pushed refund recovery commit 438b2b9: replacing unsafe Admin reconciliation resolution with Payments-owned current Global authority/case version/financial recovery/audit/immutable receipt guards. 0074 adds case version while preserving old evidence; all case reopening/completion writers advance versions. Shared request requires expectedVersion; review DTO includes current resolutionUnavailableReason. Web now validates queue/acceptance DTOs, freezes unknown decisions and offers explicit saved-resolution retry; mobile review cards link to payment evidence. Unlinked or incomplete financial work cannot be closed by entering a reason alone. Payment lookup exceptions now remain reconciliation outcomes and repeated identical open cases deduplicate.

Current checks: types/lint/static checks, 68 contracts, eight Web finance-route tests and clean/populated-upgrade migration verification pass. Added retained case identity/payload/timestamp/version proof in schema verifier. Earlier three-suite Worker run passed 45 tests; six new resolution tests had one fixture failure using nonexistent staff status inactive, corrected to suspended (also corrected that preexisting refund authority fixture so it now tests actual guard rejection). Full Core and managed browser runs are in progress. Full suite has found one real caller regression: redrivePaymentReactions ignores reconcilePayment's RECONCILIATION_REQUIRED result and no longer records PROVIDER_LOOKUP_FAILED when the provider throws; fix the caller rather than weaken the test after browser source freeze ends. No final acceptance/commit yet.

Next financial finding: buildCancellationRefundSet excludes fully refunded members and does not subtract prior successful Refunds; cancelOrder rejects any prior Refund. Zero-refund cancellation finalizes in a separate unguarded batch and has no due-job recovery if that projection is interrupted. Complete remaining-balance paid-set guards/customer preview and atomically finish new zero-refund cancellation; preserve retained historical fee evidence and flag any prior-refund component ambiguity rather than inventing allocations. All other Phase 1/setup/catalog/warehouse/Scheduled/delivery/membership-removal journeys remain open.


Final reconciliation resolution slice: full Core passed 1,012 tests in 168 files (268.77 seconds), full Web passed 374 tests in 96 files, 68 contracts and 26 harness tests pass. Final unresolved-recovery audit refinement then passed 28 focused Worker tests across Refund recovery, resolution and payment redrive. Types/lint/format/static checks, migration clean/populated upgrade verification and final Core dry-run build pass. Both real local Web/Core/D1 browser cases passed at 1440/390 in 2.4 minutes with lost-resolution response, identical retry, current queue after reload and unlinked-evidence rejection. Mobile screenshot inspected. Logs: reconciliation-final-core-tests.log, reconciliation-final-web-tests.log, reconciliation-resolution-browser.log, reconciliation-final-core-build.log, reconciliation-harness.log.

Fixed verification findings: reaction redrive now handles a returned reconciliation-required result as failed lookup and preserves its bounded retry diagnostic; its nominal lookup fixture now includes an actual provider attempt/lookup, rather than counting an unlinked case as a lookup. Staff rejection fixtures use the real suspended vocabulary. The stale customer cancellation wording assertion now verifies pending acceptance never claims confirmed refund while terminal canonical completion does. Initial full runs failed on the identified caller/stale assertion; final runs above pass. Whole commerce phases remain open; 0074 is authored/tested only, with no retained deployment migration/reset. Continue remaining-balance cancellation and zero-refund atomic/recovery handling, then remaining Phase 1/setup dependencies.


2026-09-08 remaining-balance cancellation verified: canonical prior successful Refunds now reduce each original/addition member, while fully refunded payments remain in the complete current version/status/identity/balance guard. Pending/uncertain/unfinished refund recovery blocks admission. Customer preview matches remaining value. Historical fee plus prior-refund component ambiguity remains explicit financial review. Zero-balance admission completes Order/cancellation/release/required audit/frozen receipt atomically, without another Refund. Registered cancellation recovery can complete retained accepted zero-refund intents in legal Order states; the existing domain notification reconciliation projects persisted completion after interruption.

Evidence: final 44 focused Worker tests pass across cancellation and customer detail, including real Refund request/read-only provider-observation recovery, original plus additions, full prior refund, lost completion audit rollback, concurrent prior refund, registered legacy recovery and customer preview. Earlier affected Admin run passed 60 tests before the final validation/source-identity refinement. Six local Web/Core/D1 browser cases pass at 1440/390 for zero/partial/full prior refunds, lost response and identical replay (2.5 minutes). Mobile completed screenshot inspected. Browser fixture prior financial states are explicit test seams, not provider acceptance. Workspace types/lint/format/naming/architecture/readiness, diff check and Core dry-run build pass; browser setup executes Web build. Logs: remaining-cancellation-final-worker.log, remaining-cancellation-browser.log, remaining-cancellation-core-build.log. No schema change or retained DB reset/migration. Whole phases remain open.

Next: bound stuck-payment sweep retries/leases and prevent repeated oldest unresolved payments starving the queue; then continue outstanding Phase 1 and setup dependencies. Admin timeline's legacy Order-committed label displays current CANCELED status in the screenshot; correct its historical presentation during complete journey work. All remaining authorized phases and external activation gates remain as recorded above.


IN PROGRESS after pushed cancellation commit 1db81e0: authored 0075 payment_lookup_recovery with separate leases/version/attempts; stuck-payment sweep now bounds five read-only calls, backoff, current financial projection and audited exhaustion, avoiding repeated oldest-payment starvation. Added typed Admin payment recheck command/read progress/Core RPC/Web route/component with frozen-response retry. Initial 13 Worker/scheduling tests pass, 68 contracts, types/lint/static checks and fresh/populated migrations pass. First Worker iteration exposed an incorrectly assumed reconciliation-case updated_at column; corrected against actual schema. Managed browser is running; hold runtime source stable until completion. No new slice acceptance/commit yet, no retained DB mutation. Pending: use payments.manage consistently for payment (not refund) recheck, more starvation/revocation/exhaustion-failure tests, final browser/build/diff review. Continue all remaining phases thereafter.


Payment lookup continuation: 16 focused Worker/scheduling tests now pass, including bounded queue progress past eleven older unresolved Payments, final-attempt audit rollback/recovery without a sixth call, current authority revocation and conflicting new claims. Payment recheck consistently requires Global payments.manage. Full Web 374/96, contracts 68 and harness 26 pass; types/lint/format/static checks, Core dry-run build and migration checks pass. Upgrade verifier explicitly compares retained Payment rows and proves the new recovery table starts empty with enforced Payment FK.

Browser first run timed out during migration startup before any test; unchanged 180-second setup guard and zero test retries preserved. Second run passed both desktop/mobile acceptance/replay/reload journeys in 2.0 minutes; mobile screenshot inspected. Latest Web-only refinement adds an explicit stopped-check notice and distinguishes a failed read refresh from unknown command acceptance; mobile browser fixture now starts from EXHAUSTED and requires recovery version 1. That refinement still needs its browser run. Full Core suite currently running in payment-lookup-full-core.log; no result yet. Runtime source otherwise stable. No retained DB changes, phase acceptance or new commit yet.

Next confirmed Phase 1 defect: create-payment.ts persists provider Attempt before a conditional INITIATED/version-1 Payment update and continuation without a complete batch guard. Ignored/lost update can leave inconsistent dependent effects. Known provider acceptance with failed persistence survives only in reconciliation-case JSON while reconcilePayment requires an existing Attempt, so the new bounded lookup cannot recover that missing mapping. Repair durable provider-creation evidence/adoption and guarded persistence, retaining unknown outcomes and never repeating charge creation blindly. Provider response absence without a recoverable identity remains explicit external evidence limitation. Do not mistake bounded lookup scheduling for this missing creation-recovery capability.


Final payment lookup slice: full Core passed 1,027 tests in 169 files (265.80 seconds); 16 focused Worker/scheduling tests, 374 Web tests, 68 contracts and 26 harness tests pass. Types/lint/format/naming/architecture/readiness, fresh/populated migrations, retained Payment identity preservation, Core dry-run and Web build pass. Final browser desktop/mobile run passed both cases in 2.0 minutes with zero retries: new lookup and exhausted lookup recheck, lost response, identical saved body/key replay, versioned acceptance, current progress/audit after reload and no overflow. Mobile screenshot inspected. First browser setup timed out before tests; subsequent complete runs passed under the unchanged startup limit. Logs: payment-lookup-full-core.log, payment-lookup-worker.log, payment-lookup-web-tests.log, payment-lookup-browser-final.log, payment-lookup-core-build.log, payment-lookup-harness.log.

0075 is authored/tested only. New Core/Admin RPC recheckAdminPayment uses current Global payments.manage and both expected versions, guards active lease/state/audit/receipt, and only queues read-only recovery. Exhaustion is a visible reviewed condition, never financial failure or authority to charge again. Existing databases remain untouched. No whole commerce phase or provider acceptance is claimed. Continue the confirmed creation-persistence guard/missing-reference recovery defect, then the remaining authorized phases; external account capability/operational inputs remain final activation gates.


IN PROGRESS after pushed payment lookup commit a7cabe5: reproduced createPayment falsely returning success when D1 ignored its initial Payment transition. Authored 0076 immutable private payment_creation_observation; provider creation response now persists separately from guarded local adoption. recoverPaymentCreation guards current immutable subject/customer/money identity, version, Payment transition, Attempt, usable continuation, mandatory audit and applied_at in one batch. Concurrent adoption verifies existing complete evidence; original create replay and registered 15-minute lookup adopt without another external create. Invalid/expired continuation is not exposed; its saved reference remains usable for lookup. Resolution excludes unapplied creation evidence. Arbitrary provider error text no longer enters creation reconciliation-case details.

Current checks: initial negative test failed as expected (old command returned ok:true). Then 54 affected Worker tests passed; final four-suite refinement passed 46 tests including concurrent adoption, ignored dependent inserts, receipt immutability, lost audit, actual registered lookup and existing paid commitment. Types/lint/format/static checks, migration fresh/populated preservation and Core dry-run build pass. Full Core suite currently runs in payment-creation-full-core.log; no result yet. New 0076 not applied to any retained database; no Web/RPC surface changed and no new browser/provider acceptance claimed. Private continuation data stays out of logs/audit/public recovery reads.

Still pending this slice: full-suite outcome, final diff/canonical evidence review and commit/push. Known limits: no provider lookup can reconstruct a reference that was never durably captured without a verified provider idempotency-lookup capability; historical case-only unmapped references are not invented as creation observations. Conflicting terminal Payment state with unapplied creation evidence stays in financial review. Continue independent remaining commerce phases, with these real provider/evidence gates stated explicitly; no whole phase has yet been accepted.


Final creation adoption evidence: full Core passed 1,032 tests in 169 files (266.27 seconds). The final focused four-suite run passed 46 Worker tests; earlier overlapping creation/reconciliation/lookup/commitment run passed 54. Covered ignored Payment/Attempt/continuation/audit effects, immutable receipt, concurrent same-key adoption, identical continuation replay, and actual registered 15-minute provider lookup after lost local persistence, with one provider create. Types/lint/format/naming/architecture/readiness, clean/populated migrations and Core dry-run build pass. No new Web/RPC or browser/provider acceptance; prior browser evidence remains separate. Logs: payment-creation-regression.log (expected negative proof), payment-creation-final-worker.log, payment-creation-full-core.log, payment-creation-core-build.log. 0076 is authored/tested only; retained deployments untouched.

Next confirmed Phase 1 recovery gap: listDueInbox only selects RECEIVED/RETRY_REQUIRED, so a verified event marked RECONCILIATION_REQUIRED with INTENT_MAPPING_AMBIGUOUS or REFUND_UNMAPPED cannot recover automatically when the mapping appears. Unmapped Payment cases also retain null linkage, preventing later finance resolution. Repair narrowly bounded replay of mapping-related exceptions and guarded case association after verified application; do not reopen mismatched financial/illegal-transition or exhausted cases indiscriminately. Continue setup and remaining authorized phases after correctness dependencies. Unknown creation with no durable provider reference and historical case-only linkage remain explicit evidence limitations; never resubmit blindly.


2026-09-08 provider inbox recovery in progress after 081a1aa: mapping-only INTENT_MAPPING_AMBIGUOUS/REFUND_UNMAPPED exceptions now remain due within bounded replay. Lease claims durably consume attempts before application; both ingress and scheduled claims enforce ten attempts and 24-hour age. Expired final leases escalate without an eleventh application. Per-event application failure retains controlled retry and allows other due events to run. APPLIED/DUPLICATE evidence cannot be downgraded by late failure. Scheduled replay validates signature-verification evidence, normalized provider/event/hash, exact money and settlement before application. Verified unique mappings link previously unlinked matching reference cases through versioned required-audit transactions; cases remain OPEN. Failed association retries after canonical financial work without repeating it.

Evidence: 45 focused Worker tests passed before the final shared-claim bound; final 54 Worker tests across inbox, Refund and registered scheduling pass. Tests include real signed early Payment/Refund callbacks during provider creation, later mapping recovery, ignored required case-link audit rollback, abandoned final lease, repeated application failure with healthy queue progress, terminal preservation and malformed/unverified replay rejection. Types/lint/naming/architecture/readiness and diff checks pass. Full Core suite still running in provider-inbox-full-core.log; no result claimed yet. No schema/RPC/Web change, retained DB migration/reset, browser or live provider acceptance.

Next: inspect full suite, build/diff review and commit this slice. Exhausted inbox operator recovery is still missing and exhaustion cases can be unlinked; add a guarded, versioned, audited action and observable Web workflow without treating a reason as financial evidence. Continue remaining Phase 1 and all authorized commerce phases; no whole phase accepted.

Operator recovery design note: use a Payments-owned Global payments.manage command with current case version, exact verified event identity/current inbox status and lease, reason/key, required audit and frozen acceptance. Do not reset original received_at to bypass age limits; a fresh bounded recovery window needs separate persisted start evidence and preservation of prior attempts in audit/history. Case closure must exclude its unapplied provider inbox evidence as well as existing financial/Order/Refund recovery. Existing DTO only exposes case version/status/payment linkage, so add a safe recovery read model (no raw payload or private provider data) and Web saved-request replay. This is pending implementation, not accepted behavior.

Verification correction: the first full Core run failed 1/1042 tests because it loaded the pre-bound repository implementation while the new eleventh-attempt assertion was added during execution; 1041 passed. Final focused 54 tests passed with both changes loaded. A fresh full run with runtime/test sources held fixed is running in provider-inbox-full-core-final.log (session 70849). Do not report full-suite success until inspecting that result. Formatting and Core dry-run build pass.


Final provider inbox recovery evidence: stable full Core run passed 1,042 tests in 169 files (261.75 seconds). Final focused run passed 54 Worker tests. Types/lint/format/naming/architecture/readiness, diff check and Core dry-run build pass. Logs: provider-inbox-full-core-final.log, provider-inbox-final-worker.log, provider-inbox-core-build.log. The earlier mixed-source run failed the newly added retry-limit assertion; no checks were weakened. No schema/RPC/Web changes or new browser/provider acceptance. Retained databases untouched. This completes the bounded mapping-recovery slice only; whole commerce phases remain open. Continue guarded exhausted-event operator recovery and pending-inbox case-closure validation, then remaining authorized phases.


2026-09-08 provider inbox closure guard: shared Payments resolution eligibility now excludes pending/retry/reconciliation inbox work linked through current provider attempts or the case provider/event identity. A terminal Payment cannot hide unapplied event work. Worker test reaches FAILED through real provider lookup, seeds an explicit retained inbox seam, verifies closure rejection, then executes actual redrive and successful resolution. Added transaction-time inbox arrival test proves no partial closure/audit/idempotency result. Forty affected Worker tests pass across resolution, inbox and Admin finance; workspace types/lint/naming/architecture/readiness and diff checks pass. Log: inbox-resolution-worker.log. No schema/RPC/Web change or new browser/provider acceptance. Prior full 1042-test result belongs to f9f6922 before this focused refinement. All phases remain open; continue exhausted-event operator recovery.


IN PROGRESS after d069d98: 0077 adds nullable provider inbox recovery_started_at without changing original received_at. Payments retryProviderEvent requires Global payments.manage, current case version, exact verified financial event, exhausted state, no active lease, reason/key; case version, new bounded window/attempt reset, prior-attempt audit and frozen receipt share one guarded batch. Scheduler/ingress age bounds now use explicit recovery window or original receipt time. Exact replay retains receipt even after application; rejected authority/lease/evidence/transaction writes roll back. Verified application now links formerly unlinked exhausted-event cases by exact provider/event identity, leaving OPEN.

Typed retryAdminProviderEvent RPC/Admin wrapper/Web route and reconciliation-card retry component are wired. Read model validates private normalized evidence but exposes only progress/eligibility; UI retains exact body/key after unknown response. Six new command tests plus existing inbox/closure tests pass (28 Worker tests total), including old event recovery, concurrent retries, audit rollback, revoked replay, current lease/evidence changes and case linkage. Types/lint/naming/architecture/readiness and clean/populated migration checks pass. Upgrade verifier explicitly seeds retained exhausted event and compares all original columns/row identity; new recovery timestamp remains NULL.

Managed browser tests/provider-event-retry.spec.ts is currently running at 1440/390 with session 66654 and provider-event-retry-browser.log. Source freeze until it completes. No browser acceptance yet. Only named disposable e2e-commerce-alignment-20260907 state is reset; retained DBs untouched. No commit/phase acceptance. Pending: browser outcome/visual review, test missing required audit specifically (current forced batch rollback is broad), review replay-window boundedness and normalization guards, contracts/Web affected tests, final builds/static/diff checks, canonical docs and status. Then continue all authorized phases.


Final reviewed provider-event retry slice: 57 affected Worker tests pass in five suites, including eight new retry tests, ignored mandatory audit rollback, exact 24-hour reviewed-window expiry, old receipt replay, competing/revoked requests, transaction-time lease/evidence/authority changes and exhausted-case association. 68 contract tests, 374 Web tests, types/lint/format/naming/architecture/readiness, clean/populated migrations and Core dry-run build pass. Browser passed both 1440/390 cases in 2.4 minutes with zero retries: actual Web/Core/D1 queue acceptance, lost response, identical body/key replay and persisted progress after reload. Mobile screenshot inspected without overflow. Browser proves queue acceptance; Worker tests execute the actual redrive. Live provider acceptance remains separate. Final Core-only receipt/window snapshot guard was checked in the 57-test run after browser completion.

Logs: provider-event-retry-final-worker.log, provider-event-retry-browser.log, provider-event-retry-contracts.log, provider-event-retry-web-tests.log, provider-event-retry-migrations.log, provider-event-retry-core-build.log. 0077 authored/tested only; no retained DB migration/reset. New RPC retryAdminProviderEvent and safe optional providerEventRecovery read projection are documented canonically. A queued retry does not resolve a case or repeat provider creation. No whole commerce phase accepted; reassess remaining Phase 1 call paths, then continue setup and all remaining authorized dependencies.


IN PROGRESS after pushed ee91e64: Phase 1 re-audit confirms commitment missing-hold/multi-pool/packing race regressions exist and passed in the recent full suite; do not restart those fixes. New remaining gap: redrivePaymentReactions escalates PENDING to ESCALATED while case insert can be ignored, leaving no required case; no legal operator restart exists for exhausted COMMIT_ORDER/COMMIT_AMENDMENT reactions. Edited redrive-payment-reactions.ts to guard status, required case and required PAYMENT.REACTION_ESCALATED audit atomically. Concurrent winner detection checks existing audited escalation. Two ignored-effect tests added; all 11 scheduling Worker tests plus types/lint pass (reaction-recovery-worker.log). Edits uncommitted; no phase acceptance.

Next: handle the benign concurrent already-SUCCEEDED reaction during escalation without spurious failure, then Payments-owned Global payments.manage versioned case-based retry for exhausted commerce reactions. Revalidate current canonical SUCCEEDED Payment and current reaction subject/type/state/attempts/lease; preserve prior attempts in audit, reset only retry budget, queue the same owning applier and retain frozen receipt. Do not enable retired membership restart. Add safe Admin read projection/RPC/Web unknown-response retry, race/rejection/replay and browser checks. Existing financial compensation gap remains: fully refunded uncommitted Payment with an ESCALATED reaction cannot pass closure; define explicit guarded compensation completion using actual successful Refund evidence, never label failed commitment SUCCEEDED or invent an Order. This is implementation work under existing full remaining-plan authorization, not a request for confirmation.


IN PROGRESS reaction retry after ee91e64: Payments retryPaymentReaction now validates Global payments.manage, case and Payment expected versions, exact commerce reaction subject/type/purpose, canonical SUCCEEDED, no active/successful/unfinished refund exposure, current ESCALATED state/attempts and expired lease. Case version, PENDING/reset budget, prior-attempt audit and frozen receipt are guarded atomically. Typed retryAdminPaymentReaction RPC/Admin read projection/Web route/card are wired; unknown responses preserve body/key. No schema change. Reaction escalation now guards required case and audit, and recognizes benign concurrent successful application.

31 focused Worker tests pass: real Instant quote/two-pool workflow with explicit captured-payment fixture, actual injected commitment failure/escalation, direct authenticated Core retry and actual applier success once; plus real createPayment/provider lookup followed by retained exhaustion-count fixture, command races/revocation/refund reservation/retired membership rejection/transaction-time payment version, lease, authority and ignored audit. Types/lint/naming/architecture/readiness pass.

Runtime source freeze: managed browser tests/payment-reaction-retry.spec.ts is running (session 87936, reaction-recovery-browser.log), and full Core suite is running (97223, reaction-recovery-full-core.log). Contracts/Web aggregate tests session 55214. No new browser/full-suite success yet. Only named disposable browser state reset; retained DBs untouched. Next inspect all results, mobile screenshot, final docs/build/diff, commit. Pending beyond retry: explicit finance-reviewed compensation completion for fully refunded uncommitted reactions, preserving actual Refund evidence and no fabricated Order/reaction success; then remaining Phase 1/setup and all authorized phases.


Reaction retry acceptance progress: both managed browser cases passed in 2.5 minutes at 1440/390, zero retries, real local Web/Core/D1 queue acceptance, lost response/exact replay, pending state after reload. Mobile screenshot inspected without overflow. All 68 contracts and 374 Web tests pass. Core dry-run build, format and diff checks pass. Full Core session 97223 remains running in reaction-recovery-full-core.log as of 12:32; active workerd instances are advancing, so not declared hung/passed. Keep runtime/test source fixed until result. No new commit or phase acceptance yet.

Read-only follow-up: checkout abandon currently refuses REFUNDED Payments and its already-terminal path does not release residual holds; refunded uncommitted cleanup needs an owning Checkout/Orders operation rather than calling customer abandonment or mutating their state ad hoc from Payments. paid_order_amendment supports FAILED/CANCELED as well as COMMITTED. payment_reaction supports FAILED, permitting honest non-applied compensation completion once actual full Refund evidence and no committed Order/amendment are proved. Existing finance_exception records are not resolved by successful applyCheckoutPaymentReaction; address their current recovered projection while completing finance recovery. No code for compensation has been written yet.


Final reviewed reaction retry evidence: full Core passed 1,062 tests in 171 files (379.88 seconds). Focused 31 Worker tests, 68 contracts, 374 Web tests, types/lint/format/naming/architecture/readiness, Core dry-run and Web build pass. Browser passed both widths in 2.5 minutes with zero retries and mobile visual inspection. Logs: reaction-recovery-full-core.log, reaction-recovery-worker.log, reaction-recovery-contracts.log, reaction-recovery-web-tests.log, reaction-recovery-browser.log, reaction-recovery-core-build.log. No schema change in this slice, retained database mutation or live-provider acceptance. New retryAdminPaymentReaction uses the existing commerce appliers; a missing Quote remains an exception. Reaction escalation now cannot persist without its required case and audit. Whole phases remain open; continue refunded uncommitted cleanup/compensation and recovered exception projection, then setup dependencies and all authorized phases.


2026-09-08 commitment financial-race repair after e6accad: reproduced three Instant defects in actual Worker/D1 batch interception: after prevalidation, a REFUNDED Payment, changed reaction subject or newly reserved Refund still produced applied:true. Reproduced amendment commitment after Payment version change or reserved Refund. Checkout now checks matching reaction before replay and atomically guards Payment version/purpose/subject/customer/money/captured state, reaction PENDING identity and no active/successful/unfinished refund exposure before dependent effects; final reaction update is also guarded. Amendment applies the same current financial/refund checks and exact reaction binding. No database policy triggers or schema changes.

47 affected Worker tests pass across Instant, Scheduled checkout, paid amendments and reviewed reaction retry. Types/lint/naming/architecture/readiness and diff checks pass. Negative proof logs: commitment-financial-race-regression.log (3 expected failures), amendment-financial-race-regression.log (2 expected failures). Passing log: commitment-financial-guard-worker.log. The earlier full 1062-test/browser evidence belongs to e6accad; this Core-only refinement has focused Worker verification, no new browser/provider claim. Continue refunded uncommitted cleanup/compensation and recovered exception projection, then setup and remaining phases.


IN PROGRESS after pushed e2e1845: compensation uses the existing Global refunds.manage resolveAdminReconciliationCase command, not a new money-changing RPC. Added refundedCommitmentResolutionEvidence alongside the original completed-evidence guard. Eligible fully REFUNDED/no-Order commerce reaction cases show optional resolutionAction=CONFIRM_REFUNDED_COMMITMENT and explicit Web confirmation copy. The transaction composes Checkout-owned releaseRefundedCheckoutStatements, Orders-owned completeRefundedCommitmentStatements, reaction FAILED/REFUNDED_WITHOUT_COMMITMENT, provider-action expiry, mandatory Orders/Payments audits, ordinary completed-evidence case closure and frozen original receipt. Another active Payment or winning committed Order for the Quote preserves shared entitlements. No new schema; historical capacity release is compatibility cleanup only, no new Scheduled capacity policy.

Thirty focused Worker tests pass across Instant and financial case resolution. New scenarios drive actual Refund submission/provider lookup (captured payment is the existing explicit Instant fixture), exact full sum including two Refunds, ignored hold release/reaction mutation/required Orders or Payments audit rollback, no fabricated Order, no partial key result, and replay. Types passed before these new tests; final checks pending. Log refunded-commitment-worker.log. All current compensation changes remain uncommitted; no browser/full-suite acceptance yet.

Pending: prove no cleanup of another active/winning quote owner; add paid-amendment refunded completion test; harden legacy capacity missing/ignored balance updates; review historical FAILED uncommitted reaction handling (current compensation eligibility only ESCALATED reaction-failure case); browser explicit refunded-outcome/lost-response/replay and affected Admin/contract/Web checks; canonical docs/status, build and diff. Existing finance_exception recovery on successful (rather than refunded) commitment still needs projection repair. Continue all authorized remaining phases without routine confirmation.


Refunded commitment continuation: final focused run passed 56 Worker tests across Instant, amendments, financial resolution and Admin finance. Added protection for another active quote payment (holds and ACTIVE Quote preserved), actual paid-addition creation/payment/provider refund/lookup followed by failed-addition closure without changing original Order/demand, and retained FAILED reaction cleanup. Legacy capacity cleanup now guards missing balance, underflow and ignored balance update in the composed transaction. All 374 Web tests pass; types/lint/naming/architecture/readiness pass.

Managed browser tests/refunded-commitment-resolution.spec.ts is running in session 50249, log refunded-commitment-browser.log; hold source fixed until completion. No browser result yet. Canonical API/state/domain now describe explicit CONFIRM_REFUNDED_COMMITMENT under the existing versioned Global refunds.manage resolution action. No new schema/RPC or retained DB mutation. Pending: browser result/mobile inspection, contracts result, final build/format/diff review and commit. Then repair recovered finance_exception projection after successful commitment and remaining Phase 1/setup; all phases remain open.


2026-09-08 refunded commitment slice verified: 56 affected Worker tests, 68 contracts, 374 Web tests, types/lint/naming/architecture/readiness, Core dry-run build and format/diff checks pass. Both managed browser journeys passed at 1440/390 in 2.6 minutes, including explicit confirmation, lost response/exact replay, resolved-case removal after reload and mobile visual inspection. Added three retained Scheduled-capacity compatibility scenarios through the Checkout-owned transaction helper: release once/replay, ignored balance update rollback and underflow rollback; all eight tests in abandon-checkout-attempt.integration.test.ts pass. This helper fixture is retained compatibility evidence, not an active Scheduled capacity workflow. Logs: refunded-commitment-final-worker.log, refunded-commitment-contracts.log, refunded-commitment-web-tests.log, refunded-commitment-browser.log, refunded-commitment-core-build.log, refunded-capacity-worker.log. No schema/RPC addition, retained database mutation or live provider acceptance. No whole phase accepted. Next: recovered finance_exception projection after successful commitment, then remaining commerce dependencies.


2026-09-08 after 2a471b9: fixed successful commitment exception projection. Orders now requires the exact committed Order/payment/reaction/quote relationship before resolving matching finance_exception rows and writing one stable ORDER.COMMITMENT_RECOVERED audit per exception. New commitment includes this in its transaction; replay and existing versioned Global financial-case resolution repair retained stale projections. Late failure diagnostics cannot create an exception after a matching committed winner. 53 affected Worker tests pass, including actual failed commitment/reviewed retry/redrive, retained projection seam through authenticated Core resolution, ignored audit/projection rollback and replay without duplicate Orders/audits. Types, architecture and readiness pass. No schema/RPC/Web change or new browser/provider claim. Log committed-exception-final-worker.log. Final late-diagnostic guard validation pending; all commerce phases remain open. Next checkout abandonment financial race/terminal cleanup, then remaining setup and commerce dependencies.

Final successful-commitment projection verification: all 53 affected Worker tests pass after the late-diagnostic guard; lint and diff checks pass. No retained data changed. Ready to commit this slice and continue checkout abandonment.


IN PROGRESS after pushed 6830004: reproduced abandonment admission race (Worker test returned ok:true after concurrent INITIATED Payment). Reworked abandonCheckoutAttempt into one guarded transaction: current active Customer/owned Quote/version, financial exclusion including committed link, exact held count, idempotency claim, shared Checkout entitlement release, required CHECKOUT.ABANDONED audit and frozen result. Expired/superseded Quotes release residual holds with the same financial guards. Same-key recovery after ignored required effects and competing-key serialization tested. Renamed release-refunded-checkout.ts to release-uncommitted-checkout.ts for reuse; reviewed refunded completion still passes. No schema change. 57 Worker tests pass across abandonment, real Instant quote creation-to-release and commitment/refund recovery. Types/lint/naming/architecture/readiness pass. Negative log abandon-payment-race-regression.log; positive abandon-guard-worker.log.

Web now awaits release before changing address/window/promotion or replacing the Quote; unknown release retains exact quote/body/key and retry, and concurrent payment confirmation is blocked while release is in flight. Promotion entry awaits acceptance before claiming the code was added. Focused Web tests including lost-response retry pass. Managed browser checkout-abandonment.spec.ts currently running in session 83362, log abandon-browser.log; runtime source frozen until completion. Browser uses real auth/cart/address/quote/release with explicit operational stock/readiness setup and mock delivery pricing; only named disposable e2e-commerce-alignment-20260907 is reset. No new browser/provider acceptance yet. Pending browser diagnostics/visual review, aggregate affected Web/contracts and final checks, then commit and continue remaining Phase 1/setup. No phase accepted.

Browser first run failed both widths before quoting: selecting the address while the cart was still loading never retriggered fulfillment-option reads. Trace showed real cart/address success and no fulfillment-options request. Added dependency-driven option loading with stale-response generation protection and controlled read failure, plus a delayed-cart Web regression. All nine focused Web tests and types pass. Core abandonment also rechecks a linked active customer principal at the write boundary. Managed browser rerun is session 83008 (abandon-browser-final.log); full Core suite session 11525 (commerce-recovery-full-core.log). Runtime and test source frozen during both runs. No acceptance claimed before results.

Follow-up diagnosis: configured browser rerun previously reached real fulfillment options but provider quotation was unavailable; managed wrangler.e2e.jsonc now explicitly uses the existing test-only Lalamove mock and the browser fixture supplies a synthetic pickup profile. Current browser session 43019, abandon-browser-configured.log. The full Core run (commerce-recovery-full-core.log) ended with native process exit 3221226505 before a final summary, and exposed two scheduling fixture failures: COMMIT_ORDER reactions had subscription subject types. Corrected fixture identities without changing assertions; all 11 scheduling tests pass in committed-projection-scheduling-final.log. A fresh full suite remains required.

PayMongo creation adapter now throws controlled PAYMENT_OUTCOME_UNKNOWN for network/server/unreadable/malformed responses rather than returning definite failure; explicit request-rejection statuses remain failures. Four new actual createPayment + PayMongo adapter/fixture-HTTP tests prove INITIATED + visible AMBIGUOUS_OUTCOME and no second POST on replay. All four pass, but their current placement in create-payment.integration.test.ts contributes retained pending rows before an older bounded registered-lookup test; that existing test missed its target in the first ten-row sweep. Move the new cases to a dedicated focused file after browser source freeze ends, then rerun both suites without weakening the lookup assertion. Current combined result 36 passed/1 failed; no final acceptance/commit. Latest Web aggregate 376 tests passes; types/lint/format/static checks pass. Continue all authorized work.

Read-only provider recovery research: official https://docs.paymongo.com/reference/the-payment-intent-object documents string metadata on Payment Intents, and https://docs.paymongo.com/docs/developer-tools-webhooks-events documents resource snapshots in signed events. Existing creation writes freshmarkets_idempotency_key metadata. A future verified-reference recovery may use this evidence, but needs durable requested-provider identity before submission and exact mapping guards. This is not yet implemented or account-verified.

Browser configured acceptance: both 1440/390 journeys passed with zero retries in 2.5 minutes (abandon-browser-configured.log), using real local authenticated Web/Core cart/address/quote/abandon commands, mock provider fee and explicit operational stock/pickup setup. Unknown release response preserves the Quote, and exact body/key replay returns the original one-hold release. Mobile screenshot inspected; no horizontal overflow. Existing membership banner/customer provider labeling remain known later alignment work, not accepted UX.

Moved the four PayMongo unknown-creation tests to payment-creation-outcome.integration.test.ts to isolate their pending fixtures from the older bounded scheduler test. All 37 payment creation/adapter tests now pass without changing the scheduler assertion. Added explicit 422-rejection coverage before starting the fresh standalone full Core run, session 41645, commerce-recovery-full-core-final.log. Keep runtime/test source fixed until completion. Latest full Web 376/96, contracts 68/19, types/lint/format/static gates pass; whole commerce phases remain open. No retained DB migration/reset/provider call/deployment.

Final current recovery verification: standalone full Core passed 1092 tests in 172 files (418.62 seconds), commerce-recovery-full-core-final.log. This includes explicit PayMongo 422 rejection plus four unknown-response cases, corrected scheduling identities and checkout/refund recovery. Full Web 376 tests, contracts 68, types/lint/format/naming/architecture/readiness and Core dry-run build pass. Managed checkout browser passes both widths in 2.5 minutes with actual Web/Core commands, injected lost response and stable replay; mock delivery and synthetic pickup/stock are documented setup, not live provider acceptance. Native-crashed earlier full run is not counted as passing. No schema change, retained DB mutation or deployment. Save the slice on main, then continue missing setup/identity workflows and remaining commerce dependencies; no whole phase accepted.

Read-only next findings: createPayment currently replays an unresolved INITIATED/no-provider-reference intent as ok:true/action NONE, producing a misleading browser pending message; preserve unresolved failure on that replay. Existing staff invitation acceptance needs current verified-email validation in the batch and guards for every required staff/grant/audit effect. A clean installation lacks initial Global administrator enrollment; implement configured owner identity plus verified sign-in, one-use durable grant/audit and explicit roles/scopes, never infer the owner from Git or widen existing staff access. Actual configured owner identity is a deployment input.

2026-09-08 after pushed 0f34b40: reproduced four unknown-creation replays returning success/action NONE, then fixed createPayment replay to retain PAYMENT_OUTCOME_UNRESOLVED while the original intent remains INITIATED after recovery. No second provider call or fake ready action. All 20 affected Worker tests across payment creation/unknown outcome and commerce flow pass, as do types/lint. Negative log payment-unknown-replay-regression.log; positive payment-unknown-replay-worker.log. Earlier full 1092-test evidence belongs to 0f34b40; this refinement has focused verification. No schema/RPC/UI change or new browser/provider claim. Continue setup/access dependencies; whole-phase acceptance remains open.

2026-09-08 staff invitation acceptance hardening: Core now rechecks the current verified authentication email inside the batch, claims idempotency atomically, and requires every staff identity, role, scope, audit and successful receipt write. Suppressed required effects roll back the invitation and all access; retained PROCESSING/FAILED claims with identical intent recover safely. Current identity changes reject before grants. No new schema/RPC or retained database mutation.
Validation: 28 staff Worker/D1 tests; 12 Web adapter tests; workspace types/lint, changed-file formatting, naming/architecture/readiness checks pass. Managed browser admin-staff-access.spec.ts passed all seven tests with zero retries in 2.9 minutes, including actual role/invitation creation, authenticated acceptance, lost-response identical-key retry, local-reader denial and desktop/mobile review. Mobile screenshot inspected. Auth verification and initial manager grants use explicit isolated test fixtures; email/provider delivery and initial Global setup are not accepted. Logs staff-acceptance-final-worker.log and staff-acceptance-browser.log. The added retained-claim test initially used the creation DTO for a missing version; corrected it to read the actual invitee offer and reran successfully. Continue initial administrator setup and remaining plan dependencies; no whole commerce phase accepted.
2026-09-08 after pushed 0edd870 (staff acceptance): initial Global administrator setup implementation is in progress. New migration 0078 adds singleton immutable setup evidence only; configured INITIAL_GLOBAL_ADMIN_EMAIL is empty in development/staging and synthetic only in test configuration. New typed getInitialAdministratorSetup/completeInitialAdministratorSetup RPCs and /setup Web page implement verified configured identity, no existing Global scope/no existing caller Staff identity, explicit non-membership capability grants, all-effect atomicity and exact receipt replay after config removal. No retained DB/configuration was applied remotely.
Focused Worker run initial-admin-worker-final.log passes 17 tests including conformance, creation to real role/invitation commands, competing keys, identity rejection, eight suppressed required-effect rollbacks/retries and immutable evidence. Initial fixture failures were corrected: sign in while verified before simulating revoked verification; explicitly clean test-only invitation children before removing the fixture Staff. Production constraints were unchanged. Migration verification passes fresh creation and retained 0068/current upgrades with complete pre-0078 row/grant preservation. Types/lint/naming/architecture/readiness pass. Browser currently running session 75492 (initial-admin-browser.log); do not modify runtime/tests until it completes. Web aggregate session 80667; contracts/harness command completed. Next: inspect browser/screenshots, test another identity's Global scope appearing before setup batch (current test checks same identity only), then affected runtime/build gates and commit. No whole phase accepted; continue all remaining authorized commerce work.
2026-09-08 initial administrator setup verified: complete local Web/Core workflow at /setup, configured verified identity, immutable one-use evidence (migration 0078), explicit non-membership Global grants, atomic required effects and replay after configuration removal. Added another-account Global-scope race rejection and Worker retained-upgrade empty-setup check. All 19 focused setup/conformance/upgrade tests pass. Standalone full Core passed 1116 tests / 173 files in 434.15 seconds (initial-admin-full-core.log); full Web 376 / 96, contracts 68 / 19, harness 26, migrations, types/lint/format/naming/terminology/architecture/readiness and both builds pass. Managed browser initial setup passed with zero retries in 2.8 minutes: desktop/mobile access review, wrong-account denial, lost-response exact replay and real Staff administration without seeded Staff grants. Mobile screenshot inspected. Email verification is an explicit test fixture, not live delivery or OAuth acceptance. No retained DB upgrade/reset/deployment; real initial owner configuration remains a deployment input.
Next within alignment Phase 2: harden older staff role/invitation administration, which still has pre-read-only authority and incomplete required-effect guards, before complete onboarding acceptance. Then customer invitation/profile/closure intake and remaining location/cycle setup. Prior invitation acceptance is preserved; do not restart that fix. No whole commerce phase accepted. Continue all authorized plan phases.
2026-09-08 after pushed c1de93f: reproduced role creation and invitation creation returning success after their manager lost Global scope immediately before the D1 batch (staff-creation-authority-regression.log, two failing regressions). Current uncommitted fix introduces a small shared IAM staff-administration-write helper for current authority, in-transaction claim and required receipt completion; both creation commands guard every required effect and save validated original DTO receipts. Legacy success references keep read-back compatibility; unfinished same-hash claims can recover. Fifty-seven affected Worker tests pass, including suppressed role/grant/invitation/audit/receipt rollback/retry and original receipt replay after real rename/revoke commands. Types/lint/naming/architecture/readiness pass. Browser combined initial setup/staff journeys is running session 21146 (staff-creation-browser.log); source held fixed. Remaining staff lifecycle commands still need current-authority/version/atomic-effect review, then customer setup and the rest of the plan. No whole phase accepted.
2026-09-08 staff creation verification complete: all eight combined initial setup/staff browser journeys passed with zero retries in 2.8 minutes (staff-creation-browser.log). This exercises the current guarded role/invitation commands and verified acceptance/replay at desktop/mobile widths. Fifty-seven Worker tests, types/lint/format/naming/architecture/readiness and Core dry-run build pass. Earlier full 1116-test Core evidence belongs to c1de93f; this incremental creation change has focused verification. No schema/RPC changes or retained DB mutation. Continue versioned atomic invitation revocation and remaining staff lifecycle commands, then customer and location/cycle setup; no whole phase accepted.
2026-09-08 after pushed af2811a: versioned staff invitation revocation is in progress. Admin invitation DTO/list now expose version; Core/BFF require expectedVersion. Revocation rechecks current Global authority, guards the PENDING transition, requires audit and frozen receipt in one transaction; acceptance/revocation race has one winner. Creation snapshots predating the added version decode as their known original version one; legacy successful revocation hashes remain readable. Staff-page actions retain an unconfirmed body/key, block replacement and expose explicit original-request retry. Added Worker rollback/state/scope/race tests: 62 affected tests pass, along with 12 Web adapters, contracts 68, full Web and types/lint/static checks. Browser session 44645 (staff-revocation-browser.log) is running, source frozen; includes UI invitation creation and revocation lost-response retry with edited reason at desktop/mobile widths. Next inspect results/layout; add explicit legacy receipt and missing-version adapter tests, then final relevant checks and save. Continue remaining staff lifecycle atomicity, customer setup and all later commerce phases; no whole phase accepted.
2026-09-08 staff invitation revocation verified: current Global authority, reviewed version, transition, audit and original receipt are guarded in one transaction. Acceptance/revocation races have one winner; ignored required effects roll back and retry safely. Legacy successful receipts remain readable. Staff UI preserves the exact unconfirmed request through explicit retry, including after editing the reason. All 63 affected Worker tests, 17 Web adapter tests, 68 contracts and 376 full Web tests pass; nine managed browser journeys passed with zero retries at desktop/mobile widths. Types/lint/format/naming/architecture/readiness, Core dry-run build and diff checks pass; managed browser performed the Web build. Logs staff-revocation-final-worker.log, staff-revocation-browser.log and staff-revocation-core-build.log. No schema or retained database changes, no live provider acceptance. No whole phase accepted. Next: remaining staff lifecycle atomicity, customer setup, location/cycle setup and later authorized phases.

2026-09-08 after pushed 465671e: staff rename/activate/suspend now recheck Global authority and target version/state, require transition/audit/receipt in one transaction, and replay before same-state rejection. Staff receipt is assembled inside the winning batch from current identity/role/capability/scope relations; legacy resource receipts remain readable. Shared staff Web command hook now protects list and detail actions from unconfirmed replacement. Fifty-four affected Worker tests and 381 Web tests pass; new scenarios include real invitation acceptance followed by rename/suspension, replay after later changes, competing versions, scope revocation and ignored effects. Corrected a test-only audit column typo; assertions remain intact. Types/lint/naming/architecture/readiness pass. Managed browser session 13541 (staff-lifecycle-browser.log) is running with source fixed. Next inspect browser, final build/diff, canonical/status update and save; then remaining role/scope/session administration and all later authorized commerce phases. No whole phase accepted; retained databases untouched.

2026-09-08 staff lifecycle slice verified: 54 affected Worker tests, 381 Web tests, types/lint/format/naming/architecture/readiness and Core dry-run build pass. All nine managed staff browser journeys passed with zero retries in 2.9 minutes, including real invitation acceptance then rename, suspension/lost response/exact retry and reactivation at 1440/390 widths; mobile screenshot inspected. Managed stack performed Web build. Logs staff-lifecycle-final-worker.log, staff-lifecycle-web.log, staff-lifecycle-browser.log and staff-lifecycle-core-build.log. No schema/RPC changes, retained database changes or live provider acceptance. No whole phase accepted. Continue role/scope/session command guards and full setup acceptance, then all later authorized phases.

2026-09-08 after pushed 714c661: guarded role/scope replacement and paged assignment preservation in progress. Both commands now admit current Global authority, guard reviewed staff version, require complete deletion/every insertion/audit/receipt, and revalidate active role/geography inside the batch. Shared replay precedes mutable eligibility; result snapshots preserve original relations. AdminStaffDetail now includes complete roleIds so checkbox changes preserve off-page assignments. Retained snapshots resolve saved immutable codes, not current assignments. All 73 affected Worker tests passed; separate retained-receipt test passed (one selected, 51 outside filter), contracts 68, full Web, types/lint/naming/architecture/readiness pass. Browser session 94840 (staff-grants-browser.log) is running with source fixed; now exercises real limit-one role paging, unknown role update replay and scope changes. Next browser result/visual check, final build/docs/status/diff and save; then session revocation and role-definition lifecycle, remaining setup and later phases. No phase accepted; retained databases untouched.

Staff-grant browser first run: seven passed/two failed because Playwright checkbox.check expected immediate local checked state while the controlled UI intentionally waits for Core confirmation. The test now clicks, then still requires the original request retry and confirmed checked state with both role IDs preserved; production behavior and assertions were not weakened. Rerun session 78235, staff-grants-browser-final.log. Source fixed during execution. Failure log test-cookie lines redacted. Worker and contract results remain passing.

2026-09-08 role/scope assignment slice verified: all nine managed browser journeys passed with zero retries in 3.0 minutes after correcting the click action; both widths preserve an off-page assigned role through a real limit-one picker, retry the exact lost role update and change Global/location scope through Core. Mobile screenshot inspected. 73 affected Worker tests plus retained-snapshot compatibility selection, 68 contracts, 381 Web tests, types/lint/format/naming/architecture/readiness and Core dry-run build pass; managed browser performed Web build. No migration/retained DB change or provider acceptance. Complete AdminStaffDetail.roleIds is the only contract addition. No whole phase accepted; continue session revocation/role definitions and remaining setup/commerce.

2026-09-08 after pushed e829340: remaining session and role-definition commands hardened. Session revocation guards current authority/target/version/count, requires complete deletion/audit/frozen result, preserves later logins on replay and supports retained numeric receipts. Role rename/capability/archive uses current authority/active version, required grants/deletion/audit/frozen receipt; archived existing assignments remain preserved. Role creation/detail UI now uses exact unconfirmed-request recovery and displays failed capability reads. 85 affected Worker tests pass, including ignored effects, authority races, real concurrent login, later-login replay, role edit/archive competition and replay after archive. Role-list test now walks real five-row pages with global ordering/uniqueness checks instead of assuming a seeded role is on the first page. Types pass. Browser session 92311 (staff-role-session-browser.log) running with source fixed; expanded journey creates role via form, selects capability, completes staff lifecycle/session revoke and role archive recovery. Pending browser/Web/static results, final diff/build/status and commit; then remaining customer/setup/commerce phases. No whole phase accepted; no retained DB/schema/RPC change.

2026-09-08 role/session slice verified: 85 affected Worker tests, 381 Web tests, types/lint/format/naming/architecture/readiness and Core dry-run build pass. The combined managed startup exited before tests without a useful cause. Its Web build passed; separately reran named disposable-state preparation and all 78 migrations successfully, started the exact configured vinext/Core test topology, then ran the same nine browser tests with E2E_AUTHENTICATED=1 and APP_BASE_URL=http://localhost:3100. All nine passed with zero retries in 1.1 minutes; desktop/mobile form role creation, capability selection, invitation/assignment/lifecycle, lost session response/exact replay, actual invitee logout, role rename/capability/archive recovery. Mobile archived-role screenshot inspected. Test runtime session 15582 stopped after completion. Logs staff-role-session-final-worker.log, staff-role-session-web.log, staff-role-session-startup.log, staff-role-session-runtime.log, staff-role-session-browser-final.log, staff-role-session-core-build.log. No retained database migration/reset, schema/RPC addition, provider acceptance or whole-phase acceptance. Continue remaining setup UX/customer onboarding/operational configuration and later commerce phases.

2026-09-08 after pushed 2ec5f27: customer/principal.ts first-access reconciliation now composes principal/customer creation or retained-ID linking, active relationship guards, required new-Customer audit and result read in one D1 batch. Existing Customer access uses one joined read; disabled legacy customers are rejected before linking. New CUSTOMER.PROVISIONED audit has one stable creation identity and is not fabricated for legacy relinking. Thirty focused customer/address Worker tests passed before the final read refinement; scenarios include actual authenticated direct resolution with principal disabled before batch, ignored audit rollback including missing principal, four concurrent first address reads and retained Customer ID preservation. Types/lint/naming/architecture/readiness pass after refinement. Standalone full Core now running session 21133 (customer-provisioning-full-core.log); keep runtime/test source fixed until completion. No Web/RPC/schema change or browser result for this resolver slice yet. Next inspect full run, relevant browser address/customer journey, final docs/build/diff and save; then customer onboarding/preferences/closure and remaining setup/commerce. No whole phase accepted; retained databases untouched.

Read-only next-step findings during full-suite source freeze: customer invitations have durable rows and Global creation/listing but no verified-customer offer/acceptance or revocation path. Their acceptance must compose customer provisioning rather than provisioning before a rejected invitation command. Current notification outbox requires an existing Customer FK, so invitation delivery needs an explicit retained-safe model extension; do not manufacture Customer records merely to send an invitation. Canonical profile fields permit language/notification preferences but do not specify which service notifications may be muted; preserve this as an owner acceptance input while independent onboarding proceeds. Irreversible anonymization remains dependent on real retention/field policy. Browser resolver verification will use the existing real Web/Core checkout-abandonment journey after the standalone full Core run completes.

Customer provisioning full verification: standalone Core passed 1155 tests / 173 files in 440.68 seconds (customer-provisioning-full-core.log), including the final joined legacy-status read. Managed real checkout/cart/address/release browser tests are running in session 21841 (customer-provisioning-browser.log), source fixed. Full-suite outcome supersedes earlier focused counts for this working tree; browser/provider acceptance remains separate. Next browser result, Core dry-run build/diff/status and commit, then compose verified customer invitation acceptance with guarded provisioning and harden customer administration. No whole phase accepted.

2026-09-08 customer provisioning slice verified: full Core 1155/173 passed (440.68s), types/lint/format/naming/architecture/readiness and Core dry-run build pass. Both managed checkout browser journeys passed with zero retries in 2.5 minutes, exercising real authenticated first cart/address access, quote/hold creation and lost-response release recovery at 1440/390. Operational stock/readiness are explicit disposable fixtures and delivery pricing uses the configured test adapter; this is not provider acceptance or full setup acceptance. Web build ran as part of managed setup. No schema/RPC/Web source change or retained DB mutation. Continue verified customer invitation creation/acceptance/revocation and remaining customer/setup/commerce work; no whole phase accepted.

2026-09-08 customer invitation workflow verified after 66f5bdb: added verified-identity offer/acceptance RPCs and Web page, plus versioned Global revocation and the operator invitation queue. Acceptance composes first-access provisioning with current verified email, expiry, pending version, active customer access, required audits and frozen receipt in one transaction. Creation/revocation now guard current Global customers.manage authority and all effects; legacy creation reference receipts remain readable. Exact replay, distinct-key races, acceptance/revocation races, withdrawn verification, expiry boundaries and ignored required writes are covered. Operator creation/revocation and customer acceptance retain exact unconfirmed bodies/keys; queue pagination is exercised with one-row pages. Shared Web command hook renamed use-admin-command for its actual Staff/Customer consumers.
Validation: 41 focused real Worker/D1 tests in three files (customer-invitation-race-worker.log); 385 Web tests, 68 contracts, types/lint/format/naming/architecture/readiness passed. Final managed browser run: six tests passed, zero retries, 2.8 minutes, including desktop/mobile creation, acceptance and revocation after lost responses and paginated review (customer-invitation-browser-final.log). Mobile customer page inspected. Core dry-run build passed; managed run built Web and created/migrated only named disposable e2e-commerce-alignment-20260907. No new schema/migration, retained database changes, live email/provider acceptance or whole-phase acceptance.
Next: customer access/session command atomicity and replay, remaining profile/support and privacy-policy boundaries, invitation notification delivery, then location/cycle setup and all later authorized phases. Invitation email delivery remains explicitly unavailable in the operator UI; creation is not presented as email delivery. Normal customer self-registration is independent of invitation acceptance.
2026-09-08 customer access/session administration verified after a0ff070: reproduced both old commands returning success when required audit insertion was ignored (customer-access-regression.log, two failing tests). Replaced them with current Global customers.manage admission, in-transaction claims, required complete effects/audit/receipts and original-result replay. Access changes guard the exact Customer/principal identity, state and positive version; both writes and the frozen committed-order summary are atomic. Session revocation guards target identity/version and the reviewed complete session set; a concurrent login rejects before deletion, and successful replay preserves later logins. Legacy reference/count receipts remain readable. Web detail uses the shared exact-request retry hook and validated BFF payloads. Customer audit history now follows Customer/accepted-invitation/privacy resource ownership, showing staff actions while excluding unrelated actions authored by a staff user who also has a Customer.
Validation: 53 focused real Worker tests passed (customer-access-audit-worker.log), 389 Web tests passed, types/lint/format/naming/architecture/readiness and Core dry-run build passed. Six managed browser tests passed with zero retries in 2.7 minutes (customer-access-browser-final.log): desktop/mobile invitation journey followed by lost-response disable/restore/session revocation, actual address denial/restoration, actual sign-out, and visible staff audit rows. Mobile layout inspected. Web build and disposable database setup were executed by the managed harness; retained databases were untouched. No schema change, live provider acceptance or whole-phase acceptance.
Next: remaining customer profile/support/closure work, invitation notification delivery, location/cycle administration and later authorized commerce phases. Notification muting policy was asked asynchronously: mandatory transactional emails versus customer-muted transaction updates remains unanswered; continue independent work without inventing that policy. Retention/irreversible anonymization and actual provider/account activation remain external acceptance inputs.
2026-09-08 closure recovery verified after 19f5d51: reproduced COMPLETED closure leaving customer commerce access active (customer-closure-regression.log). Privacy creation and lifecycle commands now recheck current Global customer authority and Customer identity/version, require all writes/audits/receipts in one transaction and replay original results before later state rejection. CLOSURE completion atomically disables the principal, advances Customer version, revokes the reviewed session set and records distinct closure/action audits. A later explicitly authorized restoration is preserved on closure replay. Existing historical completed requests are not backfilled with invented effects. ANONYMIZATION completion is rejected until approved retention/field policy exists; ACCESS/CORRECTION remain explicitly manual-response tracking, not implemented export or arbitrary field updates. Added Core availableActions and exact-customer filtered privacy reads, a customer-detail privacy panel, validated BFF bodies and guarded customer-detail read generations.
Validation: all 1210 Core tests in 173 files passed standalone in 377.34 seconds (customer-onboarding-full-core.log). Focused privacy/CRM tests passed (61 plus corrected competing-winner assertion rerun); 389 Web tests and 68 contracts passed; types/lint/format/naming/architecture/readiness and Core dry-run build passed. Six managed browser journeys passed with zero retries in 2.7 minutes (customer-closure-browser.log), including desktop/mobile identity review, approval, processing, lost-response closure completion, actual sign-out and visible closure audit. Mobile layout inspected. Managed tests built Web and used only the previously identified disposable database. No schema or retained database change, live provider acceptance or whole-phase acceptance.
Next: preferred-language profile editing and author-attributed append-only support notes with a retained-safe forward migration; invitation notification delivery; location/cycle setup; remaining catalog, warehouse, purchasing, preparation/delivery, membership-removal and final journeys. Notification muting policy is still unanswered; leave that dependent behavior pending and continue independent implementation. All sessions from this checkpoint are complete/stopped.
2026-09-08 customer preferences verified after 0f0948e: owner answered that transaction updates always send and promotions are optional. Canonical decision/domain/API records updated. Migration 0079 adds only nullable preferred_language and promotional_emails (0/1, no recorded opt-in by default), preserving old columns/rows. Authenticated profile read provisions through the existing guarded resolver; profile updates never provision on rejection and atomically require active identity/version, preference write, sanitized audit and frozen replay receipt. The account preferences page retains exact unknown requests and supports language edits and promotional opt-in/withdrawal. No transaction mute setting exists. Corrected stale Product Scope text to reflect implemented closure controls while retaining the anonymization policy gate.
Validation: 70 focused Worker/D1 tests including retained-0068 forward upgrade passed; populated-0078 upgrade verifier preserves every retained column/row and tests new bounds. Migration, types, lint, conventions, architecture, readiness, formatter, 26 harness tests, 68 contract tests and 389 Web tests passed. Two managed desktop/mobile browser journeys passed with zero retries (customer-preferences-browser.log), including lost-response exact replay, persisted edits and opt-out; Web production build and Core dry-run build passed. First Worker run exposed an overly broad test count (other test receipts); narrowed to the tested request/customer. Initial migration verifier assumed no future columns after 0078; split its old-migration check and added full retained-column preservation for 0079. No retained database was migrated/reset, no live messages/provider calls, no whole-phase acceptance. Next: Global profile editing and append-only staff support notes, invitation delivery, then remaining setup and commerce phases. Notification policy no longer blocked.
2026-09-08 Global profile/support slice verified after 12465ef: Global customer readers get profile and bounded exact-customer support-note pages; managers can reasonedly change approved preferences with expected Customer version and append notes. Current Global authority, all required writes/audits and frozen replay results share the complete transaction. Notes append independently without advancing Customer version, retain stable staff identity plus creation-time display name, and have immutable update/delete protection in migration 0080. The customer workspace shares its existing unknown-request controller across access/privacy/profile/notes, prevents paging from silently changing a draft's expected version, and displays loading/errors/empty/history states. Browser testing reproduced a valid long-email customer search crashing on D1 LIKE limits; literal case-insensitive instr matching fixes it and treats wildcard characters literally.
Validation: 83 customer Worker tests passed after the search fix; retained upgrade Worker test also passed in the preceding 82-test run. Coverage includes Global/local/customer denial, ignored writes/audits/receipts, lost authority before commit, same/distinct-note races, immutable author evidence, wrong foreign key, bounded inputs, customer-vs-staff profile race and search regression. Migration verification, types/lint/format/naming/architecture/readiness, 68 contracts and 389 Web tests passed. The combined browser run passed eight existing customer journeys and exposed the two new search failures; after reproducing/fixing the defect both new desktop/mobile support journeys passed with zero retries (customer-support-browser-final.log). These verify profile visibility to the customer, lost-response exact retries for profile/notes, paginated notes and mobile layout; screenshot inspected. Web build and Core dry-run passed. No retained database was migrated/reset and no live email was sent. Phase 2 remains incomplete.
Next: invitation delivery through durable notification intent, first correcting ambiguous email-error retry and atomic send-attempt evidence. Email skill read; read-only wrangler email sending list confirms an enabled domain in the account, and existing EMAIL binding remains configured. Actual FreshMarkets sender/domain acceptance is still a deployment/provider check. No email implementation edits yet. Continue remaining location/cycle, catalog/media, warehouse, purchasing, delivery, membership removal and final acceptance work.
2026-09-08 notification recovery in progress after 135ffab: two fresh regressions proved unknown provider errors were treated as retryable and ignored attempt inserts still allowed send. The internal EmailSendOutcome now distinguishes NOT_SENT/UNKNOWN, uses a closed documented pre-send rejection vocabulary, sanitizes errors and bounds Cloudflare send to 30 seconds. Required lease plus attempt commit before provider contact; final attempt/outbox projections commit together. Unknown outcomes and expired in-flight attempts cannot resend; a late known result can complete the same attempt. Direct sends honor scheduled time and five-send bounds, including a fifth-attempt crash. Two scheduled-job test fakes now explicitly identify NOT_SENT. No new schema, Web or public RPC change in this working slice.
Validation: 19 notification Worker tests pass, including four competing consumers, ignored attempt/final evidence, actual adapter network ambiguity, timeout, late success, and fifth-attempt recovery. Types/lint/format/naming/architecture/readiness pass. Full standalone Core suite is currently running session 74198 with runtime/test source fixed; log customer-notification-full-core.log. Do not claim its result yet. Canonical API/state docs reflect acceptance vs actual inbox delivery and unknown outcomes. Remaining Queue publication/DLQ operational acceptance is explicit.
Independent next-step preparation: draft-invitation-outbox-migration.mjs and generated 0081_invitation_notifications.sql are ONLY in the external artifact directory C:/Users/reggi/.codex/visualizations/2026/09/07/01a07c32-22c4-78a3-b481-1267eb6aca38/. They have not been added to repository migrations. Draft rebuilds outbox/attempt together, permits exactly one Customer/staff-invitation/customer-invitation recipient relationship, adds invitation uniqueness, recreates existing indexes/triggers, preserves all 137 retained tables' prior columns/rowids and an explicit completed send attempt in a temporary in-memory SQLite fixture. Negative ownership/FK/uniqueness checks pass. Real Worker/D1 upgrade and invitation command/UI integration remain next. No retained database was migrated/reset and no live email was sent. Read-only wrangler email sending list and current official Cloudflare Workers Email API were checked; existing binding/account sending-domain setup does not prove FreshMarkets sender or delivered-message acceptance.
2026-09-08 notification recovery verified: full standalone Core suite passed all 1243 tests in 173 files in 343.83 seconds (customer-notification-full-core.log); Core dry-run build also passed (notification-send-core-build.log). Earlier 19 focused notification tests and static/type gates passed. No browser/provider acceptance is claimed for this internal send-recovery change. Session 74198 completed. Continue by implementing the drafted retained-safe invitation outbox migration and full invitation intent/status/link flow; draft remains external until that step begins.
2026-09-08 invitation email implementation after 8d75af1: migration 0081 adds exactly one Customer/Staff-invitation/Customer-invitation outbox relationship without fabricated Customer records and preserves old outbox/attempt evidence. Staff/customer creation now requires durable email intent in the guarded transaction; revocation atomically cancels still-pending mail. Delivery checks invitation state/expiry/recipient at the send claim. Configured-origin links lead to verified-identity acceptance, never bearer authorization. Current invitation list emailStatus distinguishes queued/submitting/provider-accepted/failed/unknown/canceled/older-not-requested without changing frozen command receipts. Transaction updates remain mandatory; promotions optional, per owner answer.
Validation: 58 focused notification/initial-admin/upgrade Worker tests passed (invitation-notifications-worker-final.log). Additional explicit old outbox and completed-attempt column/rowid preservation passed in Worker upgrade (invitation-upgrade-worker.log). migration:check, Web 389 tests, types/lint/format/naming/architecture/readiness and diff checks pass. Managed desktop/mobile invitation browser run is still running session 35409, invitation-email-browser.log; do not claim its result yet. Uses only named disposable e2e-commerce-alignment-20260907. No retained DB migration/reset or live email send. Full Core rerun remains pending for this slice. Next: finish browser and affected/Core validation, commit/push this invitation slice, then location command required-effect guards and remaining setup hours/closures/cycle administration. No whole phase accepted.2026-09-08 invitation UI verified: all four managed desktop/mobile customer acceptance and staff revocation journeys passed with zero retries in 2.3 minutes (invitation-email-browser.log). They assert queued/canceled email status and retained-request retry through real Web/Core commands; mobile staff screenshot inspected. Web 389 tests, contracts 68, both builds, vinext compatibility, types/lint and static checks pass. Full standalone Core suite running session 28539 (invitation-full-core.log); runtime/test sources remain fixed. No live provider/inbox acceptance. Next location inspection found missing required-effect guards and pickup-profile non-atomic authority/mutable replay; these are source findings pending reproduced tests, not completed fixes.2026-09-08 invitation delivery slice verified: full standalone Core 1261 tests / 174 files passed in 325.25 seconds (invitation-full-core.log). Migration 0081 preserves retained outbox/attempt records while adding typed invitation recipients. Creation and pending-email revocation effects are atomic; expiry/verified-acceptance suppression, duplicate Queue delivery, ignored required effects and recovery are covered. Four desktop/mobile browser journeys, 389 Web tests, 68 contracts, migration checks, types/lint/format/naming/architecture/readiness, both builds and vinext compatibility passed. Provider submission is distinguished from inbox delivery; actual FreshMarkets sender/inbox acceptance and broader Queue operational recovery remain open. No retained DB was migrated/reset and no live mail sent. Phase 2 remains incomplete; continue location setup guards, pickup profile, hours/closures and cycles, then later authorized commerce phases.2026-09-08 location required-effect slice in progress after 8b9230b: five new Worker regressions reproduced successful location commands despite suppressed claim/capability/audit/receipt/removal effects. Required-effect guards now protect the complete location transaction, including geography revision and all unstarted-Quote invalidation; started Payments remain excluded. All 123 focused location/serviceability/geography/global-configuration Worker tests passed (location-guards-worker-final.log). Types/lint/format/naming/architecture/readiness and Core dry-run build pass. Managed location browser session 68716 still running (location-guards-browser.log); no whole phase accepted. No schema/RPC changes or retained DB migration/reset. Next pickup-profile command currently claims idempotency before validation, lacks current-authority batch guard, and returns mutable read-back on replay; its Web retry rebuilds edited payload. Those remain unfinished next work.2026-09-08 location guard slice verified: 123 focused Worker tests passed, including five reproduced pre-fix failures, geography/quote-invalidation suppression, current-authority/version races and same-command recovery. Both managed desktop/mobile location creation/activation journeys passed with zero retries (location-guards-browser.log, 2.0 minutes). Types/lint/format/naming/architecture/readiness and both builds pass. No schema/RPC changes, retained DB migration/reset, or live provider operations. The preceding full Core 1261-test pass covers 8b9230b; this location slice has the focused 123-test evidence, not a fresh full-suite claim. Phase 2 remains incomplete; pickup-profile atomic authority/frozen replay/Web recovery and remaining operating schedules/cycles are next.2026-09-08 pickup-profile slice in progress after 85f1e0e: new real Core tests reproduced mutable replay and success without required audit. Command now validates current Staff delivery.manage/scope, observed location coordinate/name, profile expected version, claim/profile/audit/frozen result in one transaction. New failures leave no claim; older same-intent unfinished records can recover and identity-only historical successes retain explicit compatibility. Shared view validator replaces Web response assertions. Web freezes payload/key and editing after unknown save, with explicit retry and retained request across scope selection. No new schema or RPC method.
Validation: 14 focused pickup/delivery-operations Worker tests pass, including suppressed claim/profile/audit/receipt, scope revoked before batch, competing updates, replay after later changes, and previous delivery operations. Existing injected-auth fixtures now include real matching IAM rows for transaction claims. Types/lint/format/naming/architecture/readiness, Core dry-run build and all 389 Web tests pass. Managed browser session 24515 is still running pickup-profile-browser.log; do not claim result. Runtime/test sources fixed during browser. Next: inspect browser outcome, add any missing focused coordinate/scope/recovery evidence, review and save pickup slice; continue location hours/closures and cycle administration, then later authorized phases. No retained DB migration/reset, live provider operation or phase acceptance.2026-09-08 pickup profile slice verified: all 15 focused Core pickup/delivery-operations tests pass, including original snapshot replay, ignored claim/profile/audit/receipt effects, current-scope revocation, competing writes and historical unfinished-claim recovery. Both desktop/mobile lost-response browser journeys passed (pickup-profile-browser.log); the additional mobile scope-switch test initially failed because its panel was collapsed after switching back, then passed after correcting navigation (pickup-profile-scope-browser-final.log). Application payload and key remain identical through scope change and retry. Web 389 tests, types/lint/format/naming/architecture/readiness and both builds passed. Shared validator added; no migration or RPC method change. No retained database mutation or live provider operation. Full-suite phase gate remains pending; preceding full Core 1261 pass belongs to 8b9230b. Next: versioned operating hours/closures and complete Scheduled cycle/window setup and checkout consumption, then all later authorized commerce phases. No whole phase accepted.2026-09-08 serviceability required-effect slice in progress after 3e4e769: new Worker regressions reproduced publication success without required claim/location-link/geography/audit/receipt effects and uncontrolled FK errors for missing parent inserts. Publication now requires every insert and verifies full old-link/zone/area retirement and unstarted-Quote invalidation inside the transaction. All 124 focused serviceability/location/geography tests pass (serviceability-effects-worker-final.log), including three suppressed retirement cases with unchanged old boundary and retry recovery. New scenarios run after existing baseline-count scenarios so those assertions remain unchanged. Types/lint/format/naming/architecture/readiness and Core dry-run build pass. Browser session 39986 running serviceability-effects-browser.log; runtime/test source fixed. No schema/RPC change or retained DB migration/reset. Next finish browser, save slice, then implement operating hours/closures and Scheduled cycle/window administration rather than claiming Phase 2 accepted.2026-09-08 serviceability slice verified: all 124 focused Worker tests passed and both managed desktop/mobile service-area publication/preview journeys passed with zero retries in 2.1 minutes (serviceability-effects-browser.log). Required publication and retirement effects now reject atomically; original-key recovery preserves complete boundary history and started Payment terms. Types/lint/format/naming/architecture/readiness and both builds pass. No schema/RPC changes or retained DB migration/reset. Next: complete versioned operating schedules/closures and Scheduled cycle/window administration. Current source still constructs a 24-hour customer window from delivery_date and lacks configured cycle opening; source findings are not accepted behavior. Full phase gate and later catalog/media, transfers, procurement, delivery, membership removal and final provider acceptance remain open.

2026-09-08 cycle/window implementation in progress after d8f7b68: migration 0082 adds explicit cycle plans/windows and immutable committed window evidence without backfilling retained facts. Global draft save/schedule RPCs, paged destination selection and Web recovery are implemented; opening/cutoff now require atomic audit/receipt. Checkout uses configured windows/planned pickup and payment guards, with customer Order rendering. Sixty-three focused Worker tests passed (cycle-workflows-worker-final.log); the earlier failed run exposed shared fixture selection/clock contamination, corrected without weakening assertions. Migration checks/types/lint/naming/architecture/readiness pass. Managed browser session 42086 is running against e2e-commerce-alignment-20260907 (cycle-browser.log), source fixed. New multi-window option regression, broader Worker/Web/contract gates, explicit upgrade assertions, final readiness alignment and browser result remain pending. No whole phase accepted; no retained DB migrated/reset. Continue operating hours/closures and all later authorized phases after this slice.

2026-09-08 cycle/window slice verified after d8f7b68: migration 0082 adds explicit cycle schedule/windows and immutable Order window evidence, with composite foreign keys preventing cross-cycle attachment and no retained-data backfill. Global draft/save/schedule/unpaid-cancel commands and paged Web setup are reachable; every required effect, current authority, timing/destination/version guard, audit and frozen receipt is atomic. Scheduled options/Quotes/Payments consume configured windows and pickup; commitment snapshots the accepted window without stock effects. Clock opening/cutoff is bounded and audited. Unpaid cancellation blocks Orders, non-definitive Payments and retained holds, invalidates unstarted Quotes/attempts, and recovers identical requests after lost responses.
Validation: full standalone Core suite passed 1302 tests/176 files (cycle-full-core.log, before final cancellation/composite-FK additions). Final cancellation/payment/upgrade focused run passed 43 tests/4 files (cycle-cancellation-worker.log); the subsequent cross-cycle upgrade regression passed (cycle-window-integrity-worker.log). Earlier focused 63 workflow, 13 readiness/upgrade and 16 administration/race tests passed. Fresh/populated migration verification passed (cycle-migration-final.log). Final Web 389 tests/96 files, contracts 68 tests, types/lint/format/naming/architecture/readiness, Core dry-run build and managed Web build passed. Vinext scan: 15 supported, zero partial/issues. Desktop/mobile browser creation, scheduling and unpaid cancellation with identical-body/key lost-response recovery passed with zero retries, 2.5 minutes (cycle-cancellation-browser.log); mobile screenshot inspected. Earlier browser fixture CLI exit hang was resolved with WRANGLER_SEND_METRICS=false without changing timeouts/assertions.
No whole commerce phase accepted. No retained/shared DB was migrated/reset, no deployment or live provider operation performed; only the previously identified disposable e2e-commerce-alignment-20260907 state was rebuilt. Next: Global versioned operating hours/closures and time-aware readiness, remaining setup acceptance, then catalog/media, transfers, exact paid procurement/receiving/packing, delivery execution/recovery and final journeys. Paid cycle compensation and provider/account acceptance remain outside this unpaid-cancel slice. Transaction notifications always send; only promotions are optional.
2026-09-08 operating-hours slice in progress after pushed 3e37b9b: migration 0083, shared schedule contracts/validators, Global read/save commands and location-linked Web editor now exist. Core saves weekly hours and dated closures with current location/global authority, complete version/schedule/geography/quote-invalidation/audit/frozen-receipt guards. Instant routing checks current hours; Scheduled checks configured pickup; Quote and payment guards preserve applicable open-interval evidence. Missing hours fail closed and block Global readiness; test setup explicitly configures synthetic 24-hour fixtures, with no retained backfill. Canonical domain/API/data docs updated.
Evidence: first 25 policy/checkout and 11 administration/upgrade Worker tests passed; 67 routing/serviceability/payment/admin tests passed after new-test cleanup used real checkout abandonment to avoid contaminating prior global hold-count assertions. Latest 23 checkout tests pass including hours changing during courier quotation. Types/lint/format/naming/architecture/readiness/migrations, Web 389 tests and contracts 68 pass. Browser session 73058 running hours-browser.log against the identified disposable e2e-commerce-alignment-20260907 state; do not claim browser acceptance yet. Current source kept fixed during browser.
Unfinished: guard Instant routing across a nearer site's time-based opening while quotation is in flight (capture earliest operating boundary across geofence candidates and fence quote/payment evidence); add boundary regression; review browser output/screenshot; run final relevant Worker tests and full Core suite because eligibility/fixtures changed, builds, final diff and commit/push. Hours source remains uncommitted. Whole Phase 2 setup acceptance and all later commerce phases remain open. No retained DB migration/reset or live provider operation.

2026-09-08 operating schedule and commerce-control recovery verified after 3e37b9b. Migration 0083 adds explicit per-location weekly hours and dated closures without inferred retained configuration. Global read/save and location-linked Web editor enforce current authority/version and complete schedule/geography/invalidation/audit/frozen-receipt effects. Instant eligibility and payment initiation respect opening/closure boundaries, including the next opening of a nearer currently closed site; Scheduled evaluates planned pickup. New options retain the global mode even when every location is unavailable. Global pause/switch/reopen Web commands now freeze the original request through unknown response/scope changes, validate results and discard stale reads.
Validation: full Core suite passed 1326 tests/179 files in 473.97s (hours-full-core.log); focused tests cover suppression, replay, scope/version races, provider-quotation hour changes, closure invalidation, exact boundaries/DST, future Scheduled pickup and supported upgrades. Web 389 tests/96 files, contracts 68/19 and harness 26 passed. Types/lint/format/naming/architecture/readiness/migration checks and both builds pass; vinext reports 15 supported, zero partial/issues. Hours desktop/mobile browser journeys passed with zero retries (hours-browser.log, 2.5m); Global pause lost-response/scope recovery passed with zero retries (commerce-recovery-browser-final.log, 2.4m). Both mobile screenshots inspected. Initial commerce browser run failed during fixture CLI cleanup after successful SQL, before page assertions; fixture now uses the already-installed wrangler-e2e CLI and explicit E2E config, preserving zero-exit requirement and existing timeout/assertions. Production/build CLI unchanged.
No whole phase accepted, deployment, retained/shared database migration/reset or live provider operation. Next: Instant readiness/promise authoring (currently no command), removal of legacy concurrent-order/readiness gates from already-paid Instant commitment while preserving stock holds/reservations, and connected second-site/staff/customer setup acceptance. Then all later authorized commerce phases remain. Transaction notifications always send; promotions alone are optional.


# Archived execution detail before owner-requested checkpoint consolidation, 2026-09-09

Historical evidence only. All status and next-action instructions in the following snapshot are superseded by COMMERCE_ALIGNMENT_EXECUTION.md. Do not load this section during ordinary continuation.

# Commerce alignment execution

Updated: 2026-09-09 06:16 UTC / Asia/Manila.
Status: GD-1 completed and pushed as `83854bd`. CA-4.2 completed and pushed as `406a405`. CA-4.3 is implemented/locally verified and pushed as `010b8af2`. CA-5.1 is committed/pushed as `e6afa560`; CA-5.2 is committed/pushed as `2f11e609`; CA-5.3 is committed/pushed as `61d1299b`; CA-5.4 is committed/pushed as `c0e7ed7c`; CA-5.5 is committed/pushed as `bad66079`; CA-5.6 is committed/pushed as `49d75d3a`; CA-5.7 is committed/pushed as `75e2105b`; CA-5.8 is committed/pushed as `19f409a1`; CA-5.9 full Scheduled customer journey is active. All nine product choices are settled. Earlier acceptance, newly mapped product-change follow-ups and provider/release gaps remain open.

## Scope and resume source

Complete all remaining workflows in [COMMERCE_ALIGNMENT_E2E_PLAN.md](../../product/COMMERCE_ALIGNMENT_E2E_PLAN.md), in dependency order. Follow [continuation plan](../COMMERCE_ALIGNMENT_CONTINUATION_PLAN.md) and [AGENTS.md](../../../AGENTS.md). Current owner instructions and canonical policy prevail over historical records. Phase 3 local acceptance does not establish release or real provider acceptance.

Previous checkpoint content is preserved byte-for-byte in [execution history](COMMERCE_ALIGNMENT_EXECUTION_HISTORY_20260909.md). Its old pauses and next actions are superseded by this current-state record. Read targeted historical sections only when needed.

## Observed workspace and preservation

- Branch: `main`. Last confirmed pushed HEAD: `19f409a1`, `fix(procurement): resolve supplier issues after cancellation`; push confirmed `75e2105b..19f409a1`. CA-4.2/CA-4.3/CA-5.1/CA-5.2 are committed. CA-5.9 is active; see its section. Initial resume HEAD was `0b55737`; its partial media work was preserved and completed.
- Protected `.codex/config.toml` is now locally deleted; preserve that owner-side change outside commits. Untracked `docs/product/SIMPLIFICATION_DISCUSSION.md` remains byte-for-byte intact and outside commits. The owner now explicitly identifies it as the record of agreed product decisions; do not infer missing technical policy or business rules from it. Additional pre-existing deletions under `.claude` and `.superpowers/sdd` are outside this work.
- Additional local deletions under `docs/superpowers/plans`, `prompts`, `reports` and `specs` appeared after the GD-1 push. They were not performed or staged by this task; preserve them outside the implementation commit. Active guidance and the saved application draft remain present.
- `docs/product/IMPLEMENTATION_STATUS.md`: pre-existing invalid UTF-8 elsewhere; append bytes only if needed.
- Only disposable database state: `apps/core/.wrangler/e2e-commerce-alignment-20260907`. No retained/shared/remote database was reset or upgraded. Migration 0086 was applied only to isolated test databases and the designated disposable browser state.
- Current process state is recorded in the active slice. Recheck processes before testing. Never overlap Core suites and managed browser stacks; keep source fixed during browser acceptance.
- No actual PayMongo/Lalamove/OAuth/email acceptance, real provider action or deployment was performed. No subagents are authorized.

## Completed steering slice: GD-1 Guidance consolidation

Owner authorization: rebuild the guidance structure without mass-deleting Markdown, preserve the agreed discussion and current implementation, extract valid requirements before archiving superseded sources, update authority routing/references, verify no decision/technical requirement/task was lost, then resume commerce automatically. This changes guidance, not application acceptance or provider/deployment authority.

Acceptance: a small routed active set; source-to-destination requirement accounting and archived originals; protected-file and application-diff preservation; all current plan criteria and CA task IDs retained; resolved active links and convention checks. No application behavior is certified by this documentation work.

Completed as `83854bd` and pushed to origin main; all nine decisions are settled and must not be asked again. The audit's approved-change table tracks additional application gaps against the new product target without invalidating historical evidence or silently treating the changes as implemented. The active next action is under CA-5.9 below.

Preservation artifacts: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/guidance-rebuild` contains initial dirty-file hashes and byte copies/hashes of the existing Markdown. The first inventory attempt failed on an unrelated broken worktree dependency symlink; the corrected `rg --files` inventory succeeded without traversing it. No application files were changed for guidance.

Owner correction, 2026-09-09: SIMPLIFICATION_DISCUSSION is the product authority; conflicting guidance must be outdated/archived and feature expansion beyond that record requires discussion. All 21 explicitly Agreed sections are mapped; the record's explicitly deferred/proposed details remain distinguished unless the owner clarifies otherwise. Existing code and unfinished safety evidence are preserved, but older plans no longer independently authorize additional product surfaces. The owner answered the concrete scope question: **Include the transfer controls**. Damage/missing observations and Global loss/inspected-return controls are explicitly approved as a supplement to the discussion. No additional feature scope is inferred.

GD-1 progress: five active guides and three focused technical references aligned, 27 original documents archived, and all nine follow-up decisions applied. The earlier archive README collision was repaired by restoring its bytes and placing the index in INDEX.md. Final working-tree checks confirm all original bytes. The earlier extra README EOF blank was corrected. `pnpm naming:check`, `pnpm harness:test` (26/26) and `git diff --check` passed. No application code changed for guidance.

Owner handoff correction: after the plain-language examples, the owner answered “yes i agree to all this” and requested a pause to switch to Astra Medium before proceeding. Both pauses are now resolved by the owner's “Continue from the checkpoint.” Leave repository settings untouched. Complete GD-1 and resume implementation under the existing authorization without another routine approval request. No new application changes, application tests, commits or push followed the discussion pause.

Approved owner supplement, 2026-09-09: all nine choices and the later plain-language examples are settled. These decisions supersede the corresponding proposed/deferred labels in the protected discussion and draft guidance; preserve the protected record itself unchanged.

- **1 approved:** one administrator Problems list linked to Orders, New / Being handled / Resolved, contact details and a short resolution note. Refund approval remains separate; daily manual review, no chat, automatic refund or new deadline.
- **2 approved:** one Admin view for a delivery week gathers configured ordering dates/cutoff, offered products, paid Orders, total quantities to buy and receiving/preparation progress. Example: the delivery-week view shows 12 kg carrots, 8 kg potatoes and 20 broccoli pieces to buy. Staff contacts the supplier manually. This organizes the agreed work; it does not add a supplier portal, messaging or approval process.
- **3 approved:** a linked receiving form records sent versus actually received quantities and actual local size counts where relevant. Example: retain 20 kg sent and 18 kg received, then record the actual Small/Medium/Large counts. Never guess those counts from weight or credit the same goods again through Add stock. Initial Scheduled goods arrive directly from the supplier at the first location and remain allocated to that week's paid Orders. CA-4.2 transfer controls remain independently approved.
- **4 approved:** prevent overlapping active product sales for the same selling option at the same location; stop or edit the existing sale first.
- **5 approved:** the quantity offered at a sale price is separate from stock. With 10 kg in stock and 5 kg offered on sale, selling 1 kg at the sale price leaves 9 kg stock and 4 kg on sale. An eligible cancellation that releases that purchase's stock restores its consumed sale quantity. Other purchases/stock removals cap the remaining sale quantity to remaining goods: if only 2 kg stock remains, at most 2 kg remains on sale. Adding fresh stock does not automatically increase the old sale quantity. Refund alone is not evidence of a stock release. Preserve the previously approved full-requested-item quantity rule and do not introduce stock-lot tracking.
- **6 approved:** selected-product sales support percentage and fixed monetary discounts per selling unit. Example: 20% off a PHP 60 pack gives PHP 48; PHP 10 off each PHP 60 pack gives PHP 50 per pack and PHP 100 for two packs. Do not restore the withdrawn percentage-only restriction or apply the fixed amount just once to an arbitrary multi-unit line.
- **7 approved:** finish the last planned Scheduled week, pause ordering, receive/count Instant stock, check product/price/location readiness, switch to Instant and reopen. This is the normal operating sequence, not a new condition that every historical Order must be finished. Outstanding Scheduled goods remain separately allocated.
- **8 explicit replacement:** calculate total grams as quantity times the recorded grams for each selling option, including piece/pack options and all paid additions. Keep the agreed 20,000 g order limit. Do not require a separate packaging allowance, size/fit inputs or weight-variation settings. This supersedes those requirements in the protected discussion and older guidance without editing the protected record. Shipping grams do not create a weight-to-count inventory conversion.
- **9 approved and expanded:** Orders by paid/delivered/canceled event date; provider-confirmed money received/refunded separately by confirmation date; paid product/selling-size quantities with cancellations separate; committed discounts/promotion usage; accepted delivery charges versus recorded delivery costs for the same Orders, unknown costs unavailable. New customers means first purchase in the selected period; unique purchasing customers means distinct people who purchased in that period; repeat customers/orders means returning purchasers and the orders they placed. The owner confirmed that recurring orders means these repeat purchases, not automatic recurring Orders or billing. Remove the prior five-report-only restriction. Verify each report against its owning backend records; approval does not establish current data coverage.

These decisions are now reconciled in Product/Design, focused references and the active plan, including removal of obsolete packaging/fit inputs, five-report-only language and settled-question labels. They remain application acceptance obligations, not completed features. The protected discussion is unchanged. Four major commerce phase blocks (4-7), earlier CA-0-2 acceptance and the audit's newly mapped product-change follow-ups remain open. Unprovided actual provider/account activation, operational contact/timing/geofence values and accounting/retention facts remain separate external inputs; they do not reopen the nine product choices.

Model-handoff verification, working tree at `0245ce0b34f7785d65a2c97b97e22118517553c8`: `git diff --check -- docs/operations/checkpoints/COMMERCE_ALIGNMENT_EXECUTION.md docs/operations/COMMERCE_ALIGNMENT_CONTINUATION_PLAN.md` and `pnpm naming:check` passed. `Get-Content -Encoding UTF8 -Raw application-before-guidance-sha256.json | ConvertFrom-Json -AsHashtable` followed by `Get-FileHash -Algorithm SHA256` for every keyed application path matched all **16** saved draft hashes. Protected discussion and its saved `before-documents/docs/product/SIMPLIFICATION_DISCUSSION.md` copy matched (`0021CDF5935E3B90D2C23EAA5EBA56DA7419EE7CD2E8FA479990E03BC64B792F`). Both handoff documents decoded with strict UTF-8; stale pending-answer labels were checked and removed. Only the checkpoint and continuation record changed for this handoff. No application tests, commit or push; full GD-1 verification remains pending.

Verified guidance inventory: AGENTS/CLAUDE/README/TRUNK routing, new ENGINEERING/Product/Design guides, revised Architecture/API/State/Data references, continuation/audit and archived originals. The audit now combines the initial 84 changed-block dispositions with 113 final source-section mappings for Domain and current design sources. Missing explicit cart-creation concurrency and metric-definition details were retained in API/Data. The active plan retains A-I, audit defects, eight phase rows and five journeys; the 21 Product decision IDs and prior CA IDs remain. App code matches the preserved CA-4.2 draft; config/other local deletions, protected discussion and encoding-damaged status remain outside commits.

GD-1 verification command: `python C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/guidance-rebuild/verify-guidance.py --staged`. Passed: 27 working-tree and staged archive hashes, 16 application hashes, protected discussion/status bytes, config deletion, 33 active Markdown documents, 148 local links/anchors, 113 source-section hashes/destinations, 21 decision rows, eight phases, five journeys, old CA IDs and root/workspace command names. Result artifact: `guidance-rebuild/verification-result.json`. `pnpm naming:check`, `pnpm harness:test` (26/26), `git diff --check` and `git diff --cached --check` passed. Reviewed 55 intended staged guidance/archive files; application code and protected/unrelated changes are outside the index. An initial command scan mistook prose for commands; corrected syntax/workspace checking passed. Archive-local `.gitattributes` preserves original CRLF bytes in Git without changing application or personal settings.

## Completed implementation slice: CA-4.2 Warehouse discrepancies and dispositions

Owning phase: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 4 — Warehouse distribution**, section D. CA-4.1 is implemented/locally verified and committed/pushed `0245ce0`. CA-4.2 owns destination checked damage/shortage, Global loss and verified sellable-return resolution, separate physical/reserved/held/transit/non-sellable reporting and final Phase 4 acceptance. Earlier CA-0–2 acceptance remains open.

Observable acceptance: a partial receipt credits only checked accepted quantities; damaged and missing quantities remain outstanding and separately visible. Global records a reasoned loss or verifies a physically received, inspected sellable return before crediting warehouse stock exactly once. Destination staff cannot resolve Global dispositions. Full accounting closes transit into `RECEIVED` only when all goods were accepted, or a distinct `RESOLVED` state when losses/returns account for any goods. Original retries, authority/state/version races, all required ledger/audit/receipt effects and concurrent acceptance/resolution must be safe. Demonstrate the normal Web path and derived Global reporting; no new authoritative total balance, price changes, operational SQL or provider actions.

Completed and pushed as `406a405`; commit/push hooks passed. Canonical API/State/Data/Design already describe this target. CA-4.2 does not implement actual sorted-size stock; the active CA-4.3 follow-up below retains that obligation.

Resume checks on `83854bd` plus the preserved CA-4.2 application draft: no matching task Worker/test process was running. The only resettable browser state resolves to `E:/GithubProjects/freshmarkets/apps/core/.wrangler/e2e-commerce-alignment-20260907`; the managed setup checks its direct-child boundary. Installed Wrangler reports 4.127.1. `pnpm format:check` initially flagged four app READMEs after guidance staging; explicit `pnpm exec oxfmt apps/core/README.md apps/core/migrations/README.md apps/core/seeds/fixtures/README.md apps/web/README.md` corrected working-tree formatting/line endings with no Git content diff. `pnpm format:check` then passed. No application file was changed by that formatting operation.

Resumed managed browser command: `E2E_START_STACK=1`, `E2E_STATE_NAME=e2e-commerce-alignment-20260907`, `pnpm --filter @freshmarkets/web exec playwright test tests/inventory-transfers.spec.ts --retries=0`. Startup succeeded. Result **1 passed / 1 failed, 2.1 minutes**: desktop 1280 passed; mobile 390 reached final distribution but a strict text locator matched both test products before search filtering settled. The test now waits for exactly one filtered article and checks its product name before asserting 98,000 total / 43,000 central / 55,000 site quantities. No application behavior was changed for this failure. Session 20975 exited; no matching task process remains. Log: `commerce-transfer-dispositions-browser-resume.log` in the evidence root. Rerun both sizes before claiming browser acceptance.

Latest browser acceptance on `83854bd` plus the complete CA-4.2 working tree: `E2E_START_STACK=1`, `E2E_STATE_NAME=e2e-commerce-alignment-20260907`, `pnpm --filter @freshmarkets/web exec playwright test tests/inventory-transfers.spec.ts --retries=0`: **2 passed, 2.0 minutes**, desktop 1280/mobile 390, after the filtered-product wait correction. Log `commerce-transfer-dispositions-browser-final.log`; session 80786 is closed and no matching task processes remain. The final session exit response was unavailable after context recovery; the saved Playwright result records both passing tests. Six discrepancy/resolution/distribution screenshots were inspected and copied to evidence directory `commerce-transfer-dispositions/`; mobile uses a horizontally scrollable list table without page overflow. Real local Web/Core/D1 actions cover loss, inspected return, terminal accounting and exact derived stock totals. No provider acceptance. Source remained fixed throughout the browser run; no Core suite overlapped.

`pnpm --filter @freshmarkets/web check:vinext` passed: **15 supported/0 issues**. Aggregate `pnpm check` **passed**, session 91540 exit 0: Core **1503 tests/191 files**, Web **403/101**, contracts **68/19**, other shared suites, harness **26**, clean/retained migration checks, conventions, architecture/readiness, lint, types and both builds. Log `commerce-transfer-dispositions-check.log`. The complete 16-file application diff was reviewed and staged explicitly; `git diff --cached --check` passed. No application edits followed aggregate startup; only checkpoint/continuation prose changed. Protected discussion SHA-256 still matches the saved original. These checks use the application working tree based on `83854bd`; only checkpoint/continuation prose changes follow aggregate startup.

Preserved CA-4.2 draft at its start, subsequently committed as `406a405`: migration 0087 expanded with `RESOLVED`, cumulative loss/return and current damaged/missing counters, immutable check/resolution evidence and a pool query index; transfer contracts/validation, Core repository/commands/read model and two RPC methods; Web resolution/distribution adapters and receipt/detail/report UI; extended browser and Worker tests; canonical State/Data and checkpoint. `receiveInventoryTransfer` can record zero-credit observations or accept recovered goods while updating remaining classifications. Loss only consumes transit; its immutable resolution is the transit loss evidence, not a second physical deduction. Verified returns require explicit physical/sellable inspection and credit the original warehouse. Global totals are derived per pool.

`pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/inventory/application/inventory-transfers.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: **38 passed/2 files** before the final Web changes (35 transfer tests). Covers damaged/missing observations, no-credit checking, recovered goods, loss/return conservation, final `RESOLVED`, replay, scope/inspection/version/category rejection, silently skipped resolution/check effects and concurrent full acceptance versus return. `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm migration:check` passed; rerun formatter after the final status-filter addition. At that earlier checkpoint there was no CA-4.2 browser or aggregate acceptance; the latest browser result above supersedes that gap. The earlier managed browser command (`E2E_START_STACK=1`, `E2E_STATE_NAME=e2e-commerce-alignment-20260907`, `pnpm --filter @freshmarkets/web exec playwright test tests/inventory-transfers.spec.ts --retries=0`) failed during startup before tests; log `commerce-transfer-dispositions-browser.log`. A standalone `pnpm --filter @freshmarkets/core exec wrangler d1 migrations apply DB --local --persist-to .wrangler/e2e-commerce-alignment-20260907` was interrupted at the tool boundary by the owner correction; its later log shows all migrations through 0087 applied. No captured exit status is available, so this is not a successful test claim. No matching task process remains on reconciliation. Log `commerce-transfer-dispositions-migration.log` in the evidence root. Core session 29214 exited.

Migration 0087 was applied only to isolated suites and the designated disposable browser state; no retained/shared deployment uses it. Its revision is within the authorized pre-launch policy and preserves the retained 0086 upgrade. Only `apps/core/.wrangler/e2e-commerce-alignment-20260907` may be recreated for the new browser run. Protected files remain untouched and outside commits.

## Completed implementation slice: CA-4.3 Actual counted-size stock

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 4 — Warehouse distribution**, GD-D17–18, with catalog/checkout prerequisites. Implemented and locally verified on the `406a405` working tree; committed/pushed as `010b8af2`, with naming/commit hooks and push confirmed. Scheduled allocation counting remains Phase 5.

Acceptance met: ordinary Product/variant authoring supports actual counted sizes and separate approximate shipping grams. Linked receiving preserves sent/accepted weight and actual counts without a second stock credit. Later counting consumes measured bulk once. Size availability, competing checkout holds, payment commitment, eligible cancellation and removal use actual counts. Rejected/racing/skipped effects roll back; identical retries return the original result. Approximate grams never manufacture pieces. Existing weight products retain their stock behavior.

Implementation scope: migration `0088_counted_stock.sql`; catalog schema, Product/SKU commands and Admin/public reads; checkout/options/quotation/revalidation and Order/amendment pool identity; procurement identity/read compatibility; inventory adjustments, transfer receipt/read models and distribution; new `stock-sorting-repository.ts` and `sort-inventory-stock.ts`; contracts/validation/RPC; Product/variant, receipt and Inventory Count sizes forms plus same-origin sort adapter; migration verifier and Worker/browser tests; canonical API/Data/State. Dedicated PIECE pools reuse existing balances, holds/reservations and ledgers. The shared sorting statement builder is needed by both linked receipt and later counting; no parallel stock ledger or workflow framework was added. Immutable sorting headers/outputs record measured bulk, actual counts and optional receipt identity. One guarded batch owns all effects. The product tracking choice and existing size stock identity cannot be reassigned through an update API.

Actual verification:

- Focused `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/inventory/application/counted-stock.integration.test.ts src/inventory/application/inventory-transfers.integration.test.ts src/checkout/application/instant-quote.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: **75 tests/4 files passed**, session 41826 exit 0. Fourteen new cases cover bulk/count conservation, full rollback when effects are skipped, competing counts/holds, linked receipt and missing goods, original replay/changed intent, revoked authority, inactive size and lost committed response. Log `counted-stock-focused.log`.
- `pnpm check`: **passed**, session 5063 exit 0: Core **1517 tests/192 files**, Web **403/101**, contracts **68/19**, shared suites, harness **26**, conventions, fresh/retained migration checks, architecture/readiness, lint, types and both builds. Includes the final assertion that two paid Small packs snapshot **2 PIECE / 600 shipping grams** and cancellation releases their reservation. Log `counted-stock-check.log`.
- `pnpm --filter @freshmarkets/web check:vinext`: **15 supported / 0 issues**.
- `E2E_START_STACK=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/counted-stock.spec.ts --retries=0`: **2 passed / 2.0m**, session 13984 exit 0. Desktop 1280/mobile 390 create Small/Large plus Medium through normal authoring, dispatch 20,000 g, receive/count 18,000 g as 20 Small/10 Large, recover/count the remaining 2,000 g as 2 Small/1 Large, retry a lost response with identical body/key, remove one Small, set exact price/activation and obtain a customer Quote for two Small. No operational SQL is used beyond the isolated authentication/IAM fixture. Log `counted-stock-browser-complete.log`.
- Application source remained fixed after aggregate startup. Only browser helper URL parsing and screenshot captures changed afterward; final browser ran those changes. No Core suite overlapped a browser stack. All processes exited; no matching task process remains. Migration 0088 was applied only to isolated test databases and the designated disposable browser state.
- Receipt, counted/bulk row and customer Quote screenshots were inspected at desktop/mobile and copied to `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/counted-stock/`. Stock actions fit mobile without page overflow. Checkout still visibly contains a membership trial banner and technical prose; these are existing **CA-6/CA-7** gaps, not accepted release UI. Local mock quotation/payment evidence is not actual provider acceptance.

Failures resolved: initial trigger inventory lacked deferred-FK migration transactions; corrected inventory passed (`counted-stock-triggers.json`). Initial typechecks found nonexistent CoreService/Dialog imports; explicit request type/existing Sheet fixed them. Migration verification originally compared 0088-added defaults as if they were old 0087 columns; the upgrade now preserves every old column/row and verifies SHARED/null defaults and empty new evidence. Initial 9 test failures omitted Product scope; fixed fixture and Count sizes lookup, then 47 and extended 75 tests passed. First browser attempt had an ambiguous bulk label (2 failures); exact label correction passed both cases (`counted-stock-browser-second.log`, session 88985). The first aggregate stopped at the browser helper Response type; narrowing to required methods fixed it. Final browser extension initially included `?created=1` in the Product ID (2 failures after successful stock actions); pathname extraction fixed it. Logs retain the failures without implying application acceptance from them. Startup migration was slow but completed; no harness/runtime configuration change was needed.

Protected Discussion SHA-256 remains `0021CDF5935E3B90D2C23EAA5EBA56DA7419EE7CD2E8FA479990E03BC64B792F`. Protected config deletion and unrelated deletions stay outside the commit. No provider action, deployment, retained/remote reset or outbound message occurred.

## Completed: CA-5.1 Delivery-week workspace and purchase confirmation

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 5 — Scheduled operations**, section G and GD-D14–18. Completed/pushed as `e6afa560`, `feat(procurement): add delivery-week purchase confirmation` (`010b8af2..e6afa560`). The named location/week view shows dates, exact paid quantities, permission-filtered Orders/preparation, current offers and receiving progress. Normal purchase confirmation supplies current IDs/versions/reviewed quantities internally and atomically records the manual purchase, requirement/receipt, audit and original replay result. Supplier contact remains manual. Existing purchase storage is reused; no migration in CA-5.1.

Verified complete CA-5.1 working tree based on `010b8af2`:
- Focused Core command: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/procurement/application/procurement-command-recovery.integration.test.ts src/admin/application/admin-operations-commands.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: **32 tests/3 files**, session 79131 exit 0. Covers reviewed-quantity/cutoff validation, scope/permission/staff/demand races, competing confirmations, omitted effects, lost committed D1 reply and original replay/changed-intent conflict. Log `scheduled-week-focused.log`.
- `pnpm check`: **passed**, session 17721 exit 0: **1533 Core/192 files, 403 Web/101, 68 contracts/19**, shared tests, harness 26, clean/retained migrations, conventions, architecture/readiness, lint, types and both builds. Log `scheduled-week-check.log`.
- `E2E_START_STACK=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-week.spec.ts --retries=0`: **2 passed/1.9m**, session 56331 exit 0. Desktop 1440/mobile 390 select a named week, confirm exact purchase, retry identical body/key after lost response, inspect paid Orders/current offers, start and record receiving, and see accepted goods in the week view. Log `scheduled-week-browser-second.log`. Synthetic committed-payment/cycle history is explicitly fixture setup; this is not provider or no-SQL customer journey acceptance.
- `pnpm --filter @freshmarkets/web check:vinext`: **15 supported/0 issues**. Screenshots inspected/copied under evidence root `scheduled-week/`; no mobile page overflow. Diff reviewed; protected Discussion hash unchanged. No Core/browser overlap, retained/remote reset, provider action, outbound message or deployment.

Resolved failures: initial parser used `z` instead of existing `validationSchema`; corrected before focused success. Web required the standard empty-state component and installed vinext navigation shim alias (full Web passed). Initial browser matched both labelled main and select; named combobox fixed both cases. Window fixture matched actual schema before browser. Aggregate first stopped on counted-stock browser formatting; formatter restored expected bytes with no Git content diff. These failure logs do not imply acceptance. Detailed receiving/counting, shortages, packing/surplus, destination purchase breakdown and full original/addition/cutoff journey remain Phase 5 follow-ups; earlier CA-0–2 and provider/release gaps remain open.
## Completed: CA-5.2 Scheduled receiving and supply discrepancies

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 5 — Scheduled operations**, section G. Acceptance: accepted goods remain cycle/destination allocated; rejected or short goods produce an explicit supply exception; staff can resolve replacement sourcing through reachable, scoped commands without silently replacing products or fabricating received goods. Complete receiving/replay/race failures must leave no partial allocation, evidence or success result. Financial resolution uses existing approved Order/refund authority, never a new policy. Actual local size counts, complete packing/inspected surplus and the original/addition/cutoff customer journey remain subsequent CA-5 obligations.

Reconciled starting evidence: `execute-receiving-command.ts` writes cycle goods but has no supply-exception insert; its accepted/rejected sum is bounded by the original requirement. `resolveAdminOperationalException` only supports fulfillment retry, not supplier shortage resolution. `consume-cycle-goods.ts` already owns packing allocation statements. CA-5.1 committed/pushed as `e6afa560` (`010b8af2..e6afa560`). CA-5.2 adds `0089_receiving_discrepancies.sql`: additive zero-default shortage/replacement counters preserve original expected/rejected quantities. The existing record-receipt command now accepts explicit missing quantities or inspected replacements, writes scoped supply exceptions and cycle goods, and preserves audit/result atomicity. Replacement acceptance cannot exceed the original paid requirement; original rejection/missing observations remain visible. The normal receiving form supplies Missing and Receive replacement actions. No supplier message or financial-policy change. Files completed in CA-5.2: migration 0089, `execute-receiving-command.ts`, `receiving-actions.ts`, `record-received-line.ts`, `list-procurement-queue.ts`, Admin operations reads/commands, Core index, contracts admin-operations, Web receiving page/record-line route, schema preservation verifier and checkpoint. First scripted replacements mishandled PowerShell nested arrays, corrupting two files and dropping intended type fields; typecheck caught it. Only those two task-owned files were restored from verified HEAD, and all intended edits reapplied with explicit patches. Final `pnpm typecheck` passed. Existing focused suite passed **49 tests/4 files**. Extended mixed receipt/replacement and omitted-effect tests initially failed only because the inventory assertion included volatile D1 metadata (55 passed); comparison now uses result rows. Rerun same four files passed **56 tests**, session 80221 exit 0, log `receiving-discrepancies-focused.log`. Exact command: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/procurement/application/receiving.integration.test.ts src/procurement/application/receiving-command-recovery.integration.test.ts src/procurement/application/procurement-command-recovery.integration.test.ts src/admin/application/admin-operations-commands.integration.test.ts`. `pnpm typecheck` passed. Initial migration verification compared added columns to an older snapshot; old columns are preserved and new zeros explicitly asserted. Browser command remains the CA-5.1 command above, now extended to receive 700 accepted/100 rejected/200 missing grams and then 300 inspected replacements with identical saved-receipt retry: **2 passed/1.9m**, session 62723 exit 0, log `receiving-discrepancies-browser.log`. Mobile screenshot inspected. Only designated disposable state received migration 0089. Final review added immutable receiving-event and original supply-observation guards plus tests; full `pnpm check` **passed**, session 14305 exit 0: **1540 Core tests/192 files, 403 Web/101, 68 contracts/19**, shared tests/harness 26, clean/retained migrations, all convention/architecture/readiness checks, lint/types and both builds. Log `receiving-discrepancies-check.log`. `pnpm --filter @freshmarkets/web check:vinext` passed **15 supported/0 issues**, log `receiving-discrepancies-vinext.log`. Final browser against all guards **passed 2 tests/1.9m**, session 64168 exit 0, log `receiving-discrepancies-browser-final.log`; Core exited first. Final screenshots copied to evidence root `receiving-discrepancies/`; same inspected desktop/mobile flow, no overflow. Application source fixed through aggregate and final browser; only checkpoint/docs updated afterward. Final diff reviewed and `git diff --check` passed; Discussion hash unchanged. Migration 0089 only reached isolated tests/designated disposable browser database. All suites/stacks exited. Those verified intended CA-5.2 files were subsequently committed/pushed as recorded below. No provider action was performed.

CA-5.2 is committed/pushed as `2f11e609`, `feat(procurement): record shortages and inspected replacements` (`e6afa560..2f11e609`). Keep new count representation and surplus safeguards explicitly tracked rather than claiming this slice completes Phase 5. The browser paid-demand fixture must be labelled explicitly: it cannot prove provider-origin payment commitment or the full no-SQL Scheduled journey. Earlier CA-0–2, approved product follow-ups and real provider/release obligations remain open.


## Active implementation slice: CA-5.9 Full Scheduled customer journey

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 5 — Scheduled operations**, section G and final Scheduled journey. Acceptance: start from the designated clean disposable stack and authenticated/IAM fixture, configure operational prerequisites through actual commands, then use real customer checkout and test-provider confirmation to create original paid Scheduled demand and a paid addition. Reach cutoff, purchase exact remaining paid quantities, receive cycle-allocated goods and pack the Order through ordinary Web/Core paths. Include accepted cancellation exclusion and preserve explicit test-provider versus actual-provider distinction. No operational SQL may fabricate Quote, payment, Order, demand, purchase, receiving or packing state. Fix only exposed authorized workflow gaps; preserve all earlier acceptance obligations.

Reconciled on pushed `19f409a1`: CA-5.1–5.8 have local operational acceptance, but their browser starting financial history is synthetic. `customer-launch-journey.spec.ts`, customer checkout/cancellation specs mostly mock API responses; they do not close this obligation. `counted-stock.spec.ts` already reaches real checkout Quote from normal Admin/location/address/cart commands and is useful read-only reference. Existing test-runtime mock provider emits a local `/development/mock-payments/:reference` URL, but no matching Web route remains; determine the proper test-only provider boundary and reachable local Core webhook without adding a production payment-success shortcut or starting a second unmanaged listener. Core public `/webhooks/payments/:provider` already verifies provider events. CA-5.9 now has an unverified browser journey draft, opt-in local provider ingress (tests/provider-gateway.ts and its Wrangler config/generated bindings), Playwright command wiring, and a Core E2E-only entrypoint that invokes the real scheduler at wall-clock time. Production entrypoints are unchanged; no operational SQL is introduced. The browser payment-host page alone is mocked; signed events go through Core verification. The addition form still requires an internal SKU; named choices remain an explicit acceptance gap. pnpm typecheck passed. An attempted prettier command failed because it is not installed; use the repository oxfmt. Initial managed browser command E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-customer-journey.spec.ts --retries=0 failed (session 5137 exit 1; scheduled-customer-journey-first.log and saved trace under scheduled-journey-first). Real checkout and verified mock event succeeded, but the test omitted the existing durable payment-reaction scheduler step before asserting a committed Order. Added that real scheduler invocation; no production behavior changed. Core/browser processes exited before editing. The test-only Core entrypoint is now included in Core typecheck. Rerun the journey, then repair exposed workflow gaps. All CA-5.8 suites/stacks exited.

Second browser run failed (session 88130 exit 1; scheduled-customer-journey-second.log) after successful scheduler-driven original Order commitment because the test parsed list items as orderId instead of the actual id. Corrected against the contract. Core test entrypoint typecheck initially exposed its production-generated ENVIRONMENT union; runtime string validation retains the test-only guard and pnpm typecheck now passes. Named addition options now span the new Core read, Orders RPC/index, shared Orders/Core-service contracts and the existing Web amendments route. AmendmentFlow now uses named search and validated responses, retaining the same draft/payment request after uncertain replies. Focused Core command pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/orders/application/order-amendment.integration.test.ts src/entrypoint/core-service-conformance.test.ts passed 16 tests/2 files (scheduled-addition-options-focused.log). pnpm lint, pnpm architecture:check and pnpm naming:check passed. No migration. Browser test now includes lost draft/payment response replay and a real two-minute cutoff. An initial multi-hunk patch failed to match formatted test code; no partial patch applied, then corrected against actual lines.

Concrete next action: rerun the named customer journey against HEAD 19f409a1 plus the listed CA-5.9 working-tree changes, then fix its observed failures and finish focused/aggregate/browser acceptance. Owning API documentation and final diff review remain unfinished. Earlier CA-0–2, approved product follow-ups, Phases 6–7 and actual provider/release inputs remain open. Three major phase blocks (5–7) remain. No Core/browser overlap; Core focused suites exited before the next browser run.
## Completed implementation slice: CA-5.8 Supplier-exception resolution after cancellation

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 5 — Scheduled operations**, section G: approved Order/financial resolution of supplier shortage or poor quality. Acceptance: accepted coordinated cancellation internally resolves tracked supplier discrepancies only when the original receipt is accounted and the same cycle/destination/pool's remaining paid unpacked demand is covered by received cycle goods. Preserve original rejected/missing quantities, original purchase snapshots, pending refund truth and the same cancellation replay. Resolution and immutable audit evidence share the guarded cancellation transaction; skipped effects, authorization/version races and competing packing/replacement/surplus must not create partial effects. Receiving shows the resolution, and the operational list stops presenting resolved tracked discrepancies as outstanding. No separate operator step, new financial authority, substitution or physical-stock netting.

Reconciled on pushed `75e2105b`: `cancelOrder` already releases operational demand inside its guarded acceptance batch before provider refunds; the shared `releaseOperationalEffectStatements` is the appropriate owning orchestration point. Procurement replacement can resolve linked supply exceptions, but cancellation cannot. `listOperationalExceptionsForLocations` continues listing rejected receipts even after their tracked replacement exceptions resolve. Existing supply_exception resolution/version and audit storage can retain the required evidence without a new balance or migration. CA-5.8 draft adds Procurement-owned `resolve-canceled-supply.ts`, composed after demand release in the paid cancellation batch. It checks fully accounted tracked receipts and remaining unpacked cycle demand, updates resolution/version and writes one immutable audit per exception with postcondition guards against silently omitted updates/audits. Existing receiving and operational reads plus shared receiving contract/Web notice show resolved cancellation without erasing discrepancy quantities or implying refund success. No migration or new operator action. Unfinished files: new resolver, Orders cancel-order, Procurement queue and recovery integration tests, Admin operations reads, operational exception read, shared admin-operations contract and Web receiving page; owning API/State/Data docs remain to update. Initial `pnpm typecheck` passed. Focused `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/procurement/application/procurement-command-recovery.integration.test.ts src/procurement/application/receiving-command-recovery.integration.test.ts src/orders/application/cancel-order.integration.test.ts src/entrypoint/core-service-conformance.test.ts` initially failed **3 / 91 passed**, session 97088 exit 1, log `supply-resolution-focused.log`: normal resolution hit an invalid JSON path and two guard assertions queried a nonexistent idempotency_records.id. The diagnostic run confirmed the JSON-path cause; corrected to $.cancellationId / $.orderId and the actual idempotency_key column. Final identical four-file command passed **94 tests/4 files**, session 68382 exit 0, log `supply-resolution-focused-final.log`. New scenarios include remaining demand, ignored resolution/audit writes, whole-cancellation rollback and original retry. Final `pnpm typecheck` and `pnpm lint` passed. Browser `E2E_START_STACK=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-week.spec.ts --retries=0` passed **2 tests/2.8m**, session 89380 exit 0, log `supply-resolution-browser.log`, with fixed source and no Core overlap. Desktop/mobile resolved receiving screenshots inspected and copied to `supply-resolution/`; original 500 rejected/1000 missing quantities remain visible, resolution requires no further operator action, and the page fits mobile. It adds second-destination shortage receiving, actual affected-Order navigation and dashboard cancellation, then resolved receipt evidence. Existing paid history remains an explicit fixture seam. Web Order cancellation explanatory copy is simplified; no new cancellation rule. API/State/Data now describe the approved resolution and derived flag. After browser exit, added the two-concurrent-cancellation case without changing application source. The same focused four-file command passed **95 tests/4 files**, session 52018 exit 0, log `supply-resolution-concurrency.log`; one resolution/audit set closes shared discrepancies after both demands release. Full `pnpm check` passed, session 54844 exit 0, log `supply-resolution-check.log`: Core **1577 tests/194 files** (470.88s), Web **403/101**, contracts **68/19**, shared suites, harness, fresh/retained migration verification, conventions, architecture/readiness, lint, types and both builds. Scope: `75e2105b` plus the 14 intended CA-5.8 files. `pnpm --filter @freshmarkets/web check:vinext` passed **15 supported/0 issues**, log `supply-resolution-vinext.log`. Browser stack exited before Core suites resumed; only the added Core concurrency test and documentation followed browser acceptance, with application source unchanged. All verification processes exited. Reviewed intended diff and new resolver; `git diff --check` passed; protected discussion hash remains unchanged.

CA-5.8 committed/pushed as `19f409a1` (`75e2105b..19f409a1`); reviewed staged diff and commit/push hooks passed. The active CA-5.9 section owns the full Scheduled journey. Earlier CA-0–2, the full no-SQL customer journey, approved product follow-ups and actual provider/release obligations remain open. Three major phase blocks (5–7) remain.
## Completed implementation slice: CA-5.7 Shortage Order review and cancellation

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 5 — Scheduled operations**, section G shortage/poor-quality resolution through approved Order/financial operations. Acceptance: the ordinary receiving discrepancy links to the exact affected paid Orders for its cycle, destination and requirement; bounded scoped reads include original/addition demand and distinguish released demand from pending refund progress. Global staff can use the existing reasoned business cancellation and coordinated original/addition refund command. Prove accepted cancellation releases demand, prevents canceled work being packed, and does not fabricate provider refund success or erase original rejected/missing receipt evidence. No substitution, partial-line cancellation, separate shortage workspace or new refund authority.

Reconciled on pushed `49d75d3a`: receiving currently offers replacement only. Weekly Orders lists every Order for the week/location with no requirement filter. `cancelAdminOrder` already calls canonical `cancelOrder` with BUSINESS / OPERATIONAL_FAILURE, Global orders.manage, reason/version/idempotency and coordinated Payments refunds. Supply exceptions currently close only through inspected replacement; final exception closure after financial resolution remains a separate visible acceptance obligation until implemented/proven. CA-5.7 draft now changes Core scheduled-week, shared scheduled-week contracts/validation, Web weekly/receiving pages and procurement recovery integration tests. The request accepts a requirement filter only for location-bound Orders and verifies its cycle/location before selecting exact SKU/pool demand; the projection carries cancellation progress separately. Receiving links to the filtered ordinary weekly page. Initial `pnpm typecheck` passed on the application draft before the new integration fixture (log `shortage-orders-types.log`, session 13541 exit 0). Focused `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/procurement/application/procurement-command-recovery.integration.test.ts src/entrypoint/core-service-conformance.test.ts` passed **36 tests/2 files**, sessions 76800 and final 27170 exit 0, logs `shortage-orders-focused.log` / `shortage-orders-focused-final.log`. Strengthened the cancellation case with an existing fulfillment record and current version: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/procurement/application/procurement-command-recovery.integration.test.ts -t 'reviews only a receiving'` passed **1 / 32 skipped**, log `shortage-orders-packing.log`; rejection is ILLEGAL_TRANSITION, not missing fixture. Refund progress remains REFUNDS_PROCESSING and original receipt evidence is unchanged. New test reaches purchase/start/shortage receiving and Global business cancellation through real Core methods; initial paid history is explicitly fixture data. Final draft `pnpm typecheck`, `pnpm lint`, `git diff --check` passed. Browser `E2E_START_STACK=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-week.spec.ts --retries=0` finished **1 passed/1 failed, 3.9m**, session 95045 exit 1, log `shortage-orders-browser.log`. Desktop passed the new link; mobile timed out at initial Global articles before the changed link. Trace shows initial weeks response 200/2.28s and selected-week request still pending when the 5-second assertion ended, with Loading state, no returned application error. Saved trace/desktop image in `shortage-orders-first/`. No process remained. Retry unchanged once to distinguish transient local stack delay; do not claim complete browser acceptance. Source fixed and no Core overlap. It extends the existing desktop/mobile receipt/replacement/surplus journey with the exact affected-Order link. API_CONTRACTS records the filter and remaining automatic exception-closure gap. Unchanged final browser rerun **passed 2 tests/2.8m**, session 90541 exit 0, log `shortage-orders-browser-retry.log`. Desktop/mobile affected-Orders screenshots inspected, readable without overflow, copied to `shortage-orders/`. Both journeys retain replacement/replay/surplus coverage; synthetic initial financial history is not the no-SQL customer journey or actual provider acceptance. Source remained fixed. Full `pnpm check` passed, session 68431 exit 0, log `shortage-orders-check.log`: Core **1573 tests/194 files** (449.96s), Web **403/101**, contracts **68/19**, shared suites, harness, fresh/retained migration verification, conventions, architecture/readiness, lint, types and both builds. Tested `49d75d3a` plus the nine intended CA-5.7 files. `pnpm --filter @freshmarkets/web check:vinext` passed **15 supported/0 issues**, log `shortage-orders-vinext.log`. Browser stack exited before aggregate startup; no application edits followed passing browser acceptance. Reviewed all nine intended files; `git diff --check` passed and protected discussion hash remains unchanged. All verification processes exited.

CA-5.7 committed/pushed as `75e2105b` (`49d75d3a..75e2105b`); staged diff and commit/push hooks passed. The active CA-5.8 section owns supplier-exception resolution. Preserve the full no-SQL customer journey, automatic exception closure after approved financial resolution, earlier CA-0–2, approved product follow-ups and actual provider/release obligations. Three major phase blocks (5–7) remain.
## Completed implementation slice: CA-5.6 Consolidated destination purchasing

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 5 — Scheduled operations**, section G: consolidated exact paid purchase quantities with destination breakdowns. Acceptance: Global procurement readers can select a named week and see exact SKU totals and each named destination quantity, including paid additions and excluding accepted cancellations; local readers retain only authorized location quantities. Reads remain bounded/paginated, totals remain correct across pages, denied scope does not leak records, and physical stock never enters the calculation. Existing per-destination purchase confirmation remains the only write. Ordinary weekly UI, no new supplier workflow, storage balance or approval step.

Reconciled at pushed `bad66079`: `getAdminScheduledWeek` requires locationId and the Web page refuses to load in Global scope. Existing demand and purchase records are already cycle/location/SKU-specific, so no schema change is expected. Current global authorization helper serves fulfillment configuration only; extend the existing operations access boundary for procurement rather than duplicating IAM. CA-5.6 draft adds optional locationId for Global-only demand reads, per-destination names/quantities and whole-SKU totals computed before pagination, composite SKU/pool/location cursors and existing destination purchase writes. Renamed the existing Global operations access helper across its four callers and added procurement capabilities; no duplicate IAM authority. Web Global scope shows consolidated quantities and retains normal destination-specific confirmation with the destination named in the Sheet. Local Orders/offers remain location-bound. CA-5.6 file scope: Core scheduled-week/access and three renamed-helper callers, shared admin-scheduled-week contracts/validation, Web procurement page, procurement recovery tests and scheduled-week browser test. Initial typecheck caught optional location narrowing in the Orders branch; corrected without assertion. Initial focused run failed 1/51 because the new second-location fixture did not exist; created its own location. Final `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/procurement/application/procurement-command-recovery.integration.test.ts src/admin/application/delivery-cycle-administration.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: **51 tests/3 files passed**, session 84749 exit 0, `destination-purchases-focused-final.log` (first failure log `destination-purchases-focused.log`) under the current evidence root. Includes exact Global/local totals, cancellation exclusion, per-destination purchase state, Global-only authorization and 51 destinations across two pages with complete totals/no duplicate/missing rows. `pnpm typecheck` and `pnpm lint` passed. Browser test now covers Global totals and purchase of the second destination, then local original purchase/replay/receiving; synthetic financial history remains a fixture seam. Browser `E2E_START_STACK=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-week.spec.ts --retries=0` **passed 2 tests/3.1m**, session 28016 exit 0, `destination-purchases-browser.log`. Global 2,500 g total splits 1,500/1,000 g; Global purchase changes only the second destination; local pending refresh/purchase replay/receiving/surplus still pass. Desktop/mobile Global screenshots inspected and copied to `destination-purchases/`; readable with no overflow. Source fixed during browser, no Core overlap. API_CONTRACTS updated for Global authorization, totals/paging and exact destination writes. Full aggregate `pnpm check` passed, session 18942 exit 0: Core **1572 tests/194 files**, Web **403/101**, contracts **68/19**, remaining shared suites, harness, fresh/retained migration verification, conventions, architecture/readiness, lint, types and both builds. Log `destination-purchases-check.log`; Core duration 456.41s. `pnpm --filter @freshmarkets/web check:vinext` passed **15 supported/0 issues**, log `destination-purchases-vinext.log`. Browser stack exited before aggregate startup; application source remained fixed from the passing browser through aggregate. Reviewed all 12 intended files; `git diff --check` passed and protected discussion SHA-256 remains unchanged. All verification processes exited. Tested working-tree scope is CA-5.6 on `bad66079`, with checkpoint prose only afterward.

CA-5.6 committed/pushed as `49d75d3a` (`bad66079..49d75d3a`); exact 12-file staging, staged diff and commit/push hooks passed. The active CA-5.7 section owns the next action. Earlier CA-0–2, approved product follow-ups, shortage-driven Order/refund resolution, full no-SQL customer journey and actual provider/release obligations remain open. Three major phase blocks (5–7) remain.
## Completed/pushed: CA-5.5 Late-payment purchasing

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 5 — Scheduled operations**, section G and paid-addition/cutoff race acceptance. Acceptance: purchase confirmation cannot freeze incomplete demand while started original/addition payments can still commit; the normal delivery-week view explains pending readiness and enables purchase of exact confirmed quantities after resolution. Canonical successful payments still commit exactly once after cutoff. Same-key replay, current scope/versions and the complete purchase transaction remain intact. New addition payments cannot start after cutoff or create partial intent/link effects. No provider acceptance or extra supplier workflow is inferred.

Working tree based on pushed `c0e7ed7c`. Implemented draft: shared Payments readiness predicate reused by surplus and purchase command/read; transactional purchase guard; `week.purchaseBlockedReason` contracts/validation and normal five-second pending refresh. Further call-path review found addition payment creation admitted a new intent after cutoff, then failed its outer link, while successful started additions could not commit after cutoff. Fixed new admission in the intent/link batch (ownership/state, version, money, cutoff/cycle); provider call occurs only after acceptance. Existing keys replay after cutoff. Addition commitment uses payment start against immutable Order cutoff and retains current Order/refund/payment-version protections. No schema change.

Unfinished intended files: `payments/infrastructure/d1/scheduled-commitment-readiness.ts`; `payments/application/create-payment.ts` and `create-amendment-payment-intent.ts`; `orders/application/apply-amendment-payment-reaction.ts`; procurement create-requirement/scheduled-surplus; Admin scheduled-week; contracts/validation admin-scheduled-week; Web procurement page; Core procurement recovery, checkout-reaction and order-amendment tests; Web scheduled-week test; API_CONTRACTS, STATE_MACHINES and this checkpoint. Do not stage unrelated README/count-stock timestamp-only changes or protected/deleted files.

Evidence root: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347`.

- Pending/failure/race tests: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/procurement/application/procurement-command-recovery.integration.test.ts src/procurement/application/scheduled-surplus.integration.test.ts`: **41 passed/2 files**, session 89935 exit 0, `late-payment-focused.log`. Signed mock-provider failures unblock both original/addition purchasing. Ordinary failed observations do not create commitment reactions; no terminal-payment policy change needed.
- Late original Quote/payment/provider reconciliation/commitment/purchase: combined same command plus `src/orders/application/apply-checkout-payment-reaction.integration.test.ts` **66 passed/3 files**, session 47044 exit 0, `late-payment-focused-final.log`; exact 2,000 g / 4 units, no physical stock, original replay. Initial shared-cycle test failed 10 tests; isolated fixture then needed schedule/windows/participation and the correct result field. Final fixture is isolated; eligibility was not weakened. `pnpm typecheck` passed (`late-payment-types.log`).
- Browser: `E2E_START_STACK=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-week.spec.ts --retries=0`: **2 passed/2.5m**, session 34485 exit 0, `late-payment-browser.log`. Real desktop/mobile pending read, automatic refresh, purchase/unknown-response replay, receiving and surplus. Synthetic pending/failure/committed-payment history is explicitly a browser fixture seam, not provider/no-SQL customer acceptance. Mobile pending screenshot inspected: readable, no overflow. Source fixed during browser; Core suites exited beforehand. This browser predates the subsequent addition admission/commitment repair; final rerun remains required.
- Addition reproduction: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/orders/application/order-amendment.integration.test.ts`: **1 failed/6 passed** with an unexpected intent after cutoff (`late-addition-reproduction.log`), then **1 failed/6 passed** on late commitment after admission repair (`late-addition-reaction-reproduction.log`). Both reproduced defects repaired. Combined four files (above plus order-amendment): **73 passed**, session 41460 exit 0, `late-payment-complete-focused.log`.
- Final intent/link transaction refinement: order-amendment plus `src/payments/application/create-payment.integration.test.ts`: **21 passed/2 files**, `late-addition-admission.log`. Added admission cutoff/cancellation/version/omitted-link tests; first run **1 failed/10 passed** because D1 rejects a TEMP test trigger (`late-addition-races.log`); normal test trigger with finally cleanup fixed it. Final order-amendment command **11 passed**, exit 0, `late-addition-races-final.log`. Rejections assert no intent/link and zero provider submissions. Late successful addition applies once and creates exactly 1,000 g demand.
- Aggregate `pnpm check`, session **22169**, exited 1: **1569 Core passed / 1 failed**, 194 files; Web 403/101 and contracts 68/19 passed. The new cutoff-admission race used Worker Date.now minus one millisecond, which can still be ahead of D1 time. Changed its cutoff mutation to D1 unixepoch time (the same clock as admission), preserving the exact-cutoff rejection assertion. Focused order-amendment rerun **11 passed**, exit 0, `late-addition-clock.log`. Full retry `pnpm check` **passed**, session **31533 exit 0**, log `late-payment-check-final.log`: **1570 Core/194 files, 403 Web/101, 68 contracts/19**, shared tests/harness, migration/convention/architecture/readiness/lint/types and both builds. Core duration 376.81 seconds. Final unchanged-source browser session **9347 exited 1** before tests: managed stack startup exceeded 180 seconds, `late-payment-browser-final.log`. Core exited before launch; process inventory was clear, and unchanged browser retry **passed 2 tests/2.8m**, session **17972 exit 0**, log `late-payment-browser-final-retry.log`. Final mobile pending screenshot inspected and final screenshots copied to `late-payment/`. All application suites/stacks exited; final source is the working tree based on `c0e7ed7c`. No application failure or acceptance is inferred from this startup failure. Both desktop/mobile pending screenshots were inspected and copied to evidence root `late-payment/`. Vinext passed 15 supported/0 issues (`late-payment-vinext.log`). Discussion SHA256 remains `0021CDF5935E3B90D2C23EAA5EBA56DA7419EE7CD2E8FA479990E03BC64B792F`.

CA-5.5 committed/pushed as `bad66079` (`c0e7ed7c..bad66079`). Intended 17-file diff, staged checks and both hooks passed. CA-5.6 subsequently completed the destination view; the full original/addition/cutoff/shortage/packing journey remains an acceptance obligation. Keep shortage-driven Order/refund resolution, earlier CA-0–2, approved product follow-ups and actual provider/release obligations open. Three major phase blocks (5–7) remain; this is not Phase 5 completion.
## Completed/pushed: CA-5.4 Inspected surplus release

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 5 — Scheduled operations**, section G. Acceptance: a scoped receiving action releases only inspected unused cycle/location/pool goods, reduces allocation and credits the same physical pool exactly once with immutable movement/ledger/audit/result. Protect outstanding paid demand awaiting packing and unresolved payment commitment. Wrong scope, stale versions, omitted effects and competing over-release leave no partial writes. Rejected/spoiled goods cannot be credited. Keep the ordinary receiving form and saved original retry; no separate stock/recovery workspace.

Implemented working tree based on `61d1299b`: `scheduled-surplus.ts` contracts/validation, Core read/command, entrypoint and receiving projection; Web same-origin route and receiving Sheet. Existing balance/movement/ledger storage is reused, with no migration. The command subtracts packed/released/disposed and outstanding OPEN demand without immutable packing evidence, requires cutoff to have passed, and guards unresolved original/addition payments plus unapplied recovery evidence. It rechecks all mutable prerequisites in the transaction before cycle debit, physical credit and every required effect. The read is bounded to the current receiving page (up to 100 requirements via bound JSON), supplies named Product/count-size/week and preserves denied/unavailable states. Counted goods credit pieces only; measured grams never receive a second credit. The normal form captures inspected quantity/reason and retains original scope/body/key after an unknown response.

Packing is a necessary dependency of surplus calculation. Review found its per-pool movement and balance writes lacked required-change guards. `fulfillment/application/consume-cycle-goods.ts` now verifies each write affects every distinct demanded pool. An omitted effect aborts fulfillment status/audit/result too. No new operator step, financial rule or schema was added.

Verified files completed in CA-5.4: new Core `procurement/application/scheduled-surplus.ts` and integration test; Core Admin operations reads/index; fulfillment consume-cycle-goods and cycle-goods integration test; new shared contracts/validation plus barrels/core-service/admin-operations; new Web receiving surplus route/component, receiving page and extended scheduled-week browser; API/DATA/STATE references and this checkpoint. Protected config/Discussion, unrelated deletions, README stat-only changes and encoding-damaged historical status remain outside the intended commit.

Actual verification:
- `pnpm typecheck` passed before final packing/name corrections; final aggregate below includes their typecheck.
- Reproduction: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/fulfillment/application/cycle-goods.integration.test.ts`: **2 failed/2 passed**, session 60813 exit 1, `scheduled-packing-reproduction.log`. Both silent omissions incorrectly returned success before the guards.
- Final focused: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/procurement/application/scheduled-surplus.integration.test.ts src/procurement/application/scheduled-counted-receipts.integration.test.ts src/fulfillment/application/cycle-goods.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: **26 tests/4 files passed**, session 95537 exit 0, `scheduled-surplus-focused-final.log`. Covers real purchase/receipt, real business Order cancellation before surplus, rollback, competing release, scope/demand/payment races, original replay and lost committed reply. Canonical payment history is a fixture seam; refund provider acceptance is separate.
- Final browser: `E2E_START_STACK=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-week.spec.ts tests/scheduled-counted-receiving.spec.ts --retries=0`: **4 passed/2.2m**, session 79016 exit 0, `scheduled-surplus-browser-final-retry.log`. Both widths (1440/390) cover counted receipt-to-packing and ordinary receipt/replacement -> real Admin cancellation -> inspected release 400 g -> identical saved retry -> 600 g remaining. Synthetic paid history remains explicit. Screenshots inspected/copied to evidence root `scheduled-surplus/`, correct Red onion label and no page overflow.
- `pnpm --filter @freshmarkets/web check:vinext`: **15 supported/0 issues**, `scheduled-surplus-vinext.log`. `git diff --check` passed. Both desktop/mobile pending screenshots were inspected and copied to evidence root `late-payment/`. Vinext passed 15 supported/0 issues (`late-payment-vinext.log`). Discussion SHA256 remains `0021CDF5935E3B90D2C23EAA5EBA56DA7419EE7CD2E8FA479990E03BC64B792F`.
- Final `pnpm check` **passed**, session 91129 exit 0: **1561 Core/194 files, 403 Web/101, 68 contracts/19**, shared tests, harness 26, clean/retained migrations, naming/format/terminology/architecture/readiness, lint/typecheck and both builds. Log `scheduled-surplus-check-final.log`; Core duration 379.30s. Core and browser never overlapped; source stayed fixed after final browser. All suites/stacks exited. Reviewed intended files were committed/pushed as `c0e7ed7c` (`61d1299b..c0e7ed7c`).

Resolved attempts: initial focused 24 passed, then stronger stock/ledger assertions and invalid-input coverage passed. First browser 9178 had counted cases pass and two surplus failures because the test remained on the delivery-week page; following its Receiving link fixed the test. Second browser 95239 passed 4/2.3m. Screenshot review then corrected the shared Product label, and packing reproduction led to the guards above. Initial aggregate 8938 was intentionally interrupted (exit 1) before those corrections; process inventory confirmed no suite remained. A final browser launch failed before tests with Windows code 3221226505 (`scheduled-surplus-browser-final.log`); identical command retry passed as above. No hidden failed gate or provider acceptance. Only isolated tests/designated disposable browser state were used; no retained/remote changes, provider action or deployment.

CA-5.4 is complete locally and pushed as `c0e7ed7c`. CA-5.5 subsequently addressed purchase readiness: `create-procurement-requirement.ts` forbids recalculation after ORDERED, while original/addition payment reactions may commit later and do not update purchasing. CA-5.5 verification records the protection against unresolved started payments. Full original/addition/cutoff customer journey, destination purchase breakdown, shortage-driven approved Order/refund resolution, earlier CA-0–2 and approved product/provider obligations remain open. Supplier overage receiving and returning already-packed canceled goods are not claimed by CA-5.4.
## Completed/pushed: CA-5.3 Scheduled receipt weight and size counts

Source: `docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md`, **Phase 5 — Scheduled operations**, section G and PRODUCT GD-D17–18. Acceptance: normal supplier receiving links measured bulk grams and actual named-size counts for one Product/cycle/location; only accepted counts credit cycle goods. Scope, versions, original replay and all receipt/allocation/audit/result effects commit atomically. Recorded weight/counts are visible, and existing Scheduled packing consumes the correct counts without physical-stock netting.

Implemented on the working tree based on `2f11e609`: migration 0090 adds immutable weighed receipt/size-event links and snapshots without rewriting retained rows. `prepareReceivingCommand` lets single and grouped receipts share the same guarded writes in one D1 batch. Shared contracts/validation, Core RPC/read projections, same-origin route and normal receiving Sheet are complete. A purchased size needs no separate Start step; partial/missing/replacement handling remains the existing receipt policy. The form retains original scope/body/key after an unknown outcome and the read ignores stale prior-scope responses. Latest five complete receipts are explicitly recent evidence. Measured grams never estimate counts or credit a second stock balance. Included counts remain bounded by the purchased requirement; additional surplus-receipt capture is not claimed.

Verified intended scope: migration 0090; Core receiving compiler/new counted command/integration test, procurement queue/Admin operations reads/index; shared contracts/validation/barrels; Web receiving page/new counted route/component/browser test; schema verifier; API/DATA/STATE references and checkpoint. No unrelated deletions, README stat-only changes, protected config or Discussion enter the commit.

- Focused: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/procurement/application/scheduled-counted-receipts.integration.test.ts src/procurement/application/receiving-command-recovery.integration.test.ts src/procurement/application/procurement-command-recovery.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: **50 tests/4 files**, session 4409 exit 0, `scheduled-counts-focused.log`. Real authoring/purchase/receipt/packing, missing/replacement, rollback, scope loss, replay and concurrent groups covered.
- `pnpm check`: **passed** through both final build `Done` records: **1548 Core/193 files, 403 Web/101, 68 contracts/19**, shared tests, harness 26, clean/retained migrations, conventions, architecture/readiness, lint/types and builds. Log `scheduled-counts-check.log`. Original session handle was lost during context recovery; existing log/process was observed to completion without restarting/overlapping suites. Core duration 487.65 seconds.
- `E2E_START_STACK=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-counted-receiving.spec.ts tests/scheduled-week.spec.ts --retries=0`: **4 passed/2.1m**, session 81341 exit 0, `scheduled-counts-browser-second.log`. Desktop 1440/mobile 390 weighed 3500 g, counted Small 2/Large 3, retried identical saved body/key after a lost response, then packed through normal UI. Ordinary receipt/shortage/replacement also passed. The committed payment/Order/cycle is explicit synthetic history, not a full customer or provider journey.
- `pnpm --filter @freshmarkets/web check:vinext`: **15 supported/0 issues**. `git diff --check` passed. Desktop/mobile screenshots inspected and copied to evidence root `scheduled-counts/`; no page overflow. Discussion SHA256 unchanged: `0021CDF5935E3B90D2C23EAA5EBA56DA7419EE7CD2E8FA479990E03BC64B792F`.

Resolved failures: narrowed idempotency-key closure type fixed before focused checks. First browser session 69469 failed all four cases in auth fixture on `::1:3100` connection refusal, before application acceptance; unchanged managed stack retry passed. No Core/browser overlap. Application source stayed fixed through final browser and aggregate; later edits are documentation only. Only isolated tests/designated disposable browser database received 0090; no real provider, retained/remote reset or deployment.

CA-5.3 was committed/pushed as `61d1299b` (`2f11e609..61d1299b`). CA-5.4 and CA-5.6 subsequently completed inspected surplus and destination purchase breakdown. Full original/addition/cutoff/cancellation journey, shortage-driven approved Order/refund resolution, earlier CA-0–2 and product/provider obligations remain open.
## Completed in this continuation: CA-4.1

Committed and pushed as `0245ce0`; seven authenticated typed RPCs, guarded draft/dispatch/receipt/cancel commands, immutable transfer/receipt storage, current capability/scope checks, per-line stock/ledger effects, original-result idempotency, same-origin adapters and normal Admin list/create/detail actions. Warehouse opening stock reuses the existing inventory adjustment path. Draft quantities are not transit. The real browser path creates synthetic named sites through Core commands and a product through Admin, adds 100000 grams through Inventory and reaches 60000/20000/20000 after two transfers. CA-4.1 alone left Phase 4 open for CA-4.2; the latest ledger also retains the newly approved CA-4.3 count follow-up. No real provider/deployment acceptance is implied.

### CA-4.1 verified evidence

Working tree based on `ab276df`: new migration, contracts/validators, `inventory/application/inventory-transfers.ts`, `inventory/infrastructure/transfer-repository.ts`, `entrypoint/inventory-transfers-rpc.ts`, explicit Core method delegation/service inventory, capability vocabulary, migration preservation verifier and `inventory-transfers.integration.test.ts`. Web routes, `components/admin/inventory-transfers.tsx`, Core-fed navigation and `tests/inventory-transfers.spec.ts` now exist. Core typechecking and the later repository-wide aggregate passed. `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/inventory/application/inventory-transfers.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: first run 8 failed/3 passed because repository mistakenly joined nonexistent `inventory_pool.product_id`; corrected to actual `product.inventory_pool_id`. Same command rerun **11 passed/2 files**. Covers two-destination 100000/60000/20000/20000 conservation (destination delta against fixture baseline), partial/full receipts, original replay after later commands, draft cancellation, competing 60000 dispatches, wrong-site denial, silent ledger/audit/idempotency insertion rollback and lost committed D1 response. Tests use isolated geography/IAM fixtures and real authenticated Core stock/movement commands. No provider acceptance. Suites exited, no browser stack running.

Subsequent focused Core runs: same two-file command **19 passed**; after adding reachable destination Instant-Quote concurrency, the three-file command (also `src/checkout/application/instant-quote.integration.test.ts`) initially had 1 failed/41 passed because preceding same-file Quote fixtures retained holds. The new fixture now includes those existing held quantities in its opening balance rather than discarding evidence; rerun **42 passed**. Added create-effect rollback, competing receipts and a retained source hold inserted between read and dispatch: **48 passed/3 files**. The mixed GRAM/PIECE multi-line/scoped receipt test passed in the final aggregate. Typecheck initially caught nonexistent Quote DTO `version`; the test now reads the owning stored version for cleanup, as existing tests do.

`pnpm typecheck`, `pnpm lint`, `pnpm architecture:check`, `pnpm naming:check` passed. `pnpm migration:check` first failed on Node SQLite null-prototype comparison, then the older permission baseline assertion; fixed scalar permission assertion and explicit additive-code exclusion while retaining all-row/grant preservation. Final migration check passed. Explicit repository `oxfmt` paths applied; final `pnpm format:check` passed after a second pass over the repository file. No unrelated files were formatted or changed.

Managed browser: `pnpm --filter @freshmarkets/web exec playwright test tests/inventory-transfers.spec.ts --retries=0` with `E2E_START_STACK=1` and `E2E_STATE_NAME=e2e-commerce-alignment-20260907`: **2 passed/2.2 minutes**, desktop 1280/mobile 390. Real same-origin site setup commands, product UI, Inventory opening-stock UI and transfer UI establish exact 60000/20000/20000 balances at newly created sites, partial receipts and identical body/key replay after lost dispatch response. No operational SQL beyond the established IAM fixture. Both assert no horizontal page overflow. Screenshot reviewed (mobile detail uses a scrollable table); copied to evidence root `commerce-transfers/warehouse-received-{1280,390}.png`. Log `commerce-transfers-browser.log`. Session 55032 exited; source fixed during browser, no Core overlap. Migration 0087 applied only to isolated suites/designated disposable state. Later source changes are the unused repository helper removal and test extensions; no browser behavior change.

Final aggregate `pnpm check` **passed** (session 89766 exit0): Core **1491 tests/191 files**, Web **403/101**, contracts **68/19**, shared packages, harness **26**, migration clean/retained upgrades, naming/terminology/commit/architecture/readiness, lint, types and both builds. Includes 23 transfer tests and the new concurrent destination Quote test. Log `commerce-transfers-check.log` under the 01a082ba evidence root. `pnpm --filter @freshmarkets/web check:vinext`: **15 supported/0 issues**. `git diff --check` passed. Tested application working tree is the complete CA-4.1 change based on `ab276df`; only documentation/checkpoint edits followed aggregate startup. No suites or browser stacks remain running. Canonical API/Data/Admin design describe the slice; Implementation Plan's stale status now points to this evidence/remaining-gap ledger. Committed/pushed `0245ce0`; no provider/release acceptance or deployment is implied.

## Completed gate: CA-3.2c / parent CA-3.2

Phase 3 local acceptance is complete on **`0d14da0`**. Final `pnpm check` passed: Core **1467/190 files**, Web **403/101**, contracts **68/19**, shared package tests, harness, migration clean/upgrade checks, conventions, lint, types and Core dry-run/Web builds. Log `commerce-phase3-final-check.log`. `pnpm catalog:check` passed (226 products/assets, 415 variants); vinext scan passed 15 supported/0 issues. No provider/deployment acceptance is inferred.

Final browser command: `$env:E2E_START_STACK='1'; $env:E2E_STATE_NAME='e2e-commerce-alignment-20260907'; pnpm --filter @freshmarkets/web exec playwright test tests/admin-catalog.spec.ts tests/category-recovery.spec.ts tests/catalog-variants.spec.ts tests/admin-promotions.spec.ts tests/product-media-recovery.spec.ts tests/promotion-media.spec.ts tests/authored-promotion-quote.spec.ts --retries=0`: **28 passed, 4.4 minutes**. Log `commerce-phase3-final-browser.log`. Source stayed fixed and no Core suite overlapped. Final screenshots reviewed and copied with test-directory identities under `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/commerce-phase3-final`; logs live beside that directory. Sessions 77881 and 30073 exited 0.

Coverage: catalog/category create/detail/edit/status/order and fixed variants; GRAM/PIECE consumption and shipping weight contracts; anonymous R2 product/campaign publication and replacement/removal/deactivation, invalid/wrong-owner/scope uploads and storage/metadata recovery; Global exact-location price/history and local read-only price/activation, direct local price-write rejection, rollback/replay/concurrent prices; promotion amount/audience/grants/status authoring, real customer Quote, explicit/automatic selection and paid claim/snapshot/component-stack guards. Core test modules include admin catalog/effects, category recovery, product/promotion media recovery, catalog service, promotion evaluator and Instant Quote/paid-reaction tests. Local provider fakes and auth fixture provisioning are explicit limits. Earlier no-SQL onboarding/setup and full customer payment/delivery/provider journeys remain CA-0-2/CA-7; the visible legacy trial banner/courier wording remains CA-6.

## Completed in this continuation: CA-3.2d

Verified source edits based on `092a0ad`: `apps/web/app/admin/catalog/products/[product-id]/page.tsx` uses a separate instance of the existing command hook for variant creation so the normal Add action retries only its own saved request; inputs freeze while unresolved. `apps/web/components/admin/global-price-panel.tsx` keeps one Save price action for original submit/retry. `global-price-panel.test.tsx` and `catalog-variants.spec.ts` retain exact-body/key replay assertions using normal actions. `admin-catalog.spec.ts` replaces its obsolete local price editor with the Global editor and asserts no local Save price control. No Core/schema/RPC change or new abstraction. `pnpm typecheck` passed; focused Web `vitest run --config vitest.config.ts components/admin/global-price-panel.test.tsx`: 2 passed/1 file.

Final browser: `$env:E2E_START_STACK='1'; $env:E2E_STATE_NAME='e2e-commerce-alignment-20260907'; pnpm --filter @freshmarkets/web exec playwright test tests/admin-catalog.spec.ts tests/catalog-variants.spec.ts --retries=0`: **12 passed/2.9 minutes**, including real Global price authoring, local read-only price/activation, desktop/mobile identical-key/body retries through Add variant and Save price. Log `commerce-normal-catalog-actions.log` under the evidence root; screenshots inspected/copied to its `commerce-normal-catalog-actions` directory. Full `pnpm --filter @freshmarkets/web test`: **403 passed/101 files**. `pnpm lint`, `pnpm naming:check`, `pnpm format:check`, `git diff --check` passed. No Core/schema/RPC changes, providers, retained DBs or deployments. Final typecheck and staged diff check passed; committed/pushed `0d14da0` (`092a0ad..0d14da0`).

## Earlier CA-3.2c gate attempts (resolved by final gate above)

`pnpm check` passed on `092a0ad`: Core 1467/190 files, Web 403/101, contracts 68/19, shared package tests, harness, migrations, conventions, lint, types and both builds. `pnpm --filter @freshmarkets/web check:vinext` passed 15 supported/0 issues. Aggregate log: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/commerce-phase3-check.log`. This aggregate predates the CA-3.2d Web changes.

Managed matrix: `pnpm --filter @freshmarkets/web exec playwright test tests/admin-catalog.spec.ts tests/category-recovery.spec.ts tests/catalog-variants.spec.ts tests/admin-promotions.spec.ts tests/product-media-recovery.spec.ts tests/promotion-media.spec.ts tests/authored-promotion-quote.spec.ts --retries=0` with the E2E variables above. First startup failed during local migration before tests; standalone `pnpm --filter @freshmarkets/core exec wrangler d1 migrations apply DB --local --persist-to .wrangler/e2e-commerce-alignment-20260907` completed remaining 0050-0086 (exit0). Same matrix rerun: **27 passed, 1 failed, 6.1 minutes**. Failure: `admin-catalog.spec.ts` expected the removed local `New price for ...` input; current policy correctly has no local write. Fixed under CA-3.2d, verification pending. Logs `commerce-phase3-browser.log` and `commerce-phase3-browser-retry.log` beside the aggregate log. Sessions 17509,99453,3761 ended; no stack remains. Only designated disposable DB changed.

Coverage audit anchors: Admin catalog/category authoring and scope tests; real category/variant/product-media/promotion-media journeys; catalog service fixed variants, exact-location prices and Core media; admin catalog integration local price rejection/activation and shipping grams; catalog-effects transaction rollback/replay/concurrent price tests; promotion evaluator explicit/automatic/targeted usage and Instant Quote plus paid-reaction component stacking/claim/snapshot/replay tests. `global-pricing.spec.ts` uses response mocks and remains presentation-only. These earlier failures are resolved by CA-3.2d and the final gate above; they remain evidence of the repaired gaps. No provider acceptance is claimed. Wider legacy trial banner/checkout language stays open CA-6 work.

## Completed in this continuation: CA-3.2b

Verified working tree based on `60f348d`: new `apps/web/tests/authored-promotion-quote.spec.ts`; shared validation bound, Core RPC validator and existing Instant quote test; Web promotion entry/total review, quote route and route test; API contract/checkpoint. Admin permits 80-character codes while Web and Core RPC rejected over 64. All three now share the validation package's existing 80-character bound; no package dependency, schema or RPC shape added.

Actual evidence: `pnpm typecheck`, `pnpm lint`, `pnpm naming:check`, `pnpm format:check` and `git diff --check` passed after formatting correction. Focused Web `vitest run --config vitest.config.ts test/app/api/checkout/quote/route.test.ts components/storefront/checkout/promotion-entry.test.tsx`: 7 passed/2 files (80 accepted/81 rejected). Core `vitest run --config vitest.config.ts src/checkout/application/instant-quote.integration.test.ts src/promotions/application/evaluate-checkout-promotions.integration.test.ts`: 27 passed/2 files, authored audience code now 80 characters. Browser command below passed once (1 test/2.0 minutes), proving real Admin create/activate -> customer Quote, applied 500 minor discount / 133500 minor total, and discard/release. Screenshot review found long-code overflow; wrapping and desktop/mobile overflow assertion added. Final identical browser command passed 1 test in 2.0 minutes with zero retries; 1280/390 screenshots inspected, no horizontal overflow, Quote discarded successfully. Evidence copied to `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/commerce-authored-promotion-quote`. Visible legacy trial banner remains open CA-6 work, not accepted new-commerce UX.

Corrected failures: first browser test configured dispatch before pickup (Core correctly rejected); second parsed cartId rather than CartView.id; third reached Core and reproduced its 64-character rejection. A temporary contracts import in validation failed typecheck; removed without adding a dependency. Two formatter failures corrected. No current test stack at that milestone; next run must not overlap Core tests.

## Completed in this continuation: CA-3.2a

Product missing-object retry and abandoned confirmed-object cleanup repaired in `product-media-recovery.ts`; no schema/RPC/UI change or new abstraction. Stable immutable object keys and create-only R2 conditions protect retries; confirmed unattached uploads expire internally after 24 hours. Canonical Architecture recovery description updated.

- Reproduction: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/product-media-recovery.integration.test.ts`: 2 failed/13 passed (missing-object retry and authority-revoked cleanup).
- After repair: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/product-media-recovery.integration.test.ts src/admin/application/promotion-media.integration.test.ts`: 27 passed/2 files. `pnpm typecheck` and `pnpm lint` passed.
- `$env:E2E_START_STACK='1'; $env:E2E_STATE_NAME='e2e-commerce-alignment-20260907'; pnpm --filter @freshmarkets/web exec playwright test tests/product-media-recovery.spec.ts --retries=0`: 2 passed in 2.0 minutes (desktop/mobile native upload lost-response replay, price/local activation, cart/public image, removal and stale ETag rejection), including managed build and disposable migration setup. Screenshots copied under `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/commerce-product-recovery`.
- Tested working tree: based on `27889d4`, only recovery implementation/test plus canonical Architecture/checkpoint changes. Source remained fixed during browser tests and no Core suite overlapped. No test stack remains running. Final naming/format/diff checks passed. Committed as `60f348d`; `git push origin main` confirmed `27889d4..60f348d`.

## Completed in this continuation: CA-3.1

Campaign media is reachable through ordinary Global Admin controls and anonymous storefront rendering. Added migration 0086 attachment/upload/cleanup storage, typed contracts and validators, seven Core RPC methods, guarded upload/update/remove commands, Admin/public reads, scheduled internal observation/cleanup, same-origin media adapters, image editor and Core-fed home campaign rail. Images have independent versions; financial definitions/history stay unchanged. Home membership strip and bundled membership campaign removal is partial CA-6 retirement; wider membership work remains open.

Local evidence against the intended working tree based on `0b55737` (these are actual commands, not discovery):

- `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/promotion-media.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: 15 passed/2 files (12 media, 3 conformance), including opening/end boundary, original replay, unknown/failed R2 writes, authority loss, cleanup retry and attachment races/rollback.
- `pnpm --filter @freshmarkets/web test`: 403 passed/101 files; `pnpm --filter @freshmarkets/contracts test`: 68 passed/19 files. After final layout changes, `pnpm --filter @freshmarkets/web exec vitest run --config vitest.config.ts components/storefront/marketplace/promo-banners.test.tsx test/app/api/admin/published-promotion-media.test.ts`: 4 passed/2 files.
- `$env:E2E_START_STACK='1'; $env:E2E_STATE_NAME='e2e-commerce-alignment-20260907'; pnpm --filter @freshmarkets/web exec playwright test tests/promotion-media.spec.ts tests/admin-promotions.spec.ts --retries=0`: 11 passed in 2.7 minutes, including desktop/mobile Admin upload/replace/remove and anonymous publication/deactivation; managed build and migration setup passed. Earlier media-only run: 2 passed; layout review then moved the editor out of Audience and replaced the transparent fixture with visible existing artwork. Final screenshots inspected and copied to `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/commerce-campaign-media`.
- `pnpm migration:check` passed, including the added populated 0085-to-0086 all-row/row-identity preservation probe. `pnpm harness:test`: 26 passed.
- `pnpm typecheck`, `pnpm lint`, `pnpm architecture:check`, `pnpm readiness:check`, `pnpm naming:check`, `pnpm format:check`, `git diff --check`, Core `build` (dry run), Web `check:vinext` passed. Last full aggregate remains the inherited f6f8c88-era check; no whole Phase 3 completion is claimed.
- Corrected test fixture initially caused 7 campaign tests to fail on lowercase codes; subsequent runs passed. Initial Windows-default decoding of the home page failed before writing that file; explicit UTF-8 retry succeeded. Final format check caught three new media routes; directory-level formatting corrected them and the recheck passed. None of these failures remains active.

No application suite/browser stack is running at this milestone. Only the designated disposable E2E state received migration 0086; no retained/shared/remote DB or deployment was changed. Protected files remain outside the intended commit. All CA-3.1 implementation files are ready to stage together; no adjacent commerce partial edits remain outside that slice. Git confirmed CA-3.1 commit `27889d4` and successful `git push origin main` (`0b55737..27889d4`).

## Open task and acceptance ledger

Three major phase blocks remain (Phases 5-7), plus earlier-phase acceptance obligations. Phase 3 is implemented/locally verified; release/provider evidence remains separately open. The rows below are tracking units, not a complete count of small implementation tasks. Split future phase rows into stable child IDs after inspecting their actual call paths; retain the parent and all source-plan criteria.

| ID | Source and coverage | Implementation / acceptance | Exit or next decomposition |
| --- | --- | --- | --- |
| CA-0-2 | Phases 0-2; sections A, E, F, relevant audit defects | Substantial existing work / partial evidence | Reconcile canonical and retained-baseline decisions; verify no-SQL site/address/serviceability/pickup/cycle setup, onboarding/scoped access and financial recovery. Register concrete missing evidence; fix any prerequisite before dependent work. Real account/provider inputs remain explicit. |
| CA-3.1 | Phase 3, section B: campaign media | Implemented / locally verified | Evidence above; no real provider/deployment acceptance implied. |
| CA-3.2a | Child of CA-3.2, section B: product upload recovery | Implemented / locally verified | Evidence above; committed/pushed `60f348d`. |
| CA-3.2b | Child of CA-3.2, sections B/F: authored promotion to real Quote | Implemented / locally verified | Real Admin-to-Quote browser and focused Core evidence above. Committed/pushed `092a0ad` (`60f348d..092a0ad`). |
| CA-3.2c | Child of CA-3.2: Phase 3 acceptance gate | Implemented / locally verified | Final aggregate and 28-browser matrix passed at `0d14da0`; evidence committed/pushed `ab276df`. |
| CA-3.2d | Child of CA-3.2: ordinary variant/price actions after unknown outcome | Implemented / locally verified | 12 browser tests and 403 Web tests passed; committed/pushed `0d14da0`. |
| CA-3.2 | Phase 3, sections B-C and promotion application in F | Implemented / locally verified | CA-3.1 and children a-d, final aggregate/browser gate; broader provider/release evidence stays open. |
| CA-4.1 | Child of CA-4: stock/dispatch/accepted receipt vertical slice | Implemented / locally verified | Aggregate, 23 transfer tests plus reachable Quote concurrency, and two browser journeys passed; evidence above. |
| CA-4.2 | Child of CA-4: discrepancies, losses and verified returns | Implemented / locally verified and pushed `406a405` | Full aggregate and desktop/mobile browser passed; evidence above. |
| CA-4.3 | Phase 4 follow-up, GD-D17–18: actual counted-size stock | Implemented / locally verified; pushed `010b8af2` | Aggregate plus desktop/mobile receipt-to-Quote journey passed; Scheduled counting stays Phase 5. |
| CA-4 | Phase 4, section D | Implemented / locally verified | CA-4.1–3 cover dispatch, receipts/discrepancies and actual counts. Provider/release acceptance remains separate. |
| CA-5.1 | Phase 5 section G, GD-D14–18 | Completed / locally verified; pushed `e6afa560` | Named week, exact purchase confirmation, scoped reads and receiving link; aggregate and desktop/mobile acceptance above. |
| CA-5.2 | Phase 5 section G: receiving/discrepancies | Completed / pushed `2f11e609` | Aggregate 1540 Core plus final desktop/mobile receipt/replacement replay passed; migration 0089 preserves retained evidence. |
| CA-5.3 | Phase 5 section G, GD-D17–18 | Completed / pushed `61d1299b` | Aggregate and 4 browser tests passed; provider/customer-history fixture limitation retained. |
| CA-5.4 | Phase 5 section G: inspected surplus | Completed / pushed `c0e7ed7c` | Aggregate 1561 Core plus final 4-browser matrix; full provider/customer-history acceptance remains separate. |
| CA-5.5 | Phase 5 section G: late-payment purchasing | Completed / pushed `bad66079` | Reconcile purchases with started original/addition payments that settle after cutoff; preserve exact paid demand and recovery. |
| CA-5.6 | Phase 5 section G: consolidated destination purchasing | Completed / pushed `49d75d3a` | Global exact totals with scoped destination breakdowns and existing per-location purchase writes. |
| CA-5.7 | Phase 5 section G: shortage Order review/cancellation | Completed / pushed `75e2105b` | Requirement-scoped affected Orders and existing coordinated cancellation; original receiving evidence and refund progress remain separate. |
| CA-5.8 | Phase 5 section G: supplier-exception resolution | Completed / pushed `19f409a1` | Internal audited resolution after accepted cancellation; protect remaining cycle demand and original receipt/refund evidence. |
| CA-5.9 | Phase 5 section G/final Scheduled journey | Active / acceptance defined | Real original/addition payment commitment through cutoff, exact purchase, receiving and packing without synthetic financial/operational SQL. |
| CA-5 | Phase 5, section G | Existing primitives / not verified as a journey | Cutoff exact paid demand, paid additions, purchase confirmation, receiving and cycle allocation, shortage resolution, picking/packing, inspected surplus; separate from Instant inventory. |
| CA-6 | Phase 6, sections E/F/H | Partial existing behavior / not verified as a phase | Preparation-stage Lalamove booking/state reconciliation, Scheduled-only manual fallback, packed handover, customer promises, full active membership/service-fee alignment across UI/Core/jobs while preserving required history. |
| CA-7 | Phase 7, sections E/F/I and final acceptance section 6 | Pending completion / not verified | Both customer modes, operator setup-to-delivery, manual fallback, failure/recovery and permission journeys; notifications, support, refunds, reorder, provider sandbox and activation readiness. Account, business-policy and operational inputs tracked separately. |

Coverage rule: every applicable criterion in source-plan sections A-I, audit defects and final journeys must have an owning row/child and evidence or an explicit open gap before completion. The broad mapping above is not proof that a criterion was individually audited. Existing historical implementation is reused where sound; no earlier phase is silently accepted or dropped.

## Verified implementation anchors

These are inherited evidence summaries for the named slices. Detailed historical commands/logs remain in the archived checkpoint and referenced artifacts; missing command provenance must be recovered before relying on an acceptance claim. They are not checks executed during continuation planning.

| Commit | Verified scope and evidence limits |
| --- | --- |
| `b8e32b2` | Cancellation/paid acceptance and complete catalog/variant/price/local activation transactions. Aggregate then passed; focused real Worker/D1 and selected browser journeys. Full commerce/provider acceptance remained open. |
| `f7f17dc` | Guarded normal variant editing; 22 focused Core tests, 2 Web editor tests, desktop/mobile browser journeys. |
| `d14de2c` | Promotion create/edit/status/grant complete effects and original retries; 25 Core tests and 5 browser journeys, affected Web/contracts/static/build checks. |
| `762a18e` | Retired membership eligibility removed from active promotion evaluation; retained unsupported conditions fail closed, reachable no-subscription Instant quote; 52 focused tests. Wider membership UI/jobs remain unfinished. |
| `f6f8c88` | Five canonical promotion benefits, caps/limits, shared integer preview calculation and retained migration 0085. Last full `pnpm check`: Core 1433/187 files, Web 398/99, contracts 68/19, shared/harness/migrations/lint/types/builds passed. Subsequent focused benefit/browser checks are recorded in history. This aggregate predates later preview/audience/media changes. |
| `e50ef9a` | Named customer selection, customer-aware eligibility preview and guarded grants. 66 focused Core tests, Web 400/100, contracts 68/19, static checks/builds and 9 browser journeys. |
| `012b5db` | Five controlled audience types, Global read/write RPCs, normal draft editor, strict checkout parsing, atomic replacement/receipt and named references. 99 Core tests across 7 files, then 13 audience tests after added privacy/local-scope coverage; Web 400/100, contracts 68/19, static checks/builds/bindings and 9 zero-retry browser journeys. Counts overlap and must not be added. No schema change in this slice. |

Other already implemented setup/category/product/R2 and financial repairs are recorded in history; their absence from this compact table is not a request to rebuild them.

Evidence directory: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a0822b-3c98-76a1-8c9e-a2d5e223a182`. Relevant log prefixes: `commerce-`, `catalog-variants-`, `variant-editor-`, `promotion-`, `promotion-benefits-`, `promotion-preview-`, `promotion-audience-`. Browser screenshots were inspected for the recorded slices. Local test-provider evidence is not provider sandbox acceptance.

## External inputs and handoff rules

Continuation-planning verification (documentation scope): `pnpm naming:check`, `pnpm harness:test` (26 passed), and `git diff --check` passed. Local links in this checkpoint and the continuation plan resolve. SHA-256 comparison confirms all nine pre-existing dirty files unchanged; the old checkpoint was archived byte-for-byte before replacement. No application suite was rerun for this documentation change. Commit/push status must be read from Git on resume.

Actual provider/account activation and credentials, operational addresses/service boundaries/hours/staffed promises, real catalog/prices, support/accounting/retention policy and any needed outbound/provider-operation authorization remain factual inputs, never invented values. Formal irreversible anonymization and official accounting remain policy-dependent under the source plan.

On resume and after each meaningful milestone, update this current record in place with the active ID, observed Git/files, exact new verification commands/results/revision, unresolved risks and next action. Preserve useful old evidence in history. Commit only intended verified work to main and push origin main; inspect actual push outcome. No broad application work was implemented or revalidated by creating the continuation plan.

## CA-5.9 final local acceptance — 9138860b

Completed and pushed 2026-09-09: `19f409a1..9138860b`. This is historical evidence, not an active task or next action. Both browser journeys and the aggregate passed; actual provider acceptance remains open. Final commit includes only the 20 intended files. Protected Discussion hash remains unchanged. The earlier failed runs below explain resolved test defects; they do not require repetition.

### Completed implementation and files

CA-5.9 adds a Customer-owned, bounded named addition search using the Order's saved location/currency and current positive exact-location prices. The existing addition write still owns final eligibility and pricing. Web validates responses, shows names and separate totals, and retains the same body/key after uncertain draft or payment replies. No migration or new business write.

Test infrastructure adds an opt-in local ingress forwarding Web requests and Core's existing verified mock webhook. A Core E2E-only entrypoint invokes the real registered minute/15-minute scheduler jobs at wall-clock time. Production entrypoints remain unchanged. The provider-hosted page is the only response fake; signed events and durable Order reactions use actual Core. Existing baseline catalog data is reused; setup changes use commands.

Intended application/contract files:

- `apps/core/src/orders/application/list-order-addition-options.ts` (new), `order-amendment.integration.test.ts`.
- `apps/core/src/entrypoint/orders-rpc.ts`, `apps/core/src/index.ts`.
- `packages/contracts/src/orders.ts`, `packages/contracts/src/core-service.ts`.
- `apps/web/app/api/commerce/orders/[order-id]/amendments/route.ts`.
- `apps/web/components/storefront/orders/amendment-flow.tsx` and its existing `.test.tsx`.
- `docs/architecture/API_CONTRACTS.md` (owning read/recovery semantics updated).

Intended test/config files:

- `apps/web/tests/scheduled-customer-journey.spec.ts` (new).
- `apps/web/tests/provider-gateway.ts`, `wrangler.provider-gateway.jsonc`, `provider-gateway-env.d.ts` (new; bindings generated by Wrangler).
- `apps/web/playwright.config.ts`.
- `apps/core/tests/browser-entrypoint.ts` (new), `apps/core/wrangler.e2e.jsonc`, `apps/core/tsconfig.json`.
- This checkpoint and its existing history file (owner-requested consolidation; prior bytes preserved in history).

Review corrections implemented: terminal payment failure is distinct from pending confirmation; malformed actions remain unknown, not processing. Renamed the expired-price test to match its actual evidence. Browser coverage now includes cross-customer RPC rejection, unchanged physical stock, desktop/mobile captures and a ten-second action timeout. The third run returned correct named options, but the test used an exact label-text selector that included option text; it now uses the verified accessible combobox role/name. These changes were accepted in CA-5.9.

### Executed verification

Scope: `19f409a1` plus CA-5.9 files above. Evidence root: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347`.

- `pnpm typecheck`: passed after including the Core E2E entrypoint. Its first included check exposed the production-generated ENVIRONMENT union; runtime string comparison retains the test-only guard. `pnpm lint`, `pnpm architecture:check`, `pnpm naming:check`: passed. `git diff --check`: passed before later documentation edits; rerun at final review.
- `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/orders/application/order-amendment.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: **16 tests / 2 files passed**, `scheduled-addition-options-focused.log`. Real Worker/D1 checks include ownership, availability, expired price, stock-independent suggestions, bounded search and existing amendment financial guards. These do not establish browser/provider acceptance.
- Browser command for all attempts: `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-customer-journey.spec.ts --retries=0` (set variables with PowerShell `$env:`).
- First attempt: **failed**, session 5137 exit 1, `scheduled-customer-journey-first.log`; verified payment event but test omitted the real payment-reaction scheduler before asserting an Order. Trace copied to `scheduled-journey-first/`. Fixed the test's missing scheduler step.
- Second attempt: **failed**, session 88130 exit 1, `scheduled-customer-journey-second.log`; original Order commitment succeeded, but test expected `orderId` where the list contract returns `id`. Corrected against the actual contract.
- Third attempt: **failed**, session 37004 exit 1, `scheduled-customer-journey-third.log`: correct GET options (HTTP 200) and an accessible named combobox were present; the wrong label selector waited the five-minute overall timeout. Inspected the trace response and accessibility snapshot; corrected the selector and bounded action waits. Subsequent typecheck/lint and the same focused Core command passed **16/2** (`scheduled-addition-options-focused-final.log`, session 75435). Fourth attempt (session 21111 exit 1, scheduled-customer-journey-fourth.log) stopped in 9.1 seconds at a test parser rejecting the cancellation status null while the request was still in flight; one test failed, mobile did not run. Named selection, original/addition commitments and both lost-reply retries had succeeded. Corrected the parser to the actual nullable contract and receiving assertion to its actual ungrouped numeric rendering. Managed migrations now use the same already-pinned Wrangler as fixture SQL and the runtime, with explicit E2E config/state; the previous startup spent most time in the newer migration CLI. This is a local test startup change, not a migration or deployment. Next command retains --max-failures=1.
- Formatter is `pnpm exec oxfmt --write <intended files>`. An earlier `prettier` attempt failed because it is not installed; no need to retry or install it. One patch mismatch was corrected without partial edits.
- Fifth browser attempt: **2 passed / 6.2 minutes**, session 12084 exit 0, `scheduled-customer-journey-fifth.log`; individual journeys 2.1/2.2 minutes include the real cutoff. Full command is the browser command above plus `--max-failures=1`. Desktop/mobile named-picker screenshots inspected; four captures copied to `scheduled-customer-journey/`. Original/addition commitment, signed-event deduplication, exact saved draft/payment retries, cross-customer rejection, accepted-cancellation exclusion, 1,500 g purchase/receipt/packing and unchanged physical stock passed. Actual provider acceptance remains open. The pinned migration command passed; no startup speedup is claimed from this run alone.
- Focused Web command `pnpm --filter @freshmarkets/web exec vitest run components/storefront/orders/amendment-flow.test.tsx test/app/orders "test/app/api/commerce/orders/[order-id]/amendments/route.test.ts"` initially had **1 failed / 7 passed**, `scheduled-addition-web.log`: old static test expected the removed SKU field. Updated it to the approved named search. `pnpm format:check` found only generated gateway-declaration whitespace; formatted after browser exit.
- Checkpoint consolidation: reduced 105,936 bytes to about 13 KB, retained all 23 CA IDs, validated links and protected Discussion hash, and verified the prior checkpoint bytes plus existing history prefix exactly. No new checkpoint file. Final aggregate passed; commit/push are next.

- Final gate: `pnpm check` **passed**, session 84645 exit 0, `scheduled-customer-journey-check.log`. Core **1579 tests / 194 files**, Web **403 / 101**, contracts **68 / 19**, shared suites, harness, migrations, format/conventions/architecture/readiness/lint/types and both builds passed. `pnpm --filter @freshmarkets/web check:vinext` passed **15 supported / 0 issues**, `scheduled-customer-journey-vinext.log`. `git diff --check` passed after final source review. No migration, deployment or actual provider acceptance is claimed.

## CA-6 completed local evidence through 93bb2ccc — archived 2026-09-09

These paragraphs preserve dated implementation and verification evidence only. Draft/process wording below describes those earlier runs; the active checkpoint owns current work. No historical next action is carried forward.

## Reconciled implementation and verification

CA-5.9 is complete locally at `9138860b`; its exact evidence is in history under **CA-5.9 final local acceptance — 9138860b**. Do not restart it. CA-6 first-booking milestone is complete locally and pushed as `bd4acef5`: automatic Instant booking after checking/final packing, durable retry before submission, one-attempt protection, current permission/readiness admission, and atomic provider identity/inbox/audit/receipt. ALLOCATING displays Finding rider without fabricated assignment. No schema change or actual provider acceptance.

Final first-booking validation on `9138860b` plus the committed application source: `pnpm check` passed (session 34363 exit 0), **1586 Core / 194 files, 403 Web / 101, 68 contracts / 19**, shared/harness/migrations/static checks and both builds; log `ca6-booking-check.log`. `pnpm --filter @freshmarkets/web check:vinext` passed, 15 supported/0 issues. Final focused command `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts src/delivery/application/request-provider-delivery.integration.test.ts` passed 19/2, `ca6-atomic-booking.log`, including lost audit/evidence rollback, unknown outcomes, revoked access and concurrent replay.

Final first-booking browser command: `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts --retries=0 --max-failures=1`. **2 passed / 2.1 minutes**, session 46217 exit 0, `ca6-instant-browser-atomic.log`, same application source as aggregate. Desktop/mobile screenshots inspected; mobile retains the horizontally scrollable queue. Evidence root: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347`. Local fake-provider acceptance only.

CA-6 manual-delivery milestone committed/pushed as `e9984290`: Scheduled manual ASSIGN/HAND_OVER/COMPLETE/FAIL command and domain policy, shared contract/RPC, typed queue decisions and ordinary Web controls. Existing attempt storage is reused; unknown cost keeps the financial evidence null, known terminal cost records charge/currency/variance. Job/stop, custody/Order progress, audit and frozen receipt share one guarded batch. Older fulfillment handover/completion is excluded during active manual custody. Failure records FAILED without fabricating return, retry, delivery or refund success; further recovery remains open.

Manual milestone files committed in `e9984290`: `apps/core/src/delivery/{domain/manual-delivery.ts,application/manage-manual-delivery.ts,application/list-delivery-dispatch.ts}`, `apps/core/src/admin/application/{operations-reads.ts,delivery-provider-operations.integration.test.ts}`, `apps/core/src/{index.ts,entrypoint/core-service-conformance.test.ts,operations/application/advance-fulfillment.ts,fulfillment/application/list-fulfillment-queue.ts}`, `packages/contracts/src/{admin-operations.ts,admin-operations.test.ts,core-service.ts}`, `apps/web/app/api/admin/manual-deliveries/route.ts`, `apps/web/components/admin/delivery/{manual-delivery-controls.tsx,external-delivery-queue.tsx}`. The existing `apps/web/tests/scheduled-customer-journey.spec.ts` now covers manual assignment before packing, handover, completion and same-body/key replay after a lost reply. API_CONTRACTS, STATE_MACHINES and DATA_MODEL are updated. Manual behavior has local acceptance below; no actual provider acceptance or deployment is claimed.

Manual draft validation: `pnpm check` **passed**, session 68554 exit 0, log `ca6-manual-check.log`: **1595 Core / 194 files, 403 Web / 101, 68 contracts / 19**, shared/harness/migrations/static checks and both builds. During the aggregate, final review added an atomic known-cost charge guard, clarified the Delivery queue heading, and extended two test assertions. Current `pnpm typecheck`, `pnpm format:check`, `pnpm lint` then passed. Follow-up `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts src/operations/application/advance-fulfillment.integration.test.ts` passed **19 tests / 1 matched file**, exit 0, `ca6-manual-final-core.log`; the second requested path did not match a file, so this is only the delivery-provider operations test result. The full gate already covered existing fulfillment suites. Initial financial-evidence trigger failure was fixed by keeping unknown evidence all-null; a later rollback assertion was corrected to count only its exact command key.

Managed browser command `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-customer-journey.spec.ts --retries=0 --max-failures=1` **passed 2 desktop/mobile journeys / 6.5 minutes**, session 54140 exit 0, `ca6-manual-browser.log`. Application source remained fixed during the run. Both prove actual local paid checkout/addition/cancellation/receiving/packing/manual assignment/handover/completion and same-body/key recovery from a lost completion reply. Provider evidence is fake/local only. Protected Discussion SHA256 still matches.

Screenshot review corrected the misleading Not booked label for manual attempts and replaced a mobile capture that missed the controls. Final command above with `--grep '390px'` **passed 1 journey / 4.1 minutes**, session 38521 exit 0, `ca6-manual-mobile-final.log`. The inspected final mobile full-page screenshot shows the manual custody state and usable completion/failure controls in the existing horizontally scrolling queue. Copied screenshots are under `ca6-manual-delivery/` in the evidence root; desktop capture predates the label-only correction. Application source stayed fixed during each browser run. Web typecheck and vinext (15 supported/0 issues) passed after the wording correction.

Final boundary review added the standard authenticated-request envelope validation to the new Service Binding method. `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/entrypoint/core-service-conformance.test.ts` passed **4 tests / 1 file**, including malformed-envelope rejection, followed by Core typecheck and `git diff --check`. This envelope guard is the only application change after final mobile acceptance; valid manual command logic is unchanged. Manual verification is finished; current process state is recorded below. The reviewed 20-file milestone is committed/pushed as `e9984290`. Scheduled received-goods booking readiness and all other CA-6 obligations remain open.
### Completed milestone — Scheduled booking readiness (CA-6)

Committed/pushed as `80594ee8`; local acceptance scope is recorded below. Acceptance: future pickup requires preparation plus received/checked exact paid goods; immediate pickup requires PACKED; current goods/time/scope are rechecked before submission; the saved delivery-window end bounds pickup; no rejected booking creates an attempt or success receipt. Queue decisions and a reachable future-booking journey must agree. Booking never reserves/consumes another quantity or implies packing/rider acceptance.

Current draft uses `fulfillment/application/scheduled-delivery-readiness.ts` as one correlated received-goods predicate for the queue, booking read and conditional admission. Paid additions are included; packed Orders use their immutable cycle packing movements rather than needing their consumed goods again in the remaining balance. It retains the operator's existing pickup-time input. `book-order-delivery.ts` uses the saved window end with the retained saved-date fallback. No schema or new operator step.

Files committed in `80594ee8`: new `apps/core/src/fulfillment/application/scheduled-delivery-readiness.ts`; changed `apps/core/src/delivery/application/{book-order-delivery.ts,request-provider-delivery.ts,list-delivery-dispatch.ts}`, `apps/core/src/admin/application/{operations-reads.ts,delivery-provider-operations.integration.test.ts}`, `packages/contracts/src/{admin-operations.ts,admin-operations.test.ts}`, `apps/web/components/admin/delivery/{external-delivery-booking.tsx,external-delivery-queue.tsx}`, `apps/web/tests/scheduled-customer-journey.spec.ts`, API_CONTRACTS and STATE_MACHINES. The existing connected browser journey now also buys a separate courier Order, receives its goods, starts preparation, books future pickup later within the saved window, then packs; it retains the prior manual journey.

Working-tree validation: `pnpm typecheck`, `pnpm lint`, `pnpm architecture:check`, `pnpm format:check` passed. `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts src/delivery/application/request-provider-delivery.integration.test.ts` passed **31 tests / 2 files**, `ca6-scheduled-readiness-core.log`. Initial failures were old Scheduled fixtures without preparation/receipts and a test-only trigger affecting D1's reported claim count; fixtures now use receiving/preparation commands and the race test interleaves after the actual claim without altering its result. No application success was fabricated or assertion weakened.

Final affected gates passed on `e9984290` plus the complete Scheduled-readiness draft: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/delivery src/fulfillment src/operations/application src/admin/application/delivery-provider-operations.integration.test.ts src/entrypoint/core-service-conformance.test.ts src/scheduling/run-scheduled-jobs.integration.test.ts` **107 tests / 18 files**, session 89685 exit 0, `ca6-readiness-affected-core.log`; Web **403/101** and contracts **68/19**, session 79737 exit 0, `ca6-readiness-web.log` / `ca6-readiness-contracts.log`. `pnpm -r build` passed both builds, session 24666 exit 0, `ca6-readiness-builds.log`. Types, lint, format, architecture/readiness/naming and diff checks passed. No schema change. These are risk-selected milestone checks; the full Phase 6 aggregate remains due at phase completion.

The extended desktop/mobile journey **passed 2 tests / 6.9 minutes**, session 82553 exit 0, `ca6-readiness-browser.log`: `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-customer-journey.spec.ts --retries=0 --max-failures=1`. All Core suites exited before the stack. Source stayed fixed during acceptance. It proves preparation-gated future booking within the saved window before packing, Finding rider independent of packing, subsequent packing and the prior manual completion/replay flow. Inspected desktop/mobile captures are copied under `ca6-scheduled-readiness/`; the existing table scrolls horizontally on mobile. No actual provider acceptance.

Final visual review corrected only the unavailable reason for cancelling/otherwise ineligible Orders so it does not tell staff to start preparation. Core typecheck, lint, formatting and diff checks passed after this read-model wording correction; booking eligibility/admission logic is unchanged from the browser scope. Protected Discussion SHA256 still matches. No verification process remains active. The reviewed 14-file milestone is committed/pushed as `80594ee8`; the other CA-6 obligations remain open.
### Completed milestone — full Order weight (CA-6), d565a640

Working tree based on `80594ee8`: quote and fulfillment-option validation, payment admission, full Order/addition commitment guards and package cap use existing line grams and payment-intent claims. No new schema, packaging inputs or customer flow. Partial files under `apps/core/src`: checkout/application/{create-checkout-quote,instant-quote,instant-quote.integration.test,list-fulfillment-options}; fulfillment/{domain/delivery-package,domain/delivery-package.test,application/order-delivery-weight}; orders/application/{create-order-amendment,apply-amendment-payment-reaction,apply-checkout-payment-reaction,apply-checkout-payment-reaction.integration.test,order-amendment.integration.test}; payments/application/{create-payment,create-checkout-payment-intent,create-amendment-payment-intent}, all `.ts`. Also `apps/web/tests/scheduled-customer-journey.spec.ts`, API_CONTRACTS and this checkpoint.

Verification on that working-tree scope:
- `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/checkout src/orders/application src/payments/application src/fulfillment`: 363 passed, 1 old quantity expectation failed (`ca6-weight-affected.log`, session 16405 exited). After correction, `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/checkout/application/instant-quote.integration.test.ts src/orders/application/apply-checkout-payment-reaction.integration.test.ts`: **49/2 passed**, `ca6-weight-final-core.log`.
- After browser-discovered availability correction and added paid-addition overweight rollback assertion: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/checkout/application/list-fulfillment-options.integration.test.ts src/orders/application/order-amendment.integration.test.ts` **17/2 passed** (`ca6-weight-options-core.log`). Includes no partial demand/status on failed overweight commitment, original replay, concurrent retained drafts and unknown payment claims. Current command still permits only one active draft; competing draft fixtures do not add a product flow.
- Types, lint, formatting, architecture/readiness/naming pass. Protected Discussion hash still matches.
- Earlier iteration failures: generic payment admission wrongly required quote fixtures in isolated payment recovery; corrected to existing trusted checkoutVersion admission and public checkout validation. Old 50 kg success expectation changed to approved 20 kg boundary. A cleanup script failed parsing without edits, then was applied directly.
- First browser run failed because overweight delivery was disabled with generic FEE_UNAVAILABLE before quote submission (`ca6-weight-browser.log`, session 53278 exited). Core option query now returns the explicit 20 kg validation message through the existing error contract. Test checks that visible message, then normal purchase. No extra contract, UI state or input added.

Final managed browser command `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-customer-journey.spec.ts --retries=0 --max-failures=1` **passed 2 desktop/mobile journeys / 6.7 minutes**, session 82409 exit 0, `ca6-weight-browser-final.log`. It proves visible overweight rejection, successful corrected cart/checkout, paid addition, received-goods future booking and manual handover/completion/replay. Application source stayed fixed. `pnpm --filter @freshmarkets/core build` passed deployment dry run (`ca6-weight-core-build.log`); managed browser setup built Web. No deployment or actual provider acceptance. No verification process remains active. These are milestone checks; the full Phase 6 aggregate and remaining ledger obligations are still due.

Completed CA-6 milestone (`c211b83c`): retire active membership enrollment/marketing/pricing/trial reminders and the general checkout minimum. Acceptance: authenticated retired RPC calls cannot create subscriptions, payment intents or recurring authorization; account/cart show ordinary shopping; both checkout modes accept otherwise eligible small nonempty carts. Keep promotion conditions, historical finance, existing cancellation and provider/payment reconciliation. New quotes already have zero service fee/null fee configuration; preserved, not rebuilt. Actual provider billing is provider-owned; disabling local enrollment does not cancel an existing provider subscription.

Working-tree edits under `apps/core/src`: entrypoint/{membership-rpc,payments-rpc}, retired-membership-rpc.integration.test, index, admin/application/{get-admin-context,admin-context.integration.test}, scheduling/job-registry, notifications/application/{project-domain-notifications,deliver-notifications,notification-outbox.integration.test}, checkout/application/{resolve-checkout-decision,resolve-checkout-decision.integration.test,create-checkout-quote,instant-quote,instant-quote.integration.test,evaluate-checkout}, orders/application/apply-checkout-payment-reaction.integration.test (all `.ts`). Web: app/account/page.tsx, app/account/membership/payment/page.tsx, app/admin/commerce-configuration/page.tsx, app/cart/page.tsx, components/storefront/storefront-shell.tsx, test/app/account/page.test.tsx, tests/instant-auto-booking.spec.ts. No storage migration or data deletion.

`pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/entrypoint src/admin/application/admin-context.integration.test.ts src/scheduling src/notifications` passed **92 tests / 19 files**, `ca6-retirement-core.log`, session 41177 exit 0. These cover retired RPC effects and retained scheduled cancellation/recovery. First typecheck caught a test request using setupIntentId instead of authorizationId; corrected, then all workspace typechecks passed. Web suite passed (`ca6-retirement-web.log`). Checkout/no-minimum gate passed **99/11**, session 39576 exit 0 (`ca6-no-minimum-core.log`). Web passed **402/101**. Architecture rejected SQL in the entrypoint test and then application-to-transport imports; the same test now lives at `apps/core/src/retired-membership-rpc.integration.test.ts`, with the original entrypoint test restored. Architecture/format/type/lint checks pass; relocated test passed (`ca6-retirement-boundary-final.log`). Managed command `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts --retries=0 --max-failures=1` **passed 2 desktop/mobile journeys / 2.2 minutes**, session 43194 exit 0 (`ca6-retirement-browser.log`). Real local authenticated enrollment rejection, ordinary Account, a 200-minor-unit merchandise basket, checkout/payment reaction, preparation and automatic booking passed. No SQL commerce setup or actual provider acceptance claimed. Application source stayed fixed during browser execution.

After browser success, final retirement review added cancellation of retained unsent TRIAL_ENDING/FIRST_PAID_RENEWAL_UPCOMING outbox rows, preserving unknown sends and prior evidence. This is the only application change after browser acceptance. `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/notifications src/retired-membership-rpc.integration.test.ts src/checkout/application/resolve-checkout-decision.integration.test.ts` passed **48/8**, session 8562 exit 0 (`ca6-retirement-final-core.log`). `pnpm -r build` passed both builds, session 95390 exit 0 (`ca6-retirement-builds.log`). Final types/lint/format/architecture/readiness/naming/diff checks passed. Protected Discussion hash matches. No verification process remains active. No schema/data deletion, actual provider acceptance or deployment. Phase aggregate remains owed after the other Phase 6 criteria are closed.

Delivery-recovery reconciliation after that commit: only UNASSIGNED/RETRY_SCHEDULED jobs are admitted by booking/manual assignment; no application command currently transitions FAILED to RETRY_SCHEDULED. Plan H authorizes explicit no-rider retry and safe Scheduled fallback after definite closure. Post-handover failure retains HANDED_OFF/OUT_FOR_DELIVERY custody (STATE_MACHINES manual section and API_CONTRACTS manageManualDelivery); those sources explicitly say no return/retry/refund is inferred. The owner has now approved the customer-return handling recorded below. Transfer returns remain separate. Retry/return commands have not yet been implemented.

Owner clarification resolved on 2026-09-09: inspect physically returned groceries; retry the same Order if suitable and the customer agrees; never automatically credit returned goods to available stock. Customer-caused missed delivery is **not automatically refundable**. Review attempt/contact evidence; courier failure alone is not proof of customer fault. Existing FreshMarkets/courier-fault remedies remain. No automatic redelivery charge; any proposed charge requires prior customer agreement. PRODUCT's explicit failed-delivery supplement owns the decision; protected Discussion remains unchanged. The previous pending question is closed.

Completed CA-6 disclosure milestone `796f9765` (tested on c211b83c plus its six-file working-tree scope): approved missed-delivery rule recorded in PRODUCT/STATE_MACHINES and shown in the payment review before proceeding to payment. Existing failure command remains unchanged. Its Worker/D1 regression now checks both pre-handover and post-handover failure: no cancellation/refund records, unchanged fulfillment custody and retained handover/actual-cost evidence. Packing is a seeded boundary in this regression; handover and failure are real commands. `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts` passed 23/1 (`ca6-missed-delivery-core.log`). Types, lint, naming and diff checks passed. Managed command `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts --retries=0 --max-failures=1` passed 2 desktop/mobile journeys / 2.2 minutes, session 46188 exit 0 (`ca6-missed-delivery-browser.log`). Checkout policy screenshots were inspected and copied to the evidence root as ca6-checkout-delivery-policy-{1440,390}.png. The notice is readable before payment; existing checkout/payment/preparation/automatic booking still passes. Managed setup built Web. No concurrent Core suite or source edits during browser acceptance. Protected Discussion hash still matches; no verification process remains active. No actual provider acceptance or deployment. This milestone completes the disclosure and existing no-automatic-refund regression, not the CA-6 task or Phase 6 aggregate.

Owner decisions closed: Instant orders cannot have items added or quantities changed after payment; Scheduled additions and existing cancellation/refund rules remain unchanged. Admin configures real areas/hours/promises; sample values suffice for local tests. Records must be retained indefinitely, without automatic record deletion. PRODUCT owns these decisions; no Instant-addition deadline or retention-duration decision remains pending. Reconciled Core create-order-amendment already rejects non-Scheduled additions. Documentation-only update: PRODUCT, E2E plan and this checkpoint; no application behavior or retention acceptance claimed. Initial PowerShell edit failed parsing before any writes; direct patch/replacement succeeded. Verification: `pnpm naming:check` and `git diff --check` passed on this documentation working-tree scope; no application tests required. CA-6 and all unfinished acceptance remain open.

Completed CA-6 manual-fallback milestone 695012de, verified on af75229a plus its working-tree scope: allow ordinary Scheduled manual assignment after definite pre-handover courier closure. Exact plan: docs/product/COMMERCE_ALIGNMENT_E2E_PLAN.md, Phase 6 — Delivery and customer alignment, section H. Acceptance: the real courier-cancel path leaves FAILED, the existing queue then offers manual assignment, only one new attempt wins, old evidence and custody/payment remain intact, and Instant/unknown/returned/post-handover work stays blocked. No new UI, operator step, contract shape or storage migration. The existing atomic command rechecks current actor/scope, latest attempt/version/handover, Order/fulfillment versions, pending cancellations and every required effect. Manual domain policy and delivery transition vocabulary changed; read model and command already share that policy.

Partial files: apps/core/src/delivery/domain/manual-delivery{,.test}.ts, commerce/state-machines{,.test}.ts, admin/application/delivery-provider-operations.integration.test.ts, commerce-flow.integration.test.ts; apps/web/tests/scheduled-customer-journey.spec.ts; API_CONTRACTS, STATE_MACHINES and this checkpoint. The commerce-flow setup wrongly required retired membership trial success; removed only that obsolete call. Initial fallback test used EXTERNAL_PROVIDER instead of persisted EXTERNAL; corrected. Broader first run found the obsolete trial and transition expectation; both corrected without weakening business assertions. Final command: pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts src/delivery/domain/manual-delivery.test.ts src/commerce — 55 tests/8 files passed, session 12628 exit 0, ca6-fallback-core-verified.log. Types/lint/architecture/readiness/diff passed before the final test-only expectation/setup corrections. Managed command E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/scheduled-customer-journey.spec.ts --retries=0 --max-failures=1 passed 2 desktop/mobile journeys / 6.4 minutes, session 65352 exit 0, ca6-fallback-browser.log. It exercises payment/receiving/preparation/booking/cancellation through real commands with explicit fake provider evidence, then manual assignment of that same Order; prior addition/manual-completion/replay coverage remains. Screenshots inspected and copied as ca6-canceled-courier-fallback-{1440,390}.png; mobile retains the existing horizontal table scroll. Final types/lint/format/naming/diff passed. No application edits during acceptance or concurrent Core suite; managed setup built Web. No verification process remains active. No schema change, actual provider acceptance or deployment. The full Phase 6 aggregate remains owed.

Current CA-6 courier-retry draft on 695012de: use the existing booking action for explicit retry after definite pre-handover closure. Shared pre-handover-retry.ts SQL facts serve read model, command validation and atomic admission. The new durable attempt plus FAILED-to-RETRY_SCHEDULED job/stop updates share a guarded D1 batch before provider submission; latest closed history, unchanged original promise/charges, current scope/readiness and unknown-attempt exclusion remain enforced. No schema or new endpoint. Queue contract renamed scheduledPickup to courierPickup because it now covers both modes; all typed callers updated together. A definitely rejected create can still be explicitly retried; automatic Instant booking never creates replacement attempts.

Partial files: delivery/application/{pre-handover-retry,book-order-delivery,request-provider-delivery,list-delivery-dispatch}.ts; admin/application/{operations-reads,delivery-provider-operations.integration.test}.ts; Web delivery/{external-delivery-booking,external-delivery-queue}.tsx and tests/{instant-auto-booking,scheduled-customer-journey}.spec.ts; contracts admin-operations{,.test}.ts; API_CONTRACTS, STATE_MACHINES and checkpoint. Acceptance: both modes retry via real prior preparation/booking/cancel commands, retain old attempt/merchant identity, one concurrent winner, frozen booking replay at tested state, zero partial retry admission on omitted stop update, expired-promise rejection and unknown-outcome exclusion. First focused Core gate 35/2 passed (ca6-courier-retry-core.log); affected gate 85/13 passed (ca6-courier-retry-affected.log, session 74518 exit 0): pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts src/delivery src/entrypoint/core-service-conformance.test.ts. Contracts 68/19 and Web 402/101 passed; Web log ca6-courier-retry-web.log. Types/lint/architecture/readiness/format passed; final UI wording changed afterward. Both builds passed (session 31630 exit 0, ca6-courier-retry-builds.log). Combined managed browser command for instant-auto-booking.spec.ts and scheduled-customer-journey.spec.ts exited 1 (session 85654, ca6-courier-retry-browser.log): both Instant desktop/mobile retry journeys passed; Scheduled then failed at SWITCH_MODE with Resolve or explicitly protect committed work in the current mode before switching, before its retry actions. GD-D15 normal mode-change acceptance remains open with this concrete reproduction. Running Scheduled separately can verify its retry flow, but cannot close this shared-state mode-change failure. Protected Discussion hash still matches. No actual provider acceptance.

Earlier acceptance gap identified during retry review: operator-created PENDING/RETRY_REQUIRED attempts lack a reachable bounded resume path after interruption before provider submission. The existing automatic initial Instant path only resumes its own intent. Such saved operator work remains excluded from replacement, but recovery must be completed before declaring delivery acceptance finished. Post-handover returned goods and revised customer promises also remain unfinished. This is an implementation gap, not a new business question.

Recovery and GD-D15 corrections now implemented in this same CA-6 draft: expire-unsubmitted-bookings.ts, invoked by reconcile-provider-observations.ts, closes only stale PENDING/RETRY_REQUIRED attempts with no provider identity after five minutes, keeping all records. Job/stop/command failure plus audit commit atomically; a concurrent provider claim wins or expiry wins, never both. Missing-audit errors propagate after rollback; only proven concurrent version changes are skipped. The queue no longer asks for a nonexistent courier order number when an attempt is definitely unsubmitted. Worker/D1 recovery gate passed 106/15: pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts src/delivery src/scheduling (session 8251 exit 0, ca6-courier-recovery-core.log). It reaches PENDING by interrupting the actual booking claim, tests the five-minute boundary, omitted-audit rollback/concurrent expiry/replay, then ordinary retry; potentially submitted states remain untouched. This closes the newly identified unsubmitted-intent recovery gap within the tested scope.

GD-D15: global-commerce-configuration.ts no longer blocks switching solely because earlier-mode Orders are unfinished. Pause/permission/version/readiness and started-payment protection remain; switch changes new commerce, never old Order terms. Added both-mode retained-Order/replay tests; affected commerce/admin config tests passed 12/2 (ca6-mode-change-core.log). A test-only widened string type was corrected; all types now pass. STATE_MACHINES also removes its stale Instant-addition deadline proposal in favor of the already approved prohibition. Both builds passed (session 31738 exit 0, ca6-recovery-mode-builds.log). These files extend the partial-file list above.

Scheduled-only browser rerun exited 1 (session 62910, ca6-courier-retry-scheduled-browser.log): desktop retry/fallback passed; mobile timed out waiting for Cancel after navigating to Delivery. Trace inspection found the queue still loading and its final GET unresolved for the 10-second timeout; preceding delivery GETs returned 200 in 173–409 ms. No loaded row with a wrong action was observed. Preserve this failure; final combined browser rerun will verify current source without relaxing timeouts or retries. Trace image: ca6-courier-retry-mobile-failure.jpeg. Final combined browser command E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts tests/scheduled-customer-journey.spec.ts --retries=0 --max-failures=1 passed 4 desktop/mobile journeys / 7.0 minutes, session 59564 exit 0, ca6-courier-recovery-final-browser.log. This includes retry in both modes, Scheduled manual fallback, and earlier Instant courier work after switching new commerce to Scheduled. Source stayed fixed; no Core suite overlap. The earlier transient queue timeout remains recorded without claiming a diagnosed application fix. Final Instant desktop/mobile and Scheduled mobile screenshots inspected; four final captures copied as ca6-final-*.png under the evidence root. No verification process remains active. No actual provider acceptance or deployment.

## Preserved CA-6 return and signed-callback milestone evidence — 2026-09-09

Historical snapshot; current scope and next action are only in COMMERCE_ALIGNMENT_EXECUTION.md. Return implementation committed/pushed as 18fc6fa1. Signed-callback test scope was still uncommitted when this evidence moved.

## Current implementation and acceptance

**Active scope: signed local delivery completion on 18fc6fa1**, still CA-6 / Phase 6 / section H. Return inspection below is committed and locally verified. The same agreement flow records separate inspection facts and restores packed/ready custody only after explicit confirmation. Normal courier retry and Scheduled manual assignment then use that exact inspection; later returns require another inspection. No restock, repeated packing, refund or extra charge follows. Current partial files and the next action are below.

Courier retry/recovery and normal mode switching are committed/pushed as **93bb2ccc**. Combined Instant/Scheduled browser acceptance passed **4 desktop/mobile journeys / 7.0 minutes**, session 59564 exit 0, `ca6-courier-recovery-final-browser.log`; tested `695012de` plus that committed scope. It includes retained Instant work after switching new commerce to Scheduled. Earlier mobile queue-loading timeout remains in history; no diagnosed fix was claimed. No actual provider acceptance.

Completed customer-agreed time milestone **37c7fe46**, tested on **93bb2ccc** plus its committed scope: customer-agreed revised delivery time before handover. One new immutable agreement table is necessary to preserve agreement separately from the original commercial snapshot. Core changes only the operational deadline plus agreement/audit/frozen receipt; no booking, charge, refund, stock or custody change. Existing booking uses the latest agreed deadline in both modes. Customer detail shows original and agreed time separately. Unknown/active attempts, post-handover work, invalid time, stale version and unauthorized scope remain rejected. Physical-return inspection and delivery notifications remain unfinished.

Files committed in 37c7fe46: `apps/core/migrations/0091_delivery_promise_revisions.sql`; Core delivery/application `{revise-delivery-promise,book-order-delivery,request-provider-delivery,list-delivery-dispatch}.ts`, admin/application `{operations-reads,delivery-provider-operations.integration.test}.ts`, orders/application/get-customer-order-detail.ts, index.ts and entrypoint/core-service-conformance.test.ts; contracts `{admin-operations,admin-operations.test,core-service,orders}.ts`; Web API admin/delivery-promises/route.ts, delivery/{delivery-promise-form,external-delivery-queue}.tsx, app/orders/[order-id]/page.tsx and tests/instant-auto-booking.spec.ts; API_CONTRACTS, DATA_MODEL, STATE_MACHINES and these existing checkpoint/history files. No protected/unrelated files included.

### Previous verified milestone — 37c7fe46

Evidence root: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347`.

- `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts src/entrypoint/core-service-conformance.test.ts`: **37/2 passed**, session 18459 exit 0, `ca6-promise-core.log`.
- `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts src/delivery src/entrypoint/core-service-conformance.test.ts src/orders/application/get-customer-order-detail.integration.test.ts`: **97/14 passed**, session 4745 exit 0, `ca6-promise-affected.log`. Includes real prepare/book/cancel/revise/retry in both modes, wrong-scope/access/invalid-time rejection, one competing winner, frozen replay after expiry, changed-key conflict, omitted-audit rollback and immutable agreement retention. The Instant fixture now supplies its original promise at creation instead of omitting it.
- `pnpm --filter @freshmarkets/contracts test`: **68/19 passed**, `ca6-promise-contracts.log`; `pnpm --filter @freshmarkets/web test`: **402/101 passed**, `ca6-promise-web.log`, session 68342 exit 0.
- `pnpm migration:check` passed fresh application and the verifier's populated retained upgrade scenarios (`ca6-promise-migrations.log`). Worker/D1 tests apply current migrations. No shared/remote database changed.
- `pnpm typecheck`, `pnpm lint`, `pnpm architecture:check`, `pnpm readiness:check`, `pnpm format:check`, `git diff --check` passed. First types failed because the contract fixture lacked the new decision field; corrected, final types pass.
- `pnpm --filter @freshmarkets/core build` and `pnpm --filter @freshmarkets/web build` passed, session 81042 exit 0, `ca6-promise-{core,web}-build.log`.
- Managed command `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts --retries=0 --max-failures=1` passed **2 desktop/mobile journeys / 2.3 minutes**, session **55869 exit 0**, `ca6-promise-browser.log`. It records agreement in Admin, checks exact replay, shows original/agreed time to the customer, then retries the courier. Core suites were finished and application source stayed fixed during that run.

Final review corrected the customer description-list grouping and prevented deadline-only updates from manufacturing another DELIVERY_FAILED notification: preserve the delivery-status timestamp while the immutable agreement stores its own recorded time. Added a real projection regression. Its first run failed only because the test used notification_type instead of the persisted event_type; corrected. Focused command `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts` passed **33/1**, `ca6-promise-final-core-verified.log`; types/lint/format passed, session 70838 exit 0. Final Core build passed (`ca6-promise-final-core-build.log`). Final same-command desktop/mobile browser run passed **2 journeys / 2.3 minutes**, **61704 exit 0**, `ca6-promise-final-browser.log`; Core suites were closed and application source stayed fixed. Final customer desktop/mobile screenshots inspected and copied to the evidence root as ca6-final-agreed-delivery-time-{1440,390}.png. Managed setup built Web from final source; no verification process remains active. Agreement notification delivery itself remains a Phase 7 obligation; no outbound message was sent.

The full Phase 6 aggregate and actual provider acceptance remain owed. Completed milestone evidence (including failures and exact commands) was moved intact into the existing history file; no new checkpoint was created.

### Current return verification

`pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/delivery-provider-operations.integration.test.ts src/delivery/domain/manual-delivery.test.ts src/commerce/state-machines.test.ts` passed **49/3**, session 74076 exit 0, `ca6-return-core.log`. It covers real Scheduled receipt/packing/manual handover/failure/inspection/reassignment and Instant external pickup/return/inspection/rebooking. Instant packing is a seeded boundary here; earlier browser acceptance proves its actual paid/reservation/packing path. Both prove omitted-custody rollback, one concurrent inspection, no stock/refund effects, frozen replay and no use of an old inspection after another return. Queue shows Returned and inspected from Core's timestamp. All types pass after that presentation change.

Initial `pnpm check` **failed**, **8412 exit 1**, `ca6-return-check.log`: **1622 Core passed / 7 failed across 196 files**; all seven failures came from two tests still using retired membership entrypoints (customer-boundary used subscription eligibility; reservation-demand still enrolled a trial). Static gates, migrations, types, contracts and Web passed; builds were not reached. Updated only these obsolete test prerequisites: customer-boundary now uses getCart and proves a forged customer ID cannot change the session-owned cart; inventory fixture omits trial enrollment while retaining reservation/demand/race assertions. No active product behavior was restored. Focused command `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/customer-boundary.integration.test.ts src/inventory/reservation-demand.integration.test.ts src/admin/application/delivery-provider-operations.integration.test.ts` passed **46/3**, session 6153 exit 0, `ca6-return-retirement-regressions.log`. Added a dispatch lookup index to the new agreement table for its repeated inspection queries; no business constraint added. Final aggregate **passed**, **92194 exit 0**, `ca6-return-check-verified.log`: **1629 Core/196 files, 402 Web/101, 68 contracts/19**, shared packages, harness, migrations, static checks and both builds. Before browser acceptance, its new redelivery-assignee locator was corrected to the existing Person delivering label; this is test-only and root check does not run Playwright. The expanded Scheduled browser journey now performs failure, inspection/agreement, manual redelivery and existing lost-completion-reply replay, and checks unchanged physical stock plus the customer promise display. Managed combined browser command `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts tests/scheduled-customer-journey.spec.ts --retries=0 --max-failures=1` failed as **75932 exit 1**, `ca6-return-browser.log`: both Instant journeys passed; Scheduled desktop completed return inspection but its new stock assertion compared the full second response (including location/version) with the first response parsed to only three quantities. The quantities matched. Corrected the second parse to the identical quantity schema, retaining exact equality. No application change; Scheduled redelivery/completion and mobile remain unverified until rerun.

## External blockers and one next action

Actual provider/account activation and credentials, real pickup addresses/service boundaries/hours/staffed promises, catalog/prices, accounting facts and live-provider/outbound authorization remain release inputs. Local implementation uses sample configuration. No current business decision blocks this draft. Official accounting and irreversible anonymization remain policy-dependent. Records are retained indefinitely; paid Instant items/quantities cannot change.

**Return acceptance:** the existing agreement form/command requires explicit confirmation that all groceries physically returned and remain suitable, with a separate inspection note and customer agreement. Preserve original attempt/custody/charges and stock consumption; atomically restore packed/ready custody so the normal courier retry or Scheduled manual assignment can proceed. Do not automatically restock, refund or charge. Extend immutable agreement evidence with explicit inspection facts; closed-attempt inspection must match the latest attempt and cannot authorize a later uninspected return. Current uncommitted scope on 37c7fe46: optional explicit returnInspection in the existing agreement request/form, separate immutable inspection note/time, atomic packed/ready custody restoration, and retry/manual eligibility tied to that exact inspected closed attempt. Helper renamed pre-handover-retry.ts to delivery-retry-readiness.ts to reflect inspected returns. Ordinary MARK_PACKED now explicitly requires PACKING, so the reviewed HANDED_OFF -> PACKED transition is unavailable through ordinary packing. Partial files: migration 0091; delivery/application/{delivery-retry-readiness,revise-delivery-promise,book-order-delivery,request-provider-delivery,list-delivery-dispatch,manage-manual-delivery}.ts, delivery/domain/manual-delivery.ts, commerce/state-machines.ts, operations/application/advance-fulfillment.ts, admin/application/operations-reads.ts, contracts admin-operations{,.test}.ts, Web API delivery-promises/route.ts and delivery-promise-form.tsx. Migration 0091 was introduced only in the prior local milestone; extend this pre-launch baseline and recreate only the established disposable DB during managed acceptance. No retained/shared deployment of 0091 has been performed in this task. A CRLF-sensitive UI replacement failed before writing; apply_patch succeeded. Formatting and all workspace types pass (ca6-return-types.log). Final combined managed browser run passed 4 desktop/mobile journeys / 7.2 minutes, session 62642 exit 0, ca6-return-browser-verified.log, on 37c7fe46 plus this intended return scope and the corrected test-only response schema. Application source stayed fixed; no process remains. Desktop/mobile inspection screenshots reviewed and copied as ca6-final-return-inspection-{1440,390}.png; mobile uses the existing horizontally scrollable queue. Final aggregate already passed as recorded above. Return scope reviewed for commit/push. Additional intended files are customer-boundary.integration.test.ts and inventory/reservation-demand.integration.test.ts for the retired test prerequisites, state-machines.test.ts, Scheduled browser test and manual-delivery-controls.tsx. Preserve earlier obligations. Same CA-6 / Phase 6 / section H; all remaining ledger obligations stay open.

**One next action:** execute the extended Instant desktop/mobile journey, inspect delivered screenshots and commit/push the verified test boundary. Current draft files: apps/core/tests/browser-entrypoint.ts, apps/web/tests/{provider-gateway.ts,instant-auto-booking.spec.ts}. Production application unchanged from 18fc6fa1. The test entrypoint supplies synthetic credentials to the production signature/inbox/application handler; it never reads developer provider secrets. Browser asserts invalid-signature rejection, real assignment/pickup/completion effects, exact duplicate preservation and customer status. Web/Core typechecks and formatting pass. Local completion is not provider/account acceptance; remaining Phase 6/7 obligations remain open. No actual provider acceptance or deployment is claimed.

Signed-delivery browser attempt: session 76586 exit 1, ca6-signed-delivery-browser.log. Desktop reached provider completion, then the test incorrectly required the delivered job to remain in the active queue. Mobile did not run. Corrected only the test: assert delivered customer detail, exact duplicate detail, and absence from active queue. Production application unchanged. Final Instant rerun passed 2 desktop/mobile journeys / 2.3 minutes, session 89402 exit 0, ca6-signed-delivery-browser-verified.log. Screenshots inspected. Extended the same verifier to Scheduled courier completion after manual fallback closes before handover; preserves existing separate inspected-return/manual-completion path. Additional draft files: apps/web/tests/{signed-delivery-events.ts,scheduled-customer-journey.spec.ts}. Types/lint/format pass; architecture and harness 26/26 passed before this test-helper extraction. Combined final rerun follows; no Core suite overlaps.


## CA-7 Problems — locally accepted and pushed as 78685870

**CA-7 / Phase 7 / GD-D11 Problems completed as 78685870:** optional affected items; new customer reports require delivered status; report replay preserves the original response after handling; selected lines and report insertion are guarded. Existing Admin pages/nav say Problems and show New / Being handled / Resolved. Only CLAIM/RESOLVE remain callable; retained INVESTIGATING/ESCALATED rows remain visible in Being handled and resolvable. Recipient phone joins existing customer email and Order links. Handling revalidates Global orders.manage and commits report/audit/frozen result together; legacy reference-only receipts remain distinguishable. No automatic refund, payment or delivery change. Customer browser retains the exact request/key after a lost response.

Verified Problems files (committed, no longer partial): Core admin/application/{finance-commands,finance-reads,order-issue-policy,get-admin-context,admin-finance.integration.test,order-issue-policy.test}.ts; Core index.ts and orders/{domain/order-issue{,.test},application/{get-customer-order-detail,list-customer-order-issues{,.integration.test},submit-customer-order-issue{,.integration.test}}}.ts; contracts admin-finance{,.test}.ts; existing Admin issues pages/actions route, customer Order page, issue-status badge, order-issue-form{,.test}.tsx and Instant browser journey; API_CONTRACTS/DATA_MODEL/STATE_MACHINES plus this checkpoint. No migration or new operator workspace.

Focused command `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/admin-finance.integration.test.ts src/admin/application/order-issue-policy.test.ts src/orders/domain/order-issue.test.ts src/orders/application/submit-customer-order-issue.integration.test.ts src/orders/application/list-customer-order-issues.integration.test.ts src/orders/application/get-customer-order-detail.integration.test.ts` passed **33/6**, session 2103 exit 0, ca7-problems-core.log. Covers report/financial separation, optional lines, wrong owner/location, omitted-audit rollback, one competing winner, replay after resolution and retained legacy-state resolution. Subsequent selected-line guard is included in the passing aggregate below.

Web tests **402/101**, contracts **68/19** passed (ca7-problems-{web,contracts}.log). Workspace types pass after updating the phone contract fixture. Initial type failures were fixture-only and corrected. Aggregate pnpm check passed, session 13202 exit 0, ca7-problems-check.log: 1631 Core/196 files, 402 Web/101, 68 contracts/19, shared/harness/migrations/static checks and both builds. Afterward removed only unused prior command code, aligned the application note bound with the existing 500-character RPC limit, and replaced the report button using an undefined color with the existing shared Button. Final Core focused tests 23/2 passed, session 96281 exit 0, ca7-problems-final-core.log; report form test 1/1, workspace types/lint/format/architecture and final Core dry build passed. Browser alertdialog locator verified against the existing component before execution. Browser session 92240 exited 1: the success assertion matched both the form status and the existing cart status region. Narrowed the test to the expected form message; no application change. Rerun session 72854 exited 0: 2 desktop/mobile journeys passed in 3.1 minutes, ca7-problems-browser-verified.log. Screenshots inspected/copied as ca7-final-problem-resolved-{1440,390}.png. No process remains. Exact command: E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts --retries=0 --max-failures=1. Source remains fixed; no overlapping Core suite. Acceptance covers optional lines, lost-response retry, Admin handling/resolution and unchanged commerce.


## CA-6 and CA-7 completed local milestones through 64c2e66d

Problems is committed/pushed as **78685870**: optional item selection, three Admin states, contact details, guarded handling and frozen replay. Local aggregate **1631 Core/196 files, 402 Web/101, 68 contracts/19** plus final focused checks and **2 desktop/mobile browser journeys** passed. Exact files/commands, corrected locator failure and screenshots are preserved in the existing history. Problem/dashboard notifications remain a separate GD-D19 obligation.

**CA-7 / GD-D19 delivery notifications committed/pushed as a567adfd:** shared delivery-notification statements persist one attempt/event intent inside both provider-observation and manual-delivery batches. The existing projector recognizes EN_ROUTE and shares stable attempt keys, retaining legacy-key deduplication. No schema, transport, new channel or operator procedure. Missing recipient retains failed intent; actual sends remain separate. Files also include Lalamove and manual integration tests, the shared signed-delivery browser helper, API_CONTRACTS and this checkpoint.

Validation: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/delivery/http/lalamove-webhook.integration.test.ts src/delivery/http/grab-express-webhook.integration.test.ts src/admin/application/delivery-provider-operations.integration.test.ts src/notifications/application/notification-outbox.integration.test.ts src/notifications/application/notification-queue.integration.test.ts` passed **67/5**, session 49269 exit 0, ca7-delivery-notifications-core.log. Proves omitted-intent rollback/provider inbox recovery, rapid pickup/completion before projection, duplicate intent/send protection, manual custody rollback/replay, existing send lease/unknown-outcome/retry safeguards. Workspace types/lint/architecture and changed-source formatting pass. Browser **93807 exit 0, four desktop/mobile journeys passed in 8.0 minutes**, ca7-delivery-notifications-browser.log: same managed environment/DB as above, `pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts tests/scheduled-customer-journey.spec.ts --retries=0 --max-failures=1`. The shared helper asserts both pending outbox intents after real paid/prepared signed completion in both modes. No Core suite overlapped; source stayed fixed. Final Core deployment dry run passed, ca7-delivery-notifications-core-build.log; format/naming checks passed. Final Lalamove suite including recipient-unavailable regression passed 13/1, session 88869 exit 0, ca7-delivery-notifications-recipient.log; application source unchanged and final Core types pass. No process remains.


**CA-7 / GD-D19 dashboard committed/pushed as 64c2e66d (tested on a567adfd plus this scope):** Core admin-notifications read is composed into the existing overview, with current selected-scope/capability enforcement. Existing overview card and bell expose the feed; operational links preserve selected scope and filter the existing fulfillment/delivery read by orderId, including terminal rows only for that explicit read. Default active queues remain unchanged. No migration or new operational workspace.

Verified dashboard files: Core admin/application/{admin-notifications,admin-overview,admin-overview.integration.test,operations-reads}.ts, Core index.ts, fulfillment/application/list-fulfillment-queue.ts, delivery/application/list-delivery-dispatch.ts; contracts admin-{overview,overview.test,operations}.ts; Web Admin overview/fulfillment pages, fulfillment/delivery read routes, admin-overview-view{,.test}.tsx, admin-shell.tsx, delivery/external-delivery-queue.tsx; Instant/shared signed-delivery browser tests; notifications/application/project-domain-notifications.ts and notification-outbox.integration.test.ts; API_CONTRACTS and this checkpoint.

Initial focused Core run passed **51/3**, session 79396 exit 0, ca7-dashboard-core.log (`pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/admin-overview.integration.test.ts src/admin/application/admin-operations.integration.test.ts src/admin/application/delivery-provider-operations.integration.test.ts`). Proves selected-location isolation, administrator-only problem visibility, permission revocation, scoped explicit terminal-order reads and unchanged default queues. Initial types identified two overview fixtures missing the new notifications field; both fixed. Workspace types/lint/architecture pass. Final Core run with refund attention passed 51/3, session 55061 exit 0, ca7-dashboard-core-final.log. Web 402/101 and contracts 68/19 passed, session 24309 exit 0, ca7-dashboard-{web,contracts}.log. Retired membership-payment projection removed without deleting retained types/records; notification outbox suite passed 14/1, session 53030 exit 0, ca7-dashboard-retired-notifications.log. Final workspace types/format and Core deployment dry run passed (ca7-dashboard-core-build.log). Browser session 59870 exited 1: both Instant widths passed, but Scheduled then tried another Order while the new explicit delivery filter was still selected. The shared test now clicks the real Show all delivery work link and verifies the unfiltered URL. Reviewed screenshots also prompted a copy-only correction from Delivery needs attention to Delivery attempt failed for historical events. Final workspace types/changed-file formatting pass. Final browser session 12600 exited 0: four desktop/mobile journeys passed in 7.4 minutes, ca7-dashboard-browser-verified.log. Source stayed fixed; no Core suite overlapped. Final dashboard screenshots inspected and retained as ca7-final-dashboard-notifications-{1440,390}.png in the evidence root. No process remains. Command/environment exactly as the prior combined browser: E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts tests/scheduled-customer-journey.spec.ts --retries=0 --max-failures=1. It now checks the dashboard report link and explicit completed-delivery view. This milestone does not complete Phase 7 or replace the final aggregate/provider gates.

### Previous verified CA-6 milestones

Return inspection is committed/pushed as **18fc6fa1**. It records customer agreement and separate inspection evidence, restores packed/ready custody atomically, and allows only the inspected attempt to authorize redelivery. It never restocks, repeats packing consumption, refunds or adds charges. Earlier attempts and original commercial promises remain retained.

**Completed CA-6 local milestone: connected courier completion in both modes**, on 18fc6fa1. Production application remains unchanged. The test entrypoint calls the production signature/inbox/application handler with synthetic credentials; the local gateway forwards only the known callback path. Shared test verification exercises invalid-signature rejection, assignment, pickup, completion, exact duplicates, customer status and completed-order removal from the active queue. Scheduled retains its separate future booking, cancellation/manual fallback, return inspection/redelivery and lost-completion-reply checks.

Acceptance: both actual local paid/prepared journeys reach courier completion through signed callbacks; invalid/duplicate callbacks cannot mutate business state incorrectly; customer views reflect pickup/delivery. This closes local connected evidence only, not actual Lalamove/PayMongo account acceptance or deployment.

Verified acceptance milestone files (commit subject: test(delivery): verify signed completion in both commerce modes): apps/core/tests/browser-entrypoint.ts; apps/web/tests/{provider-gateway.ts,instant-auto-booking.spec.ts,scheduled-customer-journey.spec.ts,signed-delivery-events.ts}; the existing checkpoint/history. No other application, contract or storage change in this acceptance scope.

Evidence root: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347`.

- Return aggregate `pnpm check`: **1629 Core/196 files, 402 Web/101, 68 contracts/19**, shared/harness/migration/static gates and both builds passed; session 92194 exit 0, ca6-return-check-verified.log. Tested 37c7fe46 plus the return scope committed as 18fc6fa1. The earlier seven obsolete-membership test failures and their verified fixes remain in history.
- Return combined browser: **4 desktop/mobile journeys / 7.2 minutes**, 62642 exit 0, ca6-return-browser-verified.log. Exact command below. Test-only stock response normalization fixed the earlier failed assertion; the application was unchanged. Final return screenshots inspected/copied as ca6-final-return-inspection-{1440,390}.png. Mobile uses the existing horizontally scrollable queue.
- Signed Instant browser: **2 desktop/mobile journeys / 2.3 minutes**, 89402 exit 0, ca6-signed-delivery-browser-verified.log. The first run (76586 exit 1) reached completion but incorrectly searched the active queue for the delivered job; corrected to assert customer detail and queue removal. No application fix was needed. Final screenshots inspected.
- Draft checks: Web/Core typechecks, formatting, lint, architecture, and harness **26/26** passed. The final shared helper and Scheduled extension passed Web types/lint/format and architecture; diffs reviewed.
- **Final combined browser: 4 desktop/mobile journeys / 6.9 minutes, session 4230 exit 0**, ca6-both-modes-delivered-browser.log. Tested 18fc6fa1 plus the listed test-only scope. No process remains. Both-mode delivered screenshots inspected/copied as ca6-final-instant-delivered-{1440,390}.png and ca6-final-scheduled-courier-delivered-{1440,390}.png. Command: `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts tests/scheduled-customer-journey.spec.ts --retries=0 --max-failures=1`. No overlapping Core suite; application/test source stays fixed during the run. Only the identified disposable database is recreated.


## 2026-09-10 — CA-3.3 and CA-3.4 local acceptance

CA-3.3 was committed/pushed as e3d4180a; CA-3.4 as 9dd13ed1. The evidence below records their tested working-tree scopes; it is not another active task list.

**CA-3.4 local acceptance on e3d4180a plus the intended scope:** new additive 0092_product_sale_targets.sql retains existing benefits/claims/paid snapshots. Existing quote claims and physical HELD rows reserve sale quantities; no second hold system/job. Implementation adds per-unit item-sale calculations/full-price-code basis, target authoring/activation overlap guards and frozen target receipts, quote item/order discount allocation, payment claim consumption, stock-depletion caps and cancellation-linked restoration. Web Create/Edit now share named Product/option/location selection with optional Instant quantity, paginated catalog reads and frozen command inputs. Core public catalog projects unconditional one-unit sale prices; cart evaluates actual item quantities through the existing promotion policy. Cards/detail/quick-view/cart render Core price decisions. Overlap errors explain the conflict, subtotal-only item-sale preview is rejected, and campaign banners identify automatic selected-item sales. Desktop/mobile connected acceptance passed as recorded below; actual provider acceptance remains outstanding.

CA-3.4 committed files: Core migration 0092; promotions/domain/checkout-promotion{,.test}.ts; promotions/application/{product-sales,evaluate-checkout-promotions}.ts; admin/application/promotion-{commands,reads}.ts; checkout/application/{instant-quote,create-checkout-quote,revalidate-checkout-quote}.ts; orders/application/{apply-checkout-payment-reaction,cancel-order,instant-commitment.integration.test}.ts; inventory/application/{adjust-inventory,inventory-transfers}.ts; contracts/{admin-promotions,checkout}.ts; validation/admin-promotions.ts; this checkpoint. Additional committed files: Core catalog/service.ts, checkout/application/{cart,release-uncommitted-checkout}.ts, admin/application/promotion-media-reads.ts; contracts/{catalog,promotion-media}.ts; Web cart/page.tsx and promotions Create/detail pages and promotion-definition-form.tsx, new promotion-product-targets-editor.tsx, new storefront/product-price.tsx, catalog-components.tsx, product-view.tsx, product-quick-view.tsx, cart-drawer.tsx, promo-banners.tsx, lib/storefront/catalog-presentation.ts. The target/allowance module and two shared Web components were added in 9dd13ed1. Prior quote/order records keep optional-field compatibility.

Current verification on e3d4180a plus CA-3.4 draft: `pnpm check` session 78984 exit 0, ca34-check.log, passed **1639 Core/196, 402 Web/101, 68 contracts/19**, shared/harness/migration/static gates and both builds. It includes the competing last-allowance/abandonment test. Final review then added the quote-refresh payment guard (both modes), STRICT to the new table, and one bound JSON list for sale/rule reads. Workspace `pnpm typecheck`, `pnpm migration:check`, `pnpm lint`, `pnpm architecture:check` and changed-source formatting pass after this delta. Final `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/orders/application/instant-commitment.integration.test.ts src/checkout/application/instant-quote.integration.test.ts src/checkout/application/revalidate-checkout-quote.integration.test.ts src/promotions/application/evaluate-checkout-promotions.integration.test.ts src/catalog/service.integration.test.ts` session 93831 exit 0 passed **71/4**, ca34-final-core.log (the revalidation-named filter matches no file; the other four suites executed). It covers paid/unknown quote-refresh hold preservation, stopped-sale paid commitment, full-price-only grocery percentage stacking, confirmed local-test refund without stock/allowance release, cancellation rollback/replay, stock-cap omission rollback, fresh-stock non-expansion, public/cart sale reads and competing allowance claims. Earlier 3535 failed on D1 bound-variable capacity and the new test using the wrong Product-view envelope; both corrected, then 99847 passed 49/3. Earlier lowercase promotion-code fixture failure was corrected; no application result was faked. Current browser `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts --retries=0 --max-failures=1` session 79972 failed before creating a sale: the test expected an obsolete location label; trace confirms the configured label is Central Cebu. It also exposed an unnecessary empty query parameter in the initial product-list request. Both are corrected; Cart page now also shows regular/sale line prices, with browser checks for the full-line allowance fallback. Types/lint pass after the Web-only correction. Rerun session **74533 exit 0 passed both desktop/mobile journeys in 3.6 minutes**, ca34-browser-final.log: real sale Create/Edit, displayed regular/sale prices, three-unit full-price fallback against a two-unit allowance, discounted cart/quote/payment, cancellation restoration, early stop, and the original delivery/support journey. Signed test-provider events are local evidence only. Four ca34-{product-sale,cart-sale}-{1440,390}.png screenshots were inspected and copied to the evidence root. A final text-only correction labels the cart unit amount as Regular; types/lint/format/architecture pass afterward. Core suites are finished; no overlap. The first shell wrapper ended after printing the log, so its exit status was not the failing Playwright status; the rerun explicitly preserves the native exit code. Application/test source stayed fixed during browser acceptance. No process remains. Final diff and required-effect/permission review passed; commit/push completed as 9dd13ed1. Public/cart read paths and transaction guards reuse existing authorities; no new recovery panel/job was added. Public catalog advertises only unconditional prices; customer/minimum conditions remain checkout decisions. Selected-item maximum-total-discount composition remains outside the implemented per-unit sale form. No actual provider acceptance.


**CA-3.3 committed/pushed as e3d4180a; tested on 64c2e66d plus this scope:** shared max-five bound, optional replacement on the existing upload intent, atomic old-image deactivation/new attachment/cleanup/audit/receipt, shared Add/Edit image controls and detail/quick-view gallery. No schema migration or new recovery surface. New components are shared by the two existing admin and two customer surfaces.

Validation on 64c2e66d plus the listed CA-3.3 draft: `pnpm check` session 67087 exit 0, ca33-check.log, **1635 Core/196 files, 402 Web/101, 68 contracts/19**, shared/harness/migration/static gates and both builds passed. Final review preserved NOT_FOUND before capacity validation and the unsaved Product-details version across image refreshes. Final `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/admin/application/product-media-recovery.integration.test.ts src/catalog/service.integration.test.ts` passed **31/2**, session 31958 exit 0, ca33-final-core.log. Includes cleanup-effect suppression rollback and competing fifth-slot writes, exact replacement replay, storage uncertainty and publication checks. Final workspace types/lint/changed-source formatting pass.

Initial media failures are resolved: RPC validator initially stripped replacement identity; shared fixtures accumulated images; cleanup sweep counted another retained scenario. The RPC field is preserved, scenarios clear prior fixture images through normal removal commands, and cleanup asserts the target identity. No application success was faked. Browser `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/product-media-recovery.spec.ts --retries=0 --max-failures=1` passed in session 69925 (exit 0, two desktop/mobile journeys, 2.7 minutes), ca33-media-browser.log. Final test-only extension waits for all refreshed admin previews and exercises quick view: session 34208 exit 0, two desktop/mobile journeys in 2.6 minutes, ca33-media-browser-final.log. Source stayed fixed during both runs; no Core suite overlapped. Final screenshots inspected/copied as ca33-{published-product-image,product-image-controls,quick-view-gallery}-{1440,390}.png in the evidence root. A final UI-only mutual-disable guard prevents other Product commands interrupting a pending image intent; workspace types/lint/format/architecture passed afterward. No process remains. Browser covers Add Product previews/limit, saved five-image capacity, lost response replay, Edit replacement/main/order/removal and customer gallery/publication. No provider acceptance claimed.
Problems (78685870), transactional delivery notifications (a567adfd), dashboard notifications (64c2e66d) and previous CA-6 local journey evidence are in the existing history. These milestones do not close actual provider or remaining Phase 7 acceptance.

## 2026-09-10 — CA-7.1 local report acceptance

## Current implementation and acceptance

CA-7.1 is implemented in draft across Core/storage/contracts/Web, not accepted. Draft migration 0093 publishes 15 approved reports and preserves superseded/blocked definitions as readable history. The active catalog omits retired membership and extra metrics. Purchases survive later refunds/cancellations; customer counts distinguish first purchase, unique purchasers, returning purchasers and repeat purchases. Payments records a nullable first-success refund date within the existing guarded status update; retained missing dates remain unknown. Received/refunded totals use separate confirmation dates, currency and location attribution. Product quantities union original and committed addition lines; cancellations remain separate. Promotions use committed allocations; delivery charges/costs use the same paid-Order cohort and unknown cost is unavailable.

The Admin page now has explicit local-date/timezone/currency controls, scoped purchased-Product selection, readable report labels and money formatting; it removes misleading one-point trend charts. Product options use a bounded, scope/search-bound cursor through the existing overview authority, so report access does not depend on catalog permission. Web reuses Core's already installed temporal-polyfill 1.0.4 (added offline) for timezone-correct calendar boundaries. Specification updates, diff review, additional evidence gaps, Web/browser/aggregate acceptance and commit/push remain unfinished.

Unfinished intended files: `apps/core/migrations/0093_commerce_reports.sql`; `apps/core/src/analytics/{metric-catalog.ts,metric-definitions.ts,metric-definitions.test.ts}` and `application/{metric-queries.ts,get-analytics-overview.ts,report-products.ts,analytics.integration.test.ts,commerce-reports.integration.test.ts}`; `apps/core/src/payments/infrastructure/d1/payment-repository.ts`; `apps/core/src/payments/application/reconcile-refunds.integration.test.ts`; `packages/contracts/src/admin-analytics.ts`; `apps/web/app/admin/analytics/page.tsx`; `apps/web/app/api/admin/analytics/{overview/route.ts,route-utils.ts}`; `apps/web/package.json`; `pnpm-lock.yaml`; this checkpoint and the existing history file. No CA-7.1 commit, browser or provider acceptance yet.

Validation on working tree based on 9dd13ed1:
- Focused command: `pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/analytics/metric-definitions.test.ts src/analytics/application/analytics.integration.test.ts src/analytics/application/commerce-reports.integration.test.ts src/payments/application/reconcile-refunds.integration.test.ts`. Latest completed session 17159: exit 0, **43 tests / 4 files**. Covers customer/Order history, original/addition quantities and cancellation dates, committed promotions, delivery charge/cost availability, historical definitions, scoped access and missing attribution, first-success refund replay. Earlier fixture failures (shared periods, wrong unpaid status, historical field spelling, missing delivery financial fields) were corrected without weakening guards. Earlier sessions 43463, 80827, 2892 and 23246 failed; 20565 passed 17/2.
- `pnpm typecheck`, `pnpm lint`, `pnpm architecture:check` passed. `pnpm migration:check` passed after the preservation harness separated the 0090–0092 boundary from 0093 and explicitly checked retained formulas/rows, only approved lifecycle changes, 15 new definitions and no invented refund dates. Evidence: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/ca71-migrations.log`. `pnpm --filter @freshmarkets/web exec vitest run --config vitest.config.ts test/app/api/admin/analytics/analytics-routes.test.ts`: 6/1 passed.
- `pnpm check` session 54433 stopped at a mock scope's unsupported marketName field; corrected. Session 47493 stopped at the contract's old dimension vocabulary expectation; added the approved skuId. Both failed before application suites. Logs ca71-check.log and ca71-check-final.log in the same evidence directory. History trailing blank line corrected; `git diff --check` now passes (only LF-normalization warnings).
- Post-focused-test changes: unknown confirmation dates only block periods intersecting their creation/last-recorded interval (neither bound is fabricated as success); exact-integer overflow is unavailable. Added corresponding assertions. Updated API/data specifications, browser mocks, and extended the existing desktop/mobile paid-sale/cancellation/delivery journey to check all 15 report reads, exact counts/received money and scoped Product selection.
- **Aggregate `pnpm check` session 31141 passed**, log `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/ca71-check-complete.log`. Result: **1644 Core / 197 files, 402 Web / 101, 68 contracts / 19**, shared packages/harness/migrations/static checks and both builds passed. Final review then retained historical financial purposes (including retired membership payments) in Global money totals; location totals remain attributable grocery payments. This changes no membership feature or action. Added regression coverage. Whole-workspace typecheck and migration checks passed again. **Final focused Core session 30796 passed: 45 tests / 4 files.** Browser session 62332 failed on the mock page Timezone selector before the four remaining tests ran. The error snapshot showed the correctly named combobox; test selectors now use accessible combobox roles for native selects. No application change was needed. That failed run log is `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/ca71-browser.log`. Command: `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts tests/admin-analytics.spec.ts --retries=0 --max-failures=1`. **Browser rerun session 96257 exited 1: three Admin report tests passed; the desktop report correctly showed no completed cancellation while its mock refund was PROCESSING. The mobile test did not run. The journey now sends/replays signed mock refund success and verifies completed cancellation before reporting.**, same command and disposable state, log `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/ca71-browser-final.log`. Application/test source is fixed; no Core suite may overlap. No completed browser/provider acceptance is claimed yet.

Additional unfinished files beyond the list above: `packages/contracts/src/admin-analytics.test.ts`; `scripts/verify-schema-integrity.mjs`; `apps/web/tests/{admin-analytics.spec.ts,instant-auto-booking.spec.ts}`; `docs/architecture/{API_CONTRACTS.md,DATA_MODEL.md}`. Missing-attribution review gap is fixed: global reports retain paid facts; scoped reports surface missing location evidence. CA-3.3/3.4 evidence remains in the existing history.
Final CA-7.1 gate: browser session 18663 exited 0, 2/2 desktop/mobile actual local Worker/D1 journeys passed after signed mock refund success/replay and completed cancellation. Command: `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts --retries=0 --max-failures=1`. Log: `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347/ca71-browser-refund.log`. Both ca71-commerce-reports-{1440,390}.png copied to that directory and visually inspected. Three Admin report browser tests passed in session 96257; that session's desktop cancellation failure was fixed by completing provider confirmation in the test, not weakening the report. Web typecheck passed after the final browser-test change. Aggregate and post-aggregate focused verification scopes are recorded above. Tested working tree based on 9dd13ed1; no real provider or deployment acceptance. All browser processes exited.

## 2026-09-10 — CA-7.2 Cart and current-price reorder local acceptance

Working tree based on pushed e54d0cb3. Both existing Core Cart projections now include Product and selling-option names through existing joins; no new contract/storage abstraction. Extended existing Cart/reorder assertions and actual desktop/mobile Buy again journey. Admin changes current price, reorder merges exactly into the existing Cart at that price, requires address/fulfillment review, and leaves paid items/financial/version snapshots unchanged.

`pnpm --filter @freshmarkets/core exec vitest run --config vitest.config.ts src/checkout/application/cart.integration.test.ts src/orders/application/reorder-order.integration.test.ts`: session 70496 passed 8 tests / 2 files, including skips, ownership, stale versions and replay. `pnpm --filter @freshmarkets/web typecheck` passed after final test edits; `git diff --check` passed. The preceding CA-7.1 aggregate passed; this two-query display change uses focused verification and does not claim Phase 7 completion.

Browser command: `E2E_START_STACK=1 E2E_PROVIDER_GATEWAY=1 E2E_STATE_NAME=e2e-commerce-alignment-20260907 pnpm --filter @freshmarkets/web exec playwright test tests/instant-auto-booking.spec.ts --retries=0 --max-failures=1`. Session 55764 failed desktop because the test assumed an empty Cart: existing 2 plus reordered 2 correctly returned 4; mobile did not run. Captured existing quantity and asserted exact addition, current-price total and unchanged Order. Final session 44701 passed both widths (2/2, 4.3m). Evidence root `C:/Users/reggi/.codex/visualizations/2026/09/08/01a082ba-d1a3-7ce3-985b-99ed446e7347`: ca72-browser.log, ca72-browser-final.log and ca72-current-price-cart-{1440,390}.png. Both screenshots inspected. No process remains. No actual provider/deployment acceptance. Paid-checkout Cart lifecycle remains an earlier acceptance gap; cleanup must preserve intervening edits.
